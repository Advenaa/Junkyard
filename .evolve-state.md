# Evolve State

Cycle: 364
Last action: verify (Discord REST polling refactor — E2E flow audit + live VPS test)
Last research type: roadmap-gap
Tests: node --import tsx/esm --test test/unit/first-mover-watchlist.test.ts test/unit/first-mover-watchlist-api.test.ts test/unit/influencer-dashboard.test.ts, cd dashboard && npx vitest run src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/Settings.influencer.test.tsx, npm run format:check, npm test (2020 passing), npm run test:dashboard (227 passing), npm run build

---

## User Priorities

Items added by the user. Evolve picks these up at P0 — before everything else.

- [x] ~~**Discord server/channel browser**~~ — Completed in Cycles 139-141. Backend API + frontend picker + verify.
- [x] ~~**Discord token management UI**~~ — Completed in Cycles 142-145. Settings can add/remove/enable/disable managed tokens, show live gateway health, and distinguish env vs DB tokens.
- [x] ~~**Account management**~~ — Completed in Cycles 146-148. Admins can invite users, review self-service access requests, and audit invite/role/request decisions from Settings.
- [x] ~~**Proxy management**~~ — Completed in Cycle 149. Managed Discord tokens now support encrypted per-token proxy configuration across REST discovery, live gateway connections, and Settings health views.
- [x] ~~**Twitter/X source management UI**~~ — Completed in Cycle 269. @handle validation, API key status, search query hints, error decoding, source type badges.
- [x] ~~**Discord REST polling refactor**~~ — Completed in Cycle 363. Gateway unwired from startup, REST polling is primary ingestion, debug logging removed, token health API returns REST-based status, dashboard updated.

## Build Progress

Tracks features from docs/ROADMAP.md. Status: planned | in-progress | done | verified.

| # | Feature | Status | Verified | Cycles | Notes |
|---|---------|--------|----------|--------|-------|
| 1.0 | Core pipeline (RSS to report) | done | yes | 1-80 | Phase 1 complete |
| 1.1 | Pre-summarize | done | yes | — | Decision tree, atomic claims |
| 1.2 | Market pulse (3h Sonnet) | done | yes | — | Output scales with activity |
| 1.3 | Raw Discord feed | done | yes | — | Live mirror on dashboard |
| 1.4 | Embeddings (Gemini) | done | yes | — | 768-dim, summary + report |
| 1.5 | RAG chat | done | yes | — | Sonnet + Gemini embed |
| 1.6 | Discord Gateway (multi-token) | done | yes | — | Circuit breaker, rate limits |
| 1.7 | Twitter/X via twitterapi.io | done | yes | — | REST API, $0.15/1K |
| 1.8 | News article extraction | done | yes | — | Readability, SSRF-safe |
| 1.9 | Entity resolution + CoinGecko | done | yes | — | Alias table, decay |
| 1.10 | Dashboard (all pages) | done | yes | 129-137 | Reports, Feed, Chat, Search, Settings |
| 1.11 | Discovery endpoints | done | yes | 139-141 | Backend (139), frontend picker (140), verify + fixes (141) |
| 2.1 | Sentiment momentum | done | yes | — | DST-safe, idempotent |
| 2.2 | Narrative clustering | done | yes | — | K-means + silhouette |
| 2.3 | Event chains | done | yes | 157-253 | Stage 1 now extracts and stores structured entity-linked events, same-entity follow-ups can inherit the prior chain root, daily/pulse prompts receive recent linked-chain context, and reports, report-list UI, webhook delivery, dashboard search, plus chat entity lookups and semantic search now surface concise chain summaries while timeline-style chat queries auto-route omitted-type semantic search to report mode, the chat UI shows when report search was used, chat responses return report/summary citations, summaries have a real detail surface for citation drilldown, search hits deep-link into the right report or summary detail page, `read_raw` citations deep-link into a raw item detail view, raw item drilldowns now show nearby same-source context, the raw feed can deep-link into raw item details and back into the same source, feed deep-links now pin older cited messages with nearby context even when they are not on the latest live page, focused feed context cards can now refocus the pinned timeline in place instead of forcing a bounce to the raw item detail page, the focused feed state now includes a clear resume-live path plus a visible handoff line showing where the live stream picks up after the pinned citation window, raw item context cards can now jump straight into the focused feed timeline instead of only hopping between isolated item-detail pages, the raw-item detail header now exposes the same focused-vs-live feed split so both surfaces share the same top-level timeline actions, the main raw-item card now uses the same focused-card visual language plus inline timeline actions as the feed’s pinned citation surface, the feed plus item-detail surfaces now share a real raw-message card component so their layout and actions stay behaviorally locked together, the remaining raw-message attachment/time/footer helpers used by those surfaces now live in one shared dashboard helper module instead of being duplicated across pages, the focused raw-feed route builders plus active-state predicate now live in one shared navigation helper module instead of being split across the feed and item-detail pages, the raw item response types plus normalization used by the feed-focused context and item-detail page now live in one shared dashboard model/helper module instead of being duplicated between those surfaces, the remaining focused raw-feed pinning state plus `/items/:id?context=` fetch lifecycle now live in one shared dashboard hook instead of staying page-local inside `/feed`, the remaining raw-feed source selection, page loading, load-more dedupe, and live polling orchestration now live in one shared dashboard stream hook instead of being embedded in the page component, the remaining focused-citation panel plus context-row rendering now live in a shared dashboard component instead of being embedded directly in `Feed.tsx`, the remaining live-stream list rendering now lives in a shared dashboard component instead of being embedded in the feed page, the remaining feed header controls plus retryable error banner now live in a shared dashboard header component instead of being embedded inline in the route page, the remaining no-sources fallback now lives in a shared dashboard component instead of being embedded in the feed route, the raw-item drilldown source-context panel now lives in a shared dashboard component instead of staying page-local inside `ItemView.tsx`, the remaining item-detail header plus metadata chrome now live in shared dashboard components instead of staying inline in the route page, the remaining item-detail loading/error/not-found route states now live in a shared dashboard component instead of staying page-local inside `ItemView.tsx`, the remaining `/items/:id?context=` fetch lifecycle plus route-state derivation now live in a shared raw-item view hook instead of staying page-local inside `ItemView.tsx`, raw-item drilldowns now expose direct previous/next-in-source hops in the shared item header so citation browsing can move sequentially without dropping back into the context list each time, the focused feed’s pinned citation card now mirrors that same linear previous/next-in-source navigation so older deep-linked timelines can be walked directly from the pinned surface itself, the repeated raw-message action builders for feed cards, focused citations, and raw-item drilldowns now live in one shared dashboard helper module so those surfaces stay behaviorally aligned as the navigation model keeps evolving, the repeated top-level citation navigation chrome for focused-feed and raw-item surfaces now lives in one shared dashboard component so their surrounding link rows stay visually and behaviorally locked too, the repeated “Earlier/Later” raw-message context section shell now lives in one shared dashboard component so focused-feed and raw-item timeline bands stay structurally aligned as those surfaces keep changing, the repeated panel-level citation title/description/aside framing now lives in one shared dashboard component so the focused-feed and raw-item wrappers read like one continuous drilldown experience instead of adjacent one-off layouts, the shared timeline-divider chrome now carries both the focused-feed live-resume handoff and the raw-item source-context handoff so those transitions read like one continuous timeline grammar instead of separate page-specific separators, the shared bordered citation-surface wrapper now frames both the focused-feed panel and the raw-item primary drilldown so the two destinations feel more like one continuous citation surface instead of separate page shells, the focused-feed destination now reuses the same metadata summary grid as the raw-item page so both citation destinations close with the same source/capture/flag detail instead of diverging at the footer, the raw-item drilldown now opens with the same shared citation header framing as the focused-feed destination so both routes introduce the timeline surface with matching explanatory context, the raw-item citation header now exposes focused/live feed navigation plus the source identifier in the shared top-of-surface chrome so both drilldown destinations surface their timeline routing context before the main card content, the focused-feed citation header now exposes a direct raw-item drilldown link alongside live-feed navigation so both destinations surface their sibling route switch in the same top-level citation chrome instead of burying it in the body card, the focused-feed destination now reuses the shared item-style source/timestamp header for top-level previous/next navigation so linear timeline browsing appears in the same upper-shell location on both drilldown surfaces instead of only in the pinned card footer, the focused-feed pinned citation now also exposes `Open source link` through the shared primary action path so it matches the raw-item drilldown’s ability to jump straight to the original source URL from the citation surface itself, nearby earlier/later context cards across both drilldown destinations now also expose direct source links when their raw items carry original URLs so you can jump straight from surrounding context into the original source without detouring through `/items/:id`, the focused-feed context bands now surface item counts plus per-card status badges so nearby-context density and processing state are visible without leaving the pinned timeline surface, pinned citations in `/feed` can now expand their same-source context window in place from the default 2 messages per side to a wider 5-message view with same-item refetch support instead of forcing a route hop for deeper context, `/items/:id` can now expand its nearby same-source context window in place from 3 to 6 items per side while keeping the current raw item stable during the larger-context fetch, both drilldown destinations now preserve the current citation surface when wider context fetches fail while exposing inline retry controls instead of blanking or silently stalling, the remaining expand/loading/error/retry context-window chrome for those surfaces now lives in one shared dashboard component so the two drilldown destinations stay aligned as the citation UX keeps evolving, widened context windows now stay widened while you continue stepping through adjacent cited items instead of snapping back to the default window on every navigation hop, the shared raw citation route helpers now carry target-aware `context` query params across `/feed` and `/items/:id` so sibling route switches preserve as much nearby same-source context as the destination surface supports, summary drilldowns now replace parsed-only extracted events with persisted event-chain context when available so users can see event timestamps, chain position, and linked timeline spans directly on `/summaries/:id`, linked-summary hops now preserve focused chain context through query-string routing so the destination summary stays anchored to the same story instead of dropping users into an unfocused event list, report detail pages now surface persisted chain drilldown cards that jump straight into the latest focused summary in an active chain instead of stopping at static chain copy, report list plus Search previews now expose direct active-chain links into those focused summary drills while still preserving the normal report-detail path, and those preview surfaces can now carry more than one active chain at once so multi-story reports no longer force users through an arbitrary single drilldown, while also indicating the exact number of hidden extra active chains beyond the visible two, letting users reveal those extra active-chain links inline on report-list and Search cards through a shared preview section with in-place loading and retry instead of forcing a full report hop, and now keep those expanded inline chain links sticky across remounts and rerenders in the same session so users do not have to reopen the same story set repeatedly, while also letting users collapse those sticky inline expansions back to the default preview in place when they only want the top story set again, now reconcile cached inline expansions against the current visible plus hidden chain set so stale story sets drop away when report previews change or shrink while partial cached expansions still keep `Show more` available, and now let fully expanded inline chain previews refresh the latest active-chain set in place instead of forcing a collapse/reset to pick up newer hidden-story changes while keeping the shared Event Chain preview summary sentence in sync when those inline previews refresh or collapse, with the surrounding report/search preview body copy now following the same refresh/collapse lifecycle instead of getting stuck on the older list/search payload, and the top-row report/search preview chrome now exposes a live `Lead chain` label that follows the current active story instead of staying frozen on the original preview order, plus a live `Active stories` count that tracks the currently visible story set during expansion and collapse instead of staying buried inside the inline preview section, while the live `Lead chain` chip now also acts as a direct focused-summary drilldown link that follows preview refresh and collapse instead of staying static text, the `Active stories` chip now drives the same inline expand/collapse preview state from the card header instead of mirroring that state passively, fully expanded report/search cards now surface a top-row `Refresh stories` control that refetches the current active-story set in place instead of forcing users back down into the inline preview controls, those same header chips now show explicit loading copy like `Loading stories...` and `Refreshing stories...` so the card chrome itself explains when an inline story update is in flight, failed inline preview fetches now also keep the recovery path visible in the card chrome with header-level retry labels like `Retry loading stories` and `Retry refresh stories` instead of hiding that state only in the lower inline preview error row, report detail drilldowns can now honor a focused `chain` query param so `/reports/:id?chain=...` moves the requested story to the top, highlights it, and exposes clear focus/switch/reset controls instead of leaving multi-chain reports as a flat unfocused grid, and the shared inline preview section now exposes an `Open focused report` link that follows the current lead story across expansion, refresh, and collapse so report-list and Search cards can hand users directly into that anchored report-detail route instead of only into the generic report page. Focused /reports/:id?chain=... routes now also reorder and highlight the matching textual Event Chain summary so the top summary panel stays aligned with the selected story instead of leaving only the drilldown cards focused. The main report-card body links on report-list and Search now also follow that current focused report-chain route from the shared preview state, so expanding, refreshing, or collapsing the active-story preview retargets the primary report click path instead of leaving it on the generic report page. Chat report citations now also carry focused report-chain routes when report-mode semantic search can identify a lead chain, so those source pills open directly into the right anchored report detail instead of always landing on the generic report route. Chat summary citations now also carry focused summary-chain routes when summary-mode semantic search can identify persisted chain context, so those source pills open directly into the right anchored summary drilldown instead of always landing on the generic summary route, and anchored report/summary chat pills now visibly mark focused chains with richer tooltip context so users can tell when a citation will open a story-specific drilldown before clicking, while repeated same-type citations from the same focused chain now collapse into one grouped chat pill with a visible count badge and merged tooltip context so chat source rows stay scannable when multiple citations point at the same story, and those grouped chat pills can now reveal the hidden same-chain citations inline through lightweight show/hide controls so users can recover the per-citation drilldown depth without giving up the scannable grouped row, with those revealed same-chain links now carrying compact inline snippet previews so chat users can judge which follow-up citation to open without relying only on the label or the grouped tooltip, and now also showing lightweight date cues pulled from the semantic-search result metadata so the expanded citation list is easier to scan chronologically, plus relative `Earlier`, `Later`, and `Same day` badges so revealed citations are easier to compare against the grouped lead at a glance, while grouped lead pills now also surface a `Lead cite` anchor plus the lead citation date so those relative badges have a clear inline reference point. The current citation/detail surfaces are formatter- and lint-clean for CI, and Settings source actions now use query-string `sourceId` routing so blank/URL-shaped source IDs can still be toggled, edited, and deleted |
| 2.4 | Competitor mapping | done | yes | 257-259 | Migration 22, CRUD queries, REST API, startup seed runner, Stage 1 inferred relationship persistence, and Settings entity detail + relationship management UI |
| 2.5 | Cycle detection | done | yes | 150-156 | Market calendar now supports recurrence, lifecycle refresh, linked entities, asset-aware catalyst context in reports, entity autocomplete in the admin calendar modal, and catalyst panels in report output/UI |
| 2.6 | Pre/post event analysis | done | yes | 153 | Daily + pulse prompts now receive structured pre/post sentiment shifts for recent calendar catalysts |
| 2.7 | Regional divergence | done | yes | 267-268 | Per-entity EN vs ID divergence API, top divergent entities API, Settings entity detail divergence card with sentiment bars + direction + badge + days selector. Verified Cycle 268: fixed null sentiment crash + dead 404 path, 1 non-blocking finding queued |
| 3.1 | Price feeds | done | yes | 271-274 | CoinGecko API, daily price tracker, prompt injection with contrarian signals, entity price API + dashboard card, report priceAlerts. Verified Cycle 275: 4-agent audit (E2E flow, dashboard, API contracts, edge cases) — all pass, no blocking issues |
| 3.2 | Influencer tracking | done | yes | 280-284 | Authors + author_calls foundation, Stage 1 author/claim extraction, Settings author surfaces, admin claim review, and live browser verification of the review flow |
| 3.3 | Cross-market correlation | done | yes | 287-293 | FRED-backed macro snapshots, daily tracker wiring, daily/pulse macro context, a `/macro` API and Pipeline macro backdrop card, first-class report `macroAlerts` surfaces, Discord delivery support for macro alerts, preview visibility on report list/search cards, and full roadmap indicator coverage including gold. Verified Cycle 293 via macro-surface contract audit plus full unit/dashboard/build sweep |
| 3.4 | Macro regime detection | done | yes | 294-297 | Report-level `macroRegime` contract plus persisted daily `macro_regimes` history, report-detail streak context, previous-regime change visibility, preview chips on report list/search cards, and a full contract-audit verification sweep across detail, previews, and delivery |
| 3.5 | Entity relationship graph | done | yes | 260-266 | Expanded structured relationship types, nullable summary-backed evidence on inferred relationships, optional temporal `since` / `until` bounds, a direct-neighborhood graph with drilldown + connected-entity inspection, and a dedicated two-hop graph API. Verified Cycle 266: 4-agent audit (E2E flow, API contracts, dashboard UI, edge cases) — all layers pass, no blocking issues |
| 3.6 | Alpha decay tracking | done | yes | 276-279 | Source tiers, alpha propagation tracking, prompt injection, API, dashboard. Verified Cycle 279: 1 blocking fix (tier PATCH handler), 8 non-blocking queued |

## Feature Work Log

Active multi-cycle builds tracked here. Each sub-task must leave the build passing.

(Discord Server/Channel Browser completed — Cycles 139-141. See Build Progress table.)

### Discord Token Management UI (verified)
Started: Cycle 142
Estimated: 4 cycles
User Priority: Discord token management UI

Sub-tasks:
- [x] Cycle 142: Foundation — migration 14 (discord_tokens table), AES-256-GCM encryption module, token CRUD queries, API endpoints (GET/POST/DELETE/PATCH /discord/tokens)
- [x] Cycle 143: Integration — reconnect() on adapter, updateTokens() on REST, loadAllTokens() merges env+DB, health endpoint, POST/DELETE trigger reload
- [x] Cycle 144: Dashboard — Token management UI in Settings (add/remove/disable managed tokens, live gateway health, env-vs-DB runtime labeling, 204-safe apiFetch)
- [x] Cycle 145: Verify end-to-end — Browser-verified with Playwright against a static preview using mocked auth/API/runtime token health. Add, disable, re-enable, and remove flows passed; screenshots captured.

Design decisions:
- Tokens encrypted at rest with AES-256-GCM (PBKDF2 key derivation)
- Encryption key from TOKEN_ENCRYPTION_KEY env var, fallback to SESSION_SECRET
- DB tokens coexist with env var tokens (env = system, DB = managed)
- Token status in DB is 'active'/'disabled' only; runtime health (connected/rate_limited/banned) tracked in-memory by Gateway adapter
- GET endpoint masks tokens (first 10 + last 4 chars)
- CLAUDE.md "no secrets in DB" exception: encrypted tokens required for UI management

### Account Management (verified)
Started: Cycle 146
Estimated: 3 cycles
User Priority: Account management

Sub-tasks:
- [x] Cycle 146: Invite flow — POST /users/invite, Users tab invite modal, pending invite rows, and update-only PATCH /users/:discordId. Browser-verified on the dashboard with mocked auth/API state.
- [x] Cycle 147: Audit log foundation — migration 15 (`user_audit_log`), GET /users/audit, and a Recent Access Activity panel in Settings > Users. Browser-verified for invite + role-change activity.
- [x] Cycle 148: Self-service role request flow + full verification — rate-limited public `POST /access-requests`, admin review via `GET/PATCH /access-requests`, login-page Request Access modal, pending request review UI, and audit entries for approvals/rejections. Browser-verified across login and admin dashboards.

### Proxy Management (verified)
Started: Cycle 149
Estimated: 1 cycle
User Priority: Proxy management

Sub-tasks:
- [x] Cycle 149: Encrypted per-token proxy support — migration 18 (`discord_tokens.proxy_url_*`), proxy-aware token reload in `index.ts`, `ProxyAgent`/`HttpsProxyAgent` support in Discord REST + Gateway, Settings proxy controls, and focused backend/dashboard tests.

### Cycle Detection (in-progress)
Started: Cycle 150
Estimated: 4 cycles
Roadmap Feature: 2.5 Cycle detection

Sub-tasks:
- [x] Cycle 150: Market calendar foundation — migration 19 (`calendar_events`), calendar query/tracker helpers, authenticated `/calendar-events` API, Pipeline tab market calendar management, and daily/pulse prompt injection for upcoming catalysts. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 151: Recurrence support + editing flows — recurring rules (`daily`, `weekly`, `monthly`, `quarterly`) now validate on create/update, upcoming catalyst prompts display recurrence metadata, and the Pipeline calendar UI supports editing existing events plus recurrence selection. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 152: Scheduler-driven lifecycle + richer report context — recurring events now auto-advance on startup + health checks, stale one-time events are pruned, and daily/pulse prompts receive both recent and upcoming calendar catalysts with regression coverage.
- [x] Cycle 154: Linked entities for market calendar events — migration 20 adds optional `calendar_events.entity_id`, calendar APIs resolve entity names/aliases to canonical IDs, the Settings modal can attach a known entity, and recent/upcoming/pre-post catalyst prompts now carry linked-entity context so event analysis can scope to the relevant asset instead of the whole market.
- [x] Cycle 155: Entity autocomplete for linked calendar assets — authenticated `GET /entities/search` now supports alias-aware prefix suggestions, and the Settings calendar modal shows canonical entity picks while creating or editing linked market catalysts. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 156: Catalyst visibility in reports — daily/pulse prompts now return optional `marketCatalysts`, `/reports/:id` exposes the field, and the report view renders a dedicated Market Catalysts panel so scheduled risks are visible alongside the report itself. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.

### Pre/Post Event Analysis (verified)
Started: Cycle 153
Estimated: 1 cycle
Roadmap Feature: 2.6 Pre/post event analysis

Sub-tasks:
- [x] Cycle 153: Pre/post sentiment context for recent catalysts — new `getSentimentShiftAroundTime()` SQL helper compares the 48h before vs the 24h after recent calendar events, and both daily/pulse prompts now receive a structured `<recent_event_analysis>` block with sentiment delta + mention counts. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.

### Event Chains (in-progress)
Started: Cycle 157
Estimated: 3 cycles
Roadmap Feature: 2.3 Event chains

Sub-tasks:
- [x] Cycle 174: CI integration entrypoint repair — restored `npm run test:integration` via `scripts/run-integration-tests.mjs`, so GitHub Actions can execute real integration files when present and cleanly skip when `test/integration/` is empty. Verified locally with `npm run test:integration`.
- [x] Cycle 175: Source action routing hardening — Settings now uses query-string `sourceId` for toggle/edit/delete actions, blank labels fall back to a safe display name, and the API accepts `/api/v1/sources/:source?sourceId=...` so legacy empty IDs and RSS URL IDs can be managed again. Verified by focused dashboard + regression tests.
- [x] Cycle 176: Raw item source context drilldown — `GET /items/:id` now supports a bounded `context` query that returns neighboring raw items from the same source stream, and the dashboard raw item page now renders earlier/later context cards with direct links into sibling raw items so chat citations have surrounding conversation instead of a single isolated message. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 177: Feed/detail drilldown loop — the raw feed now honors a `sourceId` query when opened from a drilldown, feed cards link directly to `/items/:id`, highlighted `itemId` route context is preserved when available, and raw item detail pages now offer a source-aware return link back into the same feed stream. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 178: Focused citation pinning in raw feed — when `/feed` opens with a `sourceId` plus older `itemId`, the page now fetches `/items/:id?context=2` and pins that cited message with nearby earlier/later context above the live feed, so returning from a citation still lands near the exact message even when it is older than the newest feed page. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 179: In-feed refocus navigation — focused citation context cards in `/feed` now offer in-place `Focus here` links that retarget the pinned citation timeline to an earlier/later neighboring raw item without leaving the feed page, while still keeping a secondary `Open item` path to the dedicated raw item detail view. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 180: Live-stream handoff cues for focused citations — pinned citation mode in `/feed` now shows a `Resume live feed` route back to the plain stream plus a small handoff separator that explains roughly how far later the current live feed resumes, so the focused citation window feels connected to the main timeline instead of like a separate surface. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 181: Raw-item context links into focused feed — nearby context cards on `/items/:id` now expose `View in feed` links that open `/feed?sourceId=...&itemId=...` directly for those neighboring messages, so item-detail navigation can jump straight into the focused feed timeline instead of bouncing only between isolated raw-item pages. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 182: Raw-item header timeline actions aligned — the main `/items/:id` header now separates `View in focused feed` from `Resume live feed`, matching the focused-feed surface instead of collapsing both paths into one ambiguous back link. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 183: Main raw-item card aligned with focused feed — the primary item card on `/items/:id` now uses focused-card styling, a visible `Focused item` badge, and inline `View in focused feed` / `Resume live feed` / `Open source link` actions so the detail page reads like the same timeline surface as the pinned citation card in `/feed`. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 184: Shared raw-message card component — extracted the duplicated feed/item-detail raw-message markup into a shared dashboard component so `/feed`, the focused citation panel, the main `/items/:id` card, and raw-item context cards now share one rendering path for badges, attachments, footer metadata, and timeline actions. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 185: Shared raw-message helper module — extracted the remaining duplicated attachment normalization plus raw-message time/footer formatting into `dashboard/src/lib/rawMessages.ts`, with `Feed.tsx` and `ItemView.tsx` now consuming the same helper paths instead of maintaining parallel parsing/formatting logic beside the shared card component. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 186: Shared raw-feed navigation helpers — extracted focused/live raw-feed route builders plus the focused-mode predicate into `dashboard/src/lib/rawFeedNavigation.ts`, added direct helper coverage, and updated `Feed.tsx` plus `ItemView.tsx` to consume the same navigation model instead of each page maintaining its own feed-link logic. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 187: Shared raw-source-item model — extracted the raw item response/context types plus normalization into `dashboard/src/lib/rawSourceItems.ts`, added direct helper coverage, and updated `Feed.tsx` plus `ItemView.tsx` to consume the same raw-item model path instead of carrying near-duplicate focused-context/item-detail types and normalization helpers. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 188: Shared focused raw-feed orchestration hook — extracted the pinned raw-feed active-state, `/items/:id?context=` fetch lifecycle, and live-gap label logic into `dashboard/src/lib/useFocusedRawFeed.ts`, added direct hook coverage, and updated `Feed.tsx` to consume the same shared focused-feed orchestration layer instead of keeping that state machine page-local. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 189: Shared raw-feed stream hook — extracted `/feed` source selection, initial page loading, load-more dedupe, and live polling into `dashboard/src/lib/useRawFeedStream.ts`, added direct hook coverage, and updated `Feed.tsx` to consume that shared stream state instead of embedding the feed orchestration inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 190: Shared focused-citation feed panel — extracted the focused citation panel, earlier/later context rows, and timeline-gap handoff into `dashboard/src/components/FocusedRawFeedPanel.tsx`, added direct component coverage, and updated `Feed.tsx` to consume that shared surface instead of embedding the focused-citation markup inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 191: Shared live feed list surface — extracted the loading state, empty state, focused-item card rendering, and load-more UI into `dashboard/src/components/RawFeedMessageList.tsx`, added direct component coverage, and updated `Feed.tsx` to consume that shared live-stream surface instead of embedding the list rendering inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 192: Shared raw-feed header chrome — extracted the raw feed title, source selector, live/paused controls, and retryable error banner into `dashboard/src/components/RawFeedHeader.tsx`, added direct component coverage, and updated `Feed.tsx` to consume that shared header surface instead of embedding the remaining shell controls inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 193: Shared no-sources raw-feed shell — extracted the “No Discord sources configured” page shell into `dashboard/src/components/RawFeedNoSources.tsx`, added direct component coverage, and updated `Feed.tsx` to consume that shared fallback instead of embedding the final no-sources route shell inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 194: Shared item-view source-context panel — extracted the `ItemView` source-context section, earlier/later context cards, and feed/item actions into `dashboard/src/components/RawItemContextPanel.tsx`, added direct component coverage, and updated `ItemView.tsx` to consume that shared context surface instead of embedding it inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 195: Shared item-view chrome components — extracted the raw-item header into `dashboard/src/components/RawItemHeader.tsx` and the metadata summary grid into `dashboard/src/components/RawItemMetadataGrid.tsx`, added direct component coverage, and updated `ItemView.tsx` to consume those shared surfaces instead of embedding the remaining item-detail chrome inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 196: Shared item-view route states — extracted the `ItemView` loading, error, and not-found states into `dashboard/src/components/RawItemRouteState.tsx`, added direct component coverage, and updated `ItemView.tsx` to consume that shared route-state surface instead of embedding the remaining route-only fallbacks inline. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 197: Shared raw-item view hook — extracted the remaining `/items/:id?context=` fetch lifecycle, normalization, request-key gating, and route-state derivation into `dashboard/src/lib/useRawItemView.ts`, added direct hook coverage, and updated `ItemView.tsx` to consume that shared orchestration layer instead of keeping the raw-item state machine page-local. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 198: Sequential raw-item source hops — updated the shared `RawItemHeader` to surface direct previous/next-in-source links, wired `ItemView.tsx` to the nearest older/newer context items from `/items/:id?context=`, and expanded the route/component coverage so raw citation browsing can move linearly through the same source stream without manually re-picking from the context list. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 199: Linear navigation in focused feed — updated the focused citation card in `dashboard/src/components/FocusedRawFeedPanel.tsx` to expose direct previous/next-in-source links using the nearest older/newer pinned-context items, and expanded component plus route coverage so deep-linked feed timelines can be browsed linearly from the pinned surface instead of only through the side context cards. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 200: Shared raw-message action builders — extracted the repeated raw-message action composition into `dashboard/src/lib/rawMessageActions.ts`, switched the live feed, focused feed, raw-item detail, and raw-item context surfaces to use those shared builders, and added direct helper coverage so feed/item navigation labels and hrefs stay behaviorally locked together across surfaces. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 201: Shared citation navigation chrome — extracted the repeated top-level citation link row into `dashboard/src/components/RawCitationNavLinks.tsx`, switched both the focused-feed panel header and raw-item header to consume it, and added direct component coverage so the surrounding navigation chrome stays consistent as citation flows continue to evolve. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 202: Shared timeline context sections — extracted the repeated “Earlier/Later” raw-message section shell into `dashboard/src/components/RawMessageContextSection.tsx`, switched both the focused-feed panel and raw-item context panel to consume it, and added direct component coverage so their timeline bands stay structurally aligned while still supplying surface-specific badges, actions, and count labels. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 203: Shared citation panel headers — extracted the repeated panel-level title/description/aside framing into `dashboard/src/components/RawCitationPanelHeader.tsx`, switched both the focused-feed panel and raw-item context panel to consume it, and added direct component coverage so the explanatory wrapper chrome stays aligned alongside the already-shared cards, links, and timeline sections. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 204: Shared timeline handoff divider — extracted the focused-feed live-gap separator into `dashboard/src/components/RawTimelineDivider.tsx`, reused it for a new raw-item “nearby source context” handoff cue before the source-context panel, and added direct component plus route coverage so both drilldown surfaces share the same timeline-transition chrome. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 205: Shared citation-surface wrapper — extracted the common bordered citation shell into `dashboard/src/components/RawCitationSurface.tsx`, switched both the focused-feed panel and the raw-item primary drilldown surface to consume it, and added direct component coverage so the two citation destinations now share the same top-level visual wrapper instead of separate page-local shells. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 206: Shared metadata footer surface — reused `dashboard/src/components/RawItemMetadataGrid.tsx` inside the focused-feed panel so the pinned citation destination now exposes the same source/capture/flag summary as the raw-item page, and expanded component plus route coverage so both citation destinations stay aligned through their closing detail area as well as their main timeline surfaces. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 207: Shared primary citation framing — reused `dashboard/src/components/RawCitationPanelHeader.tsx` at the top of `dashboard/src/pages/ItemView.tsx` so the raw-item drilldown now opens with the same explanatory citation framing as the focused-feed destination instead of dropping straight into the raw-item header, and extended the item-route coverage to lock that route-level continuity in place. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 208: Shared top-of-surface citation nav — added source-aware focused/live feed links plus the source identifier to the shared citation header row in `dashboard/src/pages/ItemView.tsx`, so the raw-item drilldown now surfaces its feed-routing context in the same top-level chrome style as the focused-feed destination instead of keeping that navigation only down on the main card. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 209: Focused-feed sibling route nav — added a direct `Open raw item` link to the shared citation header row in `dashboard/src/components/FocusedRawFeedPanel.tsx`, so the pinned feed citation now surfaces its sibling raw-item drilldown route in the same top-level citation chrome style the raw-item page uses for feed routing instead of keeping that switch only on the main card. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 210: Shared top-level sequence header — reused `dashboard/src/components/RawItemHeader.tsx` inside `dashboard/src/components/FocusedRawFeedPanel.tsx` with a custom label so the pinned citation now exposes source/timestamp plus previous/next-in-source navigation in the same upper-shell location as the raw-item drilldown, and simplified the pinned-card action set to avoid duplicating that linear navigation in the footer. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 211: Focused-feed source-link parity — extended the shared focused-feed primary action builder so the pinned citation now exposes `Open source link` when the cited item has an original URL, and added component plus route coverage so the feed-side citation surface matches the raw-item drilldown’s direct-source escape hatch instead of forcing an extra click through `/items/:id`. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 212: Context-card source-link parity — extended the shared focused-feed and raw-item context action builders so earlier/later neighboring cards now expose `Open source link` when their raw items carry original URLs, and added helper, component, plus feed-route coverage so surrounding context can jump straight to the source instead of forcing an intermediate raw-item drilldown. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 213: Focused-feed context visibility — added count labels and per-card status badges to the focused-feed “Earlier/Later” context sections in `dashboard/src/components/FocusedRawFeedPanel.tsx`, and expanded component coverage so nearby-context density plus processing state stay visible on the pinned timeline surface instead of only on the raw-item drilldown. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 214: Expandable pinned-feed context window — added an in-place `Show more context` path to `dashboard/src/components/FocusedRawFeedPanel.tsx`, wired `dashboard/src/pages/Feed.tsx` to expand pinned citation context from 2 to 5 same-source items per side, and tightened `dashboard/src/lib/useFocusedRawFeed.ts` so widening the context window refetches once for the same pinned item without bouncing through duplicate requests. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 215: Expandable raw-item context window — added an in-place `Show more context` path to `dashboard/src/components/RawItemContextPanel.tsx`, wired `dashboard/src/pages/ItemView.tsx` to expand nearby same-source context from 3 to 6 items per side, and tightened `dashboard/src/lib/useRawItemView.ts` so larger same-item context loads keep the current raw item visible while the wider context window is fetched. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 216: Retryable wider-context failures — tightened `dashboard/src/lib/useFocusedRawFeed.ts` and `dashboard/src/lib/useRawItemView.ts` so failed wider-context fetches preserve the current pinned citation or raw item instead of blanking out, then added inline retry/error feedback to `dashboard/src/components/FocusedRawFeedPanel.tsx` and `dashboard/src/components/RawItemContextPanel.tsx` so both drilldown destinations can recover in place after expansion errors. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 217: Shared context-window controls — extracted the repeated expand/loading/error/retry context-window chrome into `dashboard/src/components/RawContextWindowControls.tsx`, switched both `dashboard/src/components/FocusedRawFeedPanel.tsx` and `dashboard/src/components/RawItemContextPanel.tsx` to consume it, and added direct component coverage so the two drilldown destinations stay visually and behaviorally aligned as citation-context UX continues to evolve. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 218: Sticky expanded context navigation — updated `dashboard/src/pages/Feed.tsx` and `dashboard/src/pages/ItemView.tsx` so widened context windows stay expanded while you keep stepping through adjacent cited items in the same drilldown route instead of resetting to the default window size on each navigation hop, and extended route coverage to lock that behavior in for both focused-feed and raw-item browsing flows. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 219: Cross-route sticky context handoff — moved raw citation context size into shared route helpers, updated the focused-feed and raw-item drilldown surfaces to read/write target-aware `context` query params, and added route coverage in both directions so widened same-source context now survives `/feed` ↔ `/items/:id` handoffs instead of dropping back to each page’s default window. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 220: Summary drilldown chain context — added persisted summary-event chain queries plus `/summaries/:id` enrichment so summary detail pages can show real event timestamps, linked-chain position, and timeline spans from the `events` table when available instead of only parsed LLM event text, updated the Summary UI to render that chain context, and repaired the stale PR-003 structural unit check to follow the extracted raw-feed no-sources component so the full unit suite stays green after the feed refactor. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 221: Sticky summary chain focus — updated `/summaries/:id` so previous/next linked-summary drilldowns now carry a focused `chain` query param, the destination summary highlights matching chain events instead of dropping users into an unfocused event list, and summary chain navigation stays anchored to the same developing story across route hops. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 222: Report-to-summary chain drilldowns — enriched `/reports/:id` with persisted chain drilldown cards derived from recent report entity context, added report-detail links into `/summaries/:id?chain=...`, and updated report route/page coverage so event-chain panels now lead directly into the focused summary-chain flow instead of stopping at static text. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 223: Report preview chain entry links — enriched `GET /reports` and `GET /search` report hits with persisted active-chain drilldown previews, added direct `Open active chain` links to the report list and Search cards while preserving the normal report-detail path, and extended route/API contract coverage so report preview surfaces can enter focused summary-chain drilldowns without an intermediate report-detail hop. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 246: Focused summary chat citations — taught summary-mode `semantic_search` to embed persisted chain-route metadata for summary hits, updated chat citation extraction plus `ChatSources` routing so summary source pills can open `/summaries/:id?chain=...` when chain context is known instead of always dropping into the generic summary page, and extended backend/unit plus dashboard component coverage for the new optional summary `chainRootId` path. Verified by `npm test`, focused dashboard ChatSources coverage, and `npm run build`.
- [x] Cycle 254: Re-anchorable grouped chat citations — updated `ChatSources` so revealed same-chain report citations can promote themselves into the grouped lead slot in place, letting the main pill’s route, date, and relative chronology cues follow whichever citation the user wants to anchor on without leaving the chat source row, and verified the lead-switching flow with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 253: Lead anchors for grouped chat citations — updated `ChatSources` so grouped focused-chain report and summary pills now surface a visible `Lead cite` badge plus the lead citation date when they can reveal same-chain follow-ups, giving the newer relative chronology badges a clear inline reference point, and verified the anchor cue with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 252: Relative chronology cues for revealed chat citations — updated `ChatSources` so revealed same-chain report and summary links now show `Earlier`, `Later`, or `Same day` badges relative to the grouped lead citation when date metadata is available, and verified the comparison cue with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 251: Date cues for revealed chat citations — threaded semantic-search result dates through chat citation extraction, updated `ChatSources` so revealed same-chain report and summary links show lightweight date badges beside their labels, and verified the metadata path with focused handler/component regressions, `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 250: Snippet previews for revealed chat citations — updated `ChatSources` so revealed same-chain report and summary links now render compact inline snippet previews beneath their labels, kept the primary grouped pill plus show/hide disclosure intact, and verified the richer reveal path with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 249: Inline reveal for grouped chat pills — taught `ChatSources` to add lightweight show/hide controls beside grouped focused-chain report and summary pills, revealed the hidden same-chain citations as inline secondary links without disturbing the primary anchored pill, and verified the disclosure flow with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 248: Grouped focused-chain chat pills — taught `ChatSources` to collapse repeated focused report and summary citations from the same chain into one anchored pill per source type, added visible citation-count badges plus merged tooltip context for the hidden same-story citations, and verified the grouped chat-source path with focused ChatSources coverage, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 247: Visible focused-chain chat pills — taught chat citation extraction to preserve compact focused-chain labels from semantic-search metadata, updated `ChatSources` to show visible `Focused chain` badges plus richer anchored-route tooltips on report and summary source pills, and extended backend/unit plus dashboard component coverage so users can tell when a citation opens a story-specific drilldown before clicking. Verified by `npm test`, focused dashboard ChatSources coverage, and `npm run build`.
- [x] Cycle 245: Focused report chat citations — taught report-mode `semantic_search` to embed lead-chain route metadata for report hits, updated chat citation extraction plus `ChatSources` routing so report source pills can open `/reports/:id?chain=...` when a lead chain is known instead of always dropping into the generic report page, and extended backend/unit plus dashboard component coverage for the new optional `chainRootId` path. Verified by `npm test`, focused dashboard ChatSources coverage, and `npm run build`.
- [x] Cycle 244: Focused report-chain summary alignment — updated `dashboard/src/pages/ReportView.tsx` so focused `/reports/:id?chain=...` routes also reorder and highlight the matching textual Event Chain summary instead of only reordering the drilldown cards, and extended report-view coverage so the focused detail route keeps the summary copy aligned with the selected story on arrival. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 243: Focused report body-link handoff — taught the shared `ReportChainPreviewSection` to publish the current focused report href, updated report-list and Search to retarget their main report-card body links to the current `/reports/:id?chain=...` route as preview state changes, and extended focused component plus page regressions so the primary report click path now follows expansion, refresh, and collapse instead of lagging behind the preview controls. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 242: Focused report entry links from previews — extracted shared focused-report href builders, taught the shared `ReportChainPreviewSection` to expose an `Open focused report` link that follows the current lead story, and extended the report-list/Search/component/helper regressions so preview surfaces now hand off directly into `/reports/:id?chain=...` across expansion, refresh, and collapse instead of only into the generic report page. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 241: Focused report-chain routing — updated `dashboard/src/pages/ReportView.tsx` so `/reports/:id?chain=...` can move the requested drilldown to the top, highlight it, and expose clear `Focus chain` / `Show all chains` controls for multi-story reports, then extended report-view coverage so focused report routes keep the right latest-summary drilldown order and reset path instead of leaving report detail as a flat unfocused grid. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 240: Header retry-state copy for preview controls — taught the shared report-chain action helpers plus preview state to retain failed expand/refresh intent in the top-row chips, updated the inline preview surface so header controls can switch to `Retry loading stories` and `Retry refresh stories` after fetch failures without dropping the current story set, and extended focused helper/component/page regressions so recovery state stays visible from the card chrome instead of only in the lower inline preview error row. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 239: Header loading-state copy for preview controls — taught the shared report-chain action helpers to publish header-friendly loading labels, updated report-list and Search to swap top-row chips to `Loading stories...` and `Refreshing stories...` while inline preview requests are in flight, and extended focused component plus page regressions so both expansion and refresh loading states are visible from the card chrome instead of only down in the inline preview section. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 238: Header-level preview refresh controls — taught the shared `ReportChainPreviewSection` to publish a header-level refresh action state, wired fully expanded report-list and Search cards to show `Refresh stories` in their top-row chrome, and extended focused preview regressions so that header control now refetches the active-chain set in place instead of forcing users to use the lower inline preview action row. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 237: Actionable active-story preview counts — taught the shared `ReportChainPreviewSection` to publish a header-level expand/collapse action state, wired the report-list and Search `Active stories` chips into the same inline preview toggle flow, and extended focused preview regressions so those count chips now expand and collapse active-chain previews in place instead of mirroring the state passively. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 236: Actionable lead-chain preview chrome — extracted shared report-chain href builders, taught the shared `ReportChainPreviewSection` to publish the current lead-chain drilldown href, turned the report-list and Search lead-chain header chips into direct focused-summary links, and extended focused preview regressions so those chip targets follow inline preview refresh and collapse instead of staying stuck on the original preview route. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 235: Active-story preview counts — taught the shared `ReportChainPreviewSection` to publish the current visible chain count, surfaced that count in the top-row chrome of report-list and Search cards, and extended the focused preview regressions so expand plus collapse now prove the header count tracks the visible active-story set instead of staying buried inside the inline preview section. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 234: Lead-chain preview chrome — taught the shared `ReportChainPreviewSection` to publish a live lead-chain label, surfaced that label in the top-row chrome of report-list and Search cards, and extended the focused preview regressions so refresh plus collapse now prove the header chip tracks the current active story instead of staying frozen on the original preview order. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 233: Refreshed preview-body sync — taught the shared `ReportChainPreviewSection` to push refreshed `/reports/:id` TLDR/body copy back into the surrounding report-list and Search cards, while keeping the original preview copy available for `Show fewer chains` collapse, so inline chain refreshes no longer leave the adjacent card body stuck on the older list/search payload. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 232: Refreshed preview-summary sync — moved the report/search Event Chain summary sentence into the shared `ReportChainPreviewSection`, taught inline preview expansion and refresh to cache plus replace that summary from `/reports/:id`, and extended report-list/Search coverage so inline chain refreshes keep the preview summary current while collapse restores the original preview sentence. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 231: In-place refresh for expanded chain previews — added a `Refresh chains` path to the shared `ReportChainPreviewSection` so fully expanded report-preview Event Chains can pull the latest active-chain set in place, update their cached expansion payload, and avoid forcing a collapse/reopen cycle just to pick up newer hidden-story changes. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 230: Adaptive inline chain preview cache reconciliation — taught the shared `ReportChainPreviewSection` to validate cached inline expansions against the current preview roots plus current hidden-chain count, drop stale cached story sets when report previews change or shrink, and keep `Show more` visible when a cached partial expansion still leaves newer hidden chains off-screen. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 229: Collapsible sticky inline chain previews — updated the shared `ReportChainPreviewSection` so inline-expanded report-preview chains now expose `Show fewer chains`, clear their cached `sessionStorage` expansion state when collapsed, and restore the default preview set in place across both report-list and Search surfaces instead of making sticky expansion one-way. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 228: Sticky inline chain previews — taught the shared `ReportChainPreviewSection` to persist expanded report-preview chain sets in `sessionStorage`, added a focused component regression for remount persistence, and cleared page-test storage between runs so inline-expanded Event Chains stay open across rerenders/remounts within the same browsing session instead of collapsing back to the preview state. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 227: Inline preview chain expansion — added a shared `ReportChainPreviewSection` that lets report-list and Search cards reveal hidden active-chain links inline by fetching full drilldowns from `/reports/:id`, so users can stay on the preview surface while loading and retrying extra chain links instead of hopping into the full report first. Verified by `npm run test:dashboard` and `npm run build`.
- [x] Cycle 226: Exact hidden-chain preview counts — updated `getRecentReportChainDrilldowns()` to return the total matching chain count alongside preview rows, taught `GET /reports` and `GET /search` to expose `hiddenActiveChainCount` on report previews, and changed the report-list/Search overflow handoff to count-aware wording like `3 more active chains in report` instead of a vague generic link. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 225: Hidden-chain preview overflow indicators — updated `GET /reports` and `GET /search` to flag when more active chains exist beyond the two visible preview links, added `More active chains in report` handoffs on report-list and Search cards, and extended route/API coverage so multi-story report previews stay scannable without hiding the path back to the full report. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 224: Multi-chain report preview entries — widened report preview drilldowns so `GET /reports` and `GET /search` can return more than one active chain for the same report, updated the report list and Search cards to render per-chain focused-summary links instead of a single arbitrary entry, and expanded page coverage so multi-story reports keep their main report-detail path while exposing multiple direct Event Chains entry points. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 157: Structured event extraction foundation — migration 21 adds an `events` table, Stage 1 summaries now emit bounded `events`, the summarizer verifies them against surviving extracted entities, and verified events persist with optional resolved `entity_id` lookups for future chain-linking work. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 158: Same-entity follow-up linking — new verified events now look back 30 days for the most recent prior event on the same entity, run a bounded Haiku yes/no linker, and inherit the previous chain root via `chain_id` when the event is part of the same developing story. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 159: Chain context in daily/pulse prompts — recent linked event chains for currently active entities now flow into Stage 3 daily synthesis and 3-hour pulse prompts via `<recent_event_chains>`, so the model can treat exploits, audits, governance responses, and similar updates as ongoing stories instead of isolated headlines. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 160: Report visibility for event chains — market reports now support optional `eventChains`, daily/pulse prompts can emit concise chain summaries, `/reports/:id` exposes the field, and the report view renders an Event Chains panel when present. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 161: Webhook visibility for event chains — Discord delivery now includes an `Event Chains` field when reports emit chain summaries, the embed still respects Discord limits, and the webhook spec/tests cover the new delivery surface. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 162: Report-list previews for event chains — `GET /reports` now derives a tiny `eventChains` preview from stored report bodies, and the dashboard report list shows the first chain line on cards so ongoing stories are visible before opening the full report. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 163: Search visibility for event chains — `GET /search` now supports `scope=summary|report|all`, report hits derive an event-chain preview from stored report bodies, and the dashboard Search page can search summaries plus reports while surfacing the first active chain line on matching report cards. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 164: Chat visibility for event chains — `keyword_search` now loads recent linked chains for the matched entity, formats them into the tool output alongside sentiment history, and the chat tool definitions now advertise that chain context is available for entity lookups. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 165: Report semantic-search visibility for event chains — `semantic_search(type='report')` now formats report hits as compact previews with report metadata, TLDR context, and recent event-chain summaries instead of raw stored JSON, so chat can cite report-level chains cleanly. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 166: Chat planner guidance for event chains — the chat system prompt now explicitly prefers `semantic_search(type="report")` for ongoing stories, timelines, exploit follow-ups, and governance-response questions after `keyword_search`, and the semantic-search tool definition explains when report mode should be used. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 167: Semantic-search report-intent fallback — when chat calls `semantic_search` without an explicit `type`, timeline, ongoing-story, follow-up, and governance-response style queries now auto-route to report search so synthesized report-level chain context is retrieved more reliably. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 168: Chat tool-usage visibility for report search — chat now returns report-vs-summary semantic search usage labels, and the dashboard tool indicator shows when the assistant searched synthesized reports instead of only summary chunks. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 169: Chat citations for semantic search — chat responses now include `sources[]` citations extracted from semantic search results, and the dashboard renders report citations as clickable pills into `/reports/:id`. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 170: Summary detail surface for chat citations — `GET /summaries/:id` now parses stored Stage 1 summary JSON for the frontend, the dashboard has a dedicated Summary view route, and summary source pills now navigate to a real detail page showing the summary text, key events, entities, and extracted events. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 171: Search drilldowns for report and summary hits — dashboard search result cards now link directly to `/reports/:id` or `/summaries/:id` instead of stopping at preview text, and focused Search page coverage now verifies both deep-link targets. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 172: Raw item drilldowns for chat citations — chat now surfaces `read_raw` calls as item citations, `/items/:id` exposes a single raw source item with parsed attachments, and the dashboard renders a dedicated raw item page so original-message citations are clickable instead of hidden. Verified by `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 173: CI recovery for citation/detail surfaces — reproduced the failing GitHub Actions run locally, confirmed the actual blocker was `format:check` rather than the skipped integration job, ran Prettier across the CI scope, and fixed current lint blockers across report/summary/item detail pages plus supporting tests so the branch is formatter-clean, lint-clean, and test-clean again. Verified by `npm run format:check`, `npm run lint`, `npm run build`, `npm test`, and `npm run test:dashboard`.

### Competitor Mapping (verified)
Started: Cycle 257
Estimated: 3 cycles
Roadmap Feature: 2.4 Competitor mapping

Sub-tasks:
- [x] Cycle 257: Foundation — migration 22 (entity_relationships table with FK, CHECK, UNIQUE constraints, self-relationship guard), CRUD queries (getEntityRelationships, getCompetitors, upsertEntityRelationship with canonical pair ordering, deleteEntityRelationship), REST API (GET /entities/:entityId/relationships, GET /entities/:entityId/competitors, POST /entities/relationships, DELETE /entities/relationships/:id), and 25-pair competitor seed file. Codex adversarial review: 5 findings (2 blocking fixed, 3 queued).
- [x] Cycle 258: Seed runner + LLM inference — wired competitor-seeds.ts into startup seeding with alias-aware exact-name fallback lookup, added bounded Stage 1 relationship extraction plus persistence into `entity_relationships`, and tightened the relationship API route body to use shared union types instead of string force-casts. Verified with `npm test` and `npm run build`.
- [x] Cycle 259: Dashboard UI + verify — added an authenticated Entities tab in Settings with entity search, competitor coverage, full relationship inspection, and admin-only add/remove relationship controls. Verified with `npm run test:dashboard` and `npm run build`.

Design decisions:
- Pair order canonicalized in upsert (entityIdA < entityIdB) to prevent symmetric duplicates
- Self-relationships blocked at API + DB level
- Auth: reads require auth, writes require admin
- Relationships are bidirectional in queries (WHERE entity_id_a = $1 OR entity_id_b = $1)

### Entity Relationship Graph (verified)
Started: Cycle 260
Estimated: 5 cycles
Roadmap Feature: 3.5 Entity relationship graph

Sub-tasks:
- [x] Cycle 260: Structured relationship type expansion — migration 23 widens `entity_relationships.relationship_type` to include `acquired`, `founded`, `advises`, `partnered_with`, and `regulated_by`; Stage 1 schema/prompt, REST validation, and Settings relationship controls now all share the broader explicit-relationship vocabulary. Verified with `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 261: Relationship evidence links — migration 24 adds nullable `summary_id` evidence on `entity_relationships`, Stage 1 inferred relationships now persist the originating summary when available, and Settings relationship rows expose an `Open evidence summary` drilldown for evidence-backed edges. Verified with `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 262: Temporal relationship bounds — migration 25 adds nullable `since_at` and `until_at` on `entity_relationships`, the admin relationship API accepts optional temporal bounds, and Settings relationship management can create and display time-bounded edges with local datetime inputs. Verified with `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 263: Neighborhood graph view — Settings now renders a first-pass visual neighborhood graph for the selected entity, grouping direct edges by related entity, highlighting evidence-backed links, and exposing ended-vs-active connection status without introducing a separate graph API or frontend dependency. Verified with `npx vitest run src/pages/__tests__/Settings.entities.test.tsx`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 264: Graph drilldown + inspect flow — graph nodes and legend chips now focus the relationship list on a single connected entity, the Relationships panel exposes focused-connection controls, and admins can jump directly from a focused graph connection into that connected entity’s own detail view. Verified with `npx vitest run src/pages/__tests__/Settings.entities.test.tsx`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 265: Two-hop graph API — added a dedicated authenticated `/api/v1/entities/:entityId/graph` read model backed by bounded BFS-style graph assembly in the query layer, plus Settings graph loading for second-degree neighborhood context so the graph can show nearby entities beyond direct edges without overloading the direct-relationship endpoints. Verified with `npm test`, `npx vitest run src/pages/__tests__/Settings.entities.test.tsx`, `npm run test:dashboard`, and `npm run build`.

Design decisions:
- Keep relationship extraction explicit-only; do not infer graph edges from weak co-occurrence.
- Reuse one shared relationship vocabulary across DB, Stage 1 extraction, admin APIs, and Settings UI so future graph views stay contract-aligned.
- Preserve existing evidence on manual overrides by only replacing `summary_id` when a new inferred write supplies one.
- Preserve existing temporal bounds on upsert when a new write omits them, so inferred refreshes do not wipe manual date curation.
- Start graph visualization with a direct-neighborhood view powered by existing relationship reads before adding heavier graph-specific APIs or force-layout dependencies.
- Treat the graph as a navigation surface, not just a picture: graph interactions should be able to focus downstream detail panes and hop into connected-entity inspection without requiring a separate search step.
- Keep graph reads separate from the direct-relationship endpoints once neighborhood scope expands, so richer multi-hop graph context does not contort the simpler admin CRUD/read surfaces.

### Influencer Tracking (verified)
Started: Cycle 280
Estimated: 5 cycles
Roadmap Feature: 3.2 Influencer tracking + credibility scores

Sub-tasks:
- [x] Cycle 280: Foundation — Migration 29 (authors + author_calls tables with FK, CHECK, UNIQUE constraints, indexes), AuthorRow/AuthorCallRow types, 8 CRUD queries (upsert, getByHandle/Id, getTopByEntity, insertCall, getCalls, getUnresolved, resolveCall with credibility update), API endpoints (GET /entities/:entityId/authors, GET /authors/:authorId). 28 structural tests.
- [x] Cycle 281: Author extraction — summarizer now upserts unique chunk authors into `authors`, normalizes handles with trim+lowercase, and keeps latest-timestamp freshness per handle. Verified with `npm run format:check`, `npm test`, and `npm run build`.
- [x] Cycle 282: Claim extraction — Stage 1 now emits bounded `authorClaims`, summarizer verifies claim authors against chunk handles and claim entities against verified entities, and verified claims persist to `author_calls` with latest source-item traceability. Verified with `npm run format:check`, `npm test`, and `npm run build`.
- [x] Cycle 283: Dashboard — Settings now shows top authors for the selected entity, author profile cards with recent extracted calls and source-item links, plus admin claim-resolution actions backed by `PATCH /author-calls/:callId`. Verified with `npm run format:check`, `npm test`, `npm run test:dashboard`, and `npm run build`.
- [x] Cycle 284: Verify — live `dev-browser` pass against the Vite dashboard confirmed the routed Settings author flow end to end: Arbitrum shows the empty author state, Ethereum shows top authors plus source-item links, switching to `chainwatcher` shows the dismissed claim history, and resolving the DeFi Dad claim refreshes credibility to `80% accurate`.

Design decisions:
- Authors keyed by (platform, handle) with UNIQUE index — handles are normalized per platform
- Credibility score only computed after 5+ resolved calls (avoids noise from few data points)
- author_calls links to source items for traceability (FK SET NULL on delete)
- Claims categorized as bullish/bearish/event/neutral
- Claim extraction stays conservative: only explicit author-attributed takes survive, and ambiguous author/entity matches are dropped instead of guessed.

### Cross-Market Correlation (verified)
Started: Cycle 287
Estimated: 3 cycles
Roadmap Feature: 3.3 Cross-market correlation

Sub-tasks:
- [x] Cycle 287: Foundation — Migration 30 (`macro_snapshots` for `vix`, `dxy`, `us10y`, `spx`), FRED fetcher + macro tracker wired into `onDaily`, `FRED_API_KEY` config support, and `<macro_context>` injection in daily/pulse prompts with crypto aggregate sentiment plus macro bias framing. Verified with `npm run format:check`, `npm test`, and `npm run build`.
- [x] Cycle 288: API + dashboard surface — added authenticated `GET /api/v1/macro` using stored snapshots plus `buildMacroContext()`, and a new Settings Pipeline `Macro Backdrop` card with overall bias, per-indicator 1d/7d moves, and an explicit empty state before the first FRED refresh. Verified with `npm run format:check`, focused macro API/dashboard tests, `npm test` (1819 passing), and `npm run build`.
- [x] Cycle 289: Report alerts — added bounded `macroAlerts` to the shared market-report schema, taught daily and pulse prompts to emit dedicated cross-market divergence/regime lines when `<macro_context>` is present, exposed `macroAlerts` from `GET /api/v1/reports/:id`, and rendered them in the report detail view as a first-class section instead of relying on freeform prose. Verified with `npm run format:check`, focused macro report unit/dashboard tests, `npm test` (1829 passing), and `npm run build`.
- [x] Cycle 290: Delivery surface — extended webhook report parsing plus `buildFields()` so Discord embeds now include `Macro Alerts` bullets when reports emit `macroAlerts`, while remaining backward-compatible with older stored report bodies that predate the field. Verified with `npm run format:check`, focused webhook unit coverage, `npm test` (1831 passing), and `npm run build`.
- [x] Cycle 291: Preview surface — taught `GET /api/v1/reports` and `GET /api/v1/search` to expose a single `macroAlerts` preview from stored report bodies, then updated the report list and Search cards to render a `Macro Alert` line whenever no active-chain preview is occupying that secondary slot. Verified with `npm run format:check`, focused report-list/search Vitest coverage, `node --test test/unit/api-serialization.test.ts`, `npm test` (1833 passing), and `npm run build`.
- [x] Cycle 292: Gold coverage — added a backward-compatible migration that widens `macro_snapshots` to allow `gold`, taught the FRED fetcher and macro context formatter to treat gold as a first-class defensive indicator, updated prompt guidance and the Settings macro card typing, and extended focused macro tests so the fifth roadmap indicator now flows end-to-end. Verified with `npm run format:check`, focused macro foundation/prompt/dashboard tests, `npm test` (1834 passing), and `npm run build`.
- [x] Cycle 293: Verification audit — traced the `3.3` contract across `/api/v1/macro`, report detail serialization, report-list/search previews, and webhook delivery, then reran the full unit suite, full dashboard suite, and a fresh production build to confirm the macro backdrop plus `macroAlerts` surfaces are green end-to-end. Verified with `npm run format:check`, `npm test` (1834 passing), `npm run test:dashboard` (171 passing), and `npm run build`.

Design decisions:
- Use FRED as the first macro source and store absolute 1d/7d deltas instead of forcing percent changes across mixed indicator types.
- Track five reliable indicators (`vix`, broad-dollar proxy `dxy`, `us10y`, `spx`, `gold`) before considering extra macro feeds beyond the roadmap set.
- Refresh macro snapshots inside `onDaily` before synthesis so Stage 3 can consume the newest stored cross-market backdrop without adding another scheduler path yet.
- Return 404 from `/api/v1/macro` before the first stored snapshot so the dashboard can render a clear "not seeded yet" empty state instead of treating startup as an API failure.
- Store macro-vs-crypto regime calls in a dedicated `macroAlerts` array so cross-market correlation survives JSON parsing and gets a stable dashboard surface instead of hiding inside generic section prose.
- Treat rising gold as a defensive/risk-off signal in the first-pass macro context, alongside higher volatility, firmer dollar, and higher yields.

### Macro Regime Detection (verified)
Started: Cycle 294
Estimated: 2 cycles
Roadmap Feature: 3.4 Macro regime detection

Sub-tasks:
- [x] Cycle 294: Report-level regime contract — added a bounded `macroRegime` object (`classification`, `confidence`, `rationale`) to the shared market-report schema, taught daily and pulse prompts to classify the current tape as `risk-on`, `risk-off`, `transition`, or `unclear` when macro context is present, exposed the parsed object from `GET /api/v1/reports/:id`, rendered it near the top of report detail, and included it in Discord delivery as a `Macro Regime` field. Verified with `npm run format:check`, focused macro-regime unit/dashboard checks, `npm test` (1847 passing), `npm run test:dashboard` (173 passing), and `npm run build`.
- [x] Cycle 295: History foundation — added Migration 32 for a dedicated `macro_regimes` table, persisted daily `macroRegime` calls after report insertion, exposed `macroRegimeHistory` from `GET /api/v1/reports/:id`, and extended report detail to show current streak length, regime start date, and the previous daily classification when history exists. Verified with `npm run format:check`, `npm test` (1854 passing), `npm run test:dashboard` (173 passing), and `npm run build`.
- [x] Cycle 296: Preview surfaces — exposed `macroRegime` plus `macroRegimeHistory` from report-list and report-search preview payloads, added a shared dashboard macro-regime formatter helper, and rendered compact `Macro regime` chips on report cards and report search hits so current regime state is visible without opening `/reports/:id`. Verified with `npm run format:check`, focused macro preview API/dashboard checks, `npm test` (1856 passing), `npm run test:dashboard` (175 passing), and `npm run build`.
- [x] Cycle 297: Verification audit — traced the `macroRegime` plus `macroRegimeHistory` contract across report preview serialization, report detail rendering, and Discord delivery, reran focused macro-regime unit/dashboard coverage, and confirmed the full `3.4` sweep is green with `npm run format:check`, `npm test` (1856 passing), `npm run test:dashboard` (175 passing), and `npm run build`.

Design decisions:
- Start with report-level regime classification before building history, so the user-facing call path is real before adding a new table and longer-lived read model.
- Force `unclear` to be a valid regime output and require an explicit confidence score, reducing the chance that Sonnet overstates a mixed macro tape.
- Persist regime history for daily reports only on the first pass, so streak logic reflects the canonical daily tape instead of mixing shorter pulse snapshots into the same regime timeline.
- Treat macro regime as report metadata on preview surfaces, so report list/search can surface the current tape without displacing the existing event-chain and macro-alert body previews.
- Keep broader `3.4` follow-up work focused on richer change surfaces and verification now that report detail has real streak/day-count context.

### Alpha Decay Tracking (verified)
Started: Cycle 276
Estimated: 4 cycles
Roadmap Feature: 3.6 Alpha decay tracking

Sub-tasks:
- [x] Cycle 276: Foundation — Migration 28 (tier column on sources, alpha_propagation table with entity/event FKs + indexes), AlphaPropagationRow queries (insert, by-entity, by-event, summary), updateSourceTier, tier in PATCH source API, dashboard tier selector in Settings sources table. 9 backend + 2 dashboard structural tests.
- [x] Cycle 277: Detection — alpha tracker module (`src/knowledge/alpha-tracker.ts`), `resolveEntities` returns entity IDs, summarizer wires `alphaTracker.trackMentions()` after entity resolution, `createAlphaTracker` created in index.ts. 18 structural tests. Fixed cycle113 regression test for nested try/catch extraction.
- [x] Cycle 278: Prompt + Dashboard + API — `<alpha_propagation>` XML block in daily/pulse prompts with propagation speed, `GET /api/v1/entities/:entityId/alpha` endpoint (summary + records), alpha propagation card in Settings entity detail with tier timeline badges and propagation speed. 24 backend structural + 0 dashboard tests (moved to backend). Fixed synthesize-stage3 pool mock sequence for alpha query.
- [x] Cycle 279: Verify — 4-agent audit (E2E flow, API contracts, dashboard surfaces, edge cases). 1 blocking fix: tier PATCH on shared handler. 8 non-blocking findings queued.
- [x] Cycle 359: Alpha signals in reports — promoted alpha propagation from background prompt context into the market-report contract with bounded `alphaSignals`, taught the daily and pulse prompts to emit concise higher-tier timing lines from `<alpha_propagation>`, exposed the parsed field from `GET /api/v1/reports/:id`, rendered an `Alpha Signals` section in report detail, and added Discord delivery support with camelCase/snake_case compatibility plus field-cap priority ahead of catalysts and softer heuristics. Added focused schema/prompt/report-parser coverage in `test/unit/alpha-report-signals.test.ts`, routed report-detail coverage in `dashboard/src/pages/__tests__/ReportView.alpha.test.tsx`, delivery formatting/ordering coverage in `test/unit/webhook.test.ts`, and widened one brittle `api-serialization` source-slice assertion uncovered during validation. Verified with `node --test test/unit/alpha-report-signals.test.ts`, `node --import tsx/esm --test test/unit/webhook.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.alpha.test.tsx`, `npm run format:check`, `npm test` (2016 passing), `npm run test:dashboard` (227 passing), and `npm run build`.
- [x] Cycle 360: Verification closeout — extended the routed alpha-report regression so `Alpha Signals` is now explicitly covered on the latest-report `/` route as well as direct `/reports/:id` detail, then ran a direct `buildEmbed()` runtime probe against a snake_case `alpha_signals` report body to confirm Discord delivery still resolves to `Key Events` → `Event Chains` → `Macro Regime` → `Alpha Signals` under the 4-field cap with the public report URL attached. Re-ran the focused routed alpha coverage, formatter, full unit suite, full dashboard suite, and a fresh production build to close the remaining verification gap around the new report-level alpha contract.

Design decisions:
- Source tiers are user-configurable labels (alpha/influencer/general/mainstream) with 'general' as default
- alpha_propagation tracks per-entity first-mention times across tiers for propagation speed analysis
- Tier stored on sources table (not source_state) since it's a classification property, not runtime state

### Alpha Watch On Main Dashboard (verified)
Started: Cycle 357
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 357: Main dashboard alpha watch — added `getRecentAlphaWatchlist()` plus authenticated `GET /api/v1/alpha-watch` so the existing alpha-propagation system now has a market-level watch surface instead of only a per-entity Settings drilldown. `ReportView` now fetches that overview and renders a compact `Alpha Watch` card on both `/` and report detail, showing where a signal first appeared (`alpha` or `influencer`), how far it spread across later tiers, and the rough propagation speed. Added a structural API/dashboard regression in `test/unit/alpha-watch-api.test.ts`, a routed card regression in `dashboard/src/pages/__tests__/ReportView.alpha.test.tsx`, updated the other routed report-view fetch mocks for the new background request, and verified with `node --test test/unit/alpha-watch-api.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.alpha.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.calendar.test.tsx src/pages/__tests__/ReportView.divergence.test.tsx`, `npm run format:check`, `npm test` (2007 passing), `npm run test:dashboard` (225 passing), and `npm run build`.
- [x] Cycle 358: Verification closeout — extended the dedicated routed alpha-watch regression so the card is now explicitly covered on direct `/reports/:id` detail as well as the latest-report route, and added the missing empty-watchlist proof so `ReportView` hides the `Alpha Watch` card when `/api/v1/alpha-watch` returns no entries. Re-ran the focused alpha structural/API coverage plus routed report-view coverage, formatter, the full dashboard suite, and a fresh production build to close the remaining verification gap around this new background watch-card fetch.

Design decisions:
- Reuse the existing `alpha_propagation` data instead of inventing a second “early signal” heuristic, so the watch card stays grounded in the same tier timeline the prompts and Settings entity detail already use.
- Anchor each entry on the earliest higher-tier mention (`alpha` or `influencer`) and the latest observed tier, so the compact card answers both “where did this start?” and “how far has it spread?” without duplicating the full per-entity timeline UI.

### Regional Divergence (verified)
Started: Cycle 267
Estimated: 1 cycle
Roadmap Feature: 2.7 Regional divergence

Sub-tasks:
- [x] Cycle 267: API + dashboard — `getEntityDivergence()` and `getTopDivergentEntities()` queries, `GET /api/v1/divergence` and `GET /api/v1/entities/:entityId/divergence` routes, Settings entity detail divergence card with EN vs ID sentiment bars, direction labels, divergence badge (Aligned/Moderate/Divergent), and 7d/14d/30d days selector. 26 backend structural + 5 dashboard component tests. Verified with `npm test` (1598), `npm run test:dashboard` (146), and `npm run build`.
- [x] Cycle 286: Dashboard follow-up — split the entity detail loading so changing the 7d/14d/30d divergence selector only re-fetches divergence data, not relationships, competitors, graph, price, or alpha context. Verified with `npm run format:check`, focused divergence dashboard coverage, and `npm run build`.
- [x] Cycle 350: Verification closeout — re-verified the regional divergence contract across divergence query math/filtering, authenticated `GET /api/v1/divergence` and `GET /api/v1/entities/:entityId/divergence` routes, plus Settings entity-detail badges, empty state, and selector refetch behavior. Verified with focused divergence backend coverage, focused divergence dashboard coverage, `npm run format:check`, `npm test` (1984 passing), `npm run test:dashboard` (216 passing), and `npm run build`.

Design decisions:
- Backend divergence infrastructure (language tracking, divergence computation, prompt injection) already existed from Phase 1. This cycle surfaces it via API + dashboard.
- Divergence card placed before relationships in entity detail for visibility.
- Days selector (7/14/30) allows tuning lookback window without page reload.
- Divergence fetch failure doesn't block other entity data loading (`.catch(() => null)`).

### Price Feeds (verified)
Started: Cycle 271
Estimated: 4 cycles
Roadmap Feature: 3.1 Price feeds (CoinGecko API)

Sub-tasks:
- [x] Cycle 271: Foundation — Migration 27 (price_snapshots table), CoinGecko fetch module (`src/prices/coingecko.ts`), price snapshot queries (insert, batch insert, latest, history, latest-per-entity). 17 structural tests.
- [x] Cycle 272: Integration — `coingeckoApiKey` in config, `getActiveTokensWithCoinGeckoIds()` query (JOIN entities+aliases, context_key extraction), `src/prices/tracker.ts` (orchestrates entity lookup → CoinGecko fetch → batch snapshot insert), wired into `onDaily` in index.ts before synthesis. 19 structural tests.
- [x] Cycle 273: Prompt injection + API — `<price_context>` XML block in daily/pulse/flash prompts with contrarian signal detection (bearish sentiment + rising price / bullish sentiment + falling price), `priceAlerts` added to MarketReportLLMSchema, system prompts updated to reference price data, `GET /api/v1/entities/:entityId/price` endpoint with latest + history. 22 structural tests.
- [x] Cycle 274: Dashboard + verify — entity price card in Settings (current price, 24h/7d changes, volume, market cap, recent history), `priceAlerts` rendering in ReportView after marketCatalysts, `priceAlerts` added to Report/FullReport types, server-side report body parsing. 7 dashboard tests.
- [x] Cycle 349: Verification closeout — re-verified the full price-feed contract across CoinGecko foundation/tracker wiring, prompt-level price-context injection, entity price API shape, Settings entity price card/history rendering, report-detail and latest-route price surfaces, plus Discord delivery of `Price Alerts`. Verified with focused price unit coverage, focused Settings/ReportView dashboard coverage, `npm run format:check`, `npm test` (1984 passing), `npm run test:dashboard` (216 passing), and `npm run build`.

Design decisions:
- CoinGecko free tier (no API key required, 30 calls/min) as default. Optional pro tier API key support.
- Batch fetch in chunks of 50 IDs with 2s delay between chunks for rate limiting.
- price_snapshots keyed by ULID, FK to entities with CASCADE delete.
- Source field on snapshots for future multi-provider support.

### Unusual Activity Heuristics (verified)
Started: Cycle 298
Estimated: 2 cycles
Roadmap Research: R.1 Fake News / Manipulation Detection (heuristics only)

Sub-tasks:
- [x] Cycle 298: Pipeline watchlist — added `getUnusualActivityOverview()` over `entity_sentiment_daily` to flag entities whose latest daily mention counts break above their own 7-day baseline, exposed authenticated `GET /api/v1/unusual-activity`, and rendered an explicit heuristic `Unusual Activity` card in the Settings Pipeline tab with ratio, baseline, prior peak, momentum, and sentiment context plus a safe empty state. Verified with `npm run format:check`, focused unusual-activity unit/API/dashboard tests, `npm test` (1865 passing), `npm run test:dashboard` (177 passing), and `npm run build`.
- [x] Cycle 299: Report contract — added bounded `unusualActivity` to the shared market-report schema, taught daily and pulse prompts to consume `<unusual_activity>` context from the heuristic watchlist and emit cautious crowding/attention-spike lines, exposed `unusualActivity` from `GET /api/v1/reports/:id`, and rendered it in the report detail view as a first-class section. Verified with `npm run format:check`, focused unusual-activity report unit/dashboard checks, `npm test` (1872 passing), `npm run test:dashboard` (179 passing), and `npm run build`.
- [x] Cycle 300: Delivery surface — taught Discord webhook delivery to render report `unusualActivity` lines as a dedicated `Unusual Activity` embed field, prioritized that field ahead of sentiment inside the existing 4-field cap so new heuristic output stays visible on dense reports, and kept delivery backward-compatible with older stored report bodies that do not include `unusualActivity`. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/webhook.test.ts`, `npm test` (1874 passing), and `npm run build`.
- [x] Cycle 301: Preview surfaces — exposed single-line `unusualActivity` previews from stored report bodies on both `GET /api/v1/reports` and `GET /api/v1/search`, added a shared dashboard preview selector so report list and Search now fall back `Event Chain` → `Unusual Activity` → `Macro Alert`, and locked that priority with focused serialization plus page tests. Verified with `npm run format:check`, `node --test test/unit/api-serialization.test.ts`, focused report-list/search Vitest coverage, `npm test` (1876 passing), `npm run test:dashboard` (181 passing), and `npm run build`.
- [x] Cycle 302: Duplicate-cluster heuristic — extended `getUnusualActivityOverview()` with a conservative near-duplicate content pass that groups same-day `items` by normalized content similarity across authors/sources, exposed the new duplicate-cluster metrics on `GET /api/v1/unusual-activity`, injected copy-paste cluster context into daily/pulse unusual-activity prompt blocks, and surfaced the extra signal in the Settings Pipeline watchlist card. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/unusual-activity.test.ts test/unit/unusual-activity-api.test.ts test/unit/unusual-activity-report.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/Settings.unusual-activity.test.tsx`, `npm test` (1879 passing), `npm run test:dashboard` (181 passing), and `npm run build`.
- [x] Cycle 303: Low-relevance breakout bias — joined `entities.relevance` into `getUnusualActivityOverview()` so the watchlist can flag lower-relevance breakouts separately from established-asset spikes, injected `relevance` / `low_relevance` markers into daily and pulse unusual-activity prompt context, and updated the Settings Pipeline card copy plus chips to show when a flagged entity is still low relevance. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/unusual-activity.test.ts test/unit/unusual-activity-api.test.ts test/unit/unusual-activity-report.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/Settings.unusual-activity.test.tsx`, `npm test` (1882 passing), `npm run test:dashboard` (181 passing), and `npm run build`.
- [x] Cycle 304: Verification closeout — browser-verified a mocked routed dashboard flow covering the Settings Pipeline watchlist (`low relevance 2.2`, duplicate-cluster chip), report-list unusual-activity preview, report-detail `Unusual Activity` panel, and Search result preview, then sanity-checked webhook delivery field assembly via `buildFields()` to confirm `Unusual Activity` still survives the Discord field ordering. Verified with `dev-browser`, a direct `buildFields()` execution, `npm run format:check`, `npm test` (1882 passing), `npm run test:dashboard` (181 passing), and `npm run build`.

Design decisions:
- Ship this as "unusual activity" rather than manipulation detection, keeping the UX aligned with the roadmap's heuristic-only guidance.
- Reuse `entity_sentiment_daily` for a low-cost first pass, comparing the latest rollup against the prior 7-day baseline instead of inventing a new streaming detector.
- Require minimum mention thresholds and either a large baseline multiple or a strong no-history breakout so the watchlist stays focused on inspectable spikes instead of single-mention noise.
- Promote the heuristic into daily/pulse report detail before adding list/search previews or webhook delivery, so the main user-facing report contract becomes real without overloading the first slice.
- Within Discord's 4-field embed cap, prioritize `Unusual Activity` ahead of `Sentiment` so the new heuristic is visible in delivery without displacing the more established key-event and macro-regime context.
- On report preview cards, keep active-chain context first, then let `Unusual Activity` outrank `Macro Alert` so crowding/copy-trade style warnings surface before broader macro framing when only one secondary line is available.
- Keep the duplicate-content signal conservative: only surface clusters when near-duplicate same-day content spans at least three posts and two authors, and prefer `content_anchor` when available so long-form source items cluster on the underlying text rather than a shortened excerpt.
- Reuse the existing `entities.relevance` score as the low-relevance proxy instead of inventing a separate obscurity metric, so the heuristic can bias toward “small thing suddenly getting loud” behavior without adding a new model or schema.

### Influencer First-Mover Timing (verified)
Started: Cycle 305
Estimated: 5 cycles
Roadmap Gap: R.2 Influencer Credibility Scoring (factual first-mover tracking only)

Sub-tasks:
- [x] Cycle 305: Entity timing surface — enriched `getTopAuthorsByEntity()` with per-author first tracked call timing from `author_calls`, including `firstMover` and lag-vs-lead metadata, then surfaced that context in Settings so entity author lists and selected author profiles can answer “who said it first” without inventing a fake-precision credibility number. Verified with `npm run format:check`, `node --test test/unit/influencer-dashboard.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/Settings.influencer.test.tsx`, `npm test` (1884 passing), `npm run test:dashboard` (181 passing), and `npm run build`.
- [x] Cycle 306: Report context surface — added bounded `firstMovers` to the daily/pulse report contract, injected a `<first_movers>` prompt block from recent `author_calls`, exposed the field from `/api/v1/reports/:id`, and rendered a dedicated `First Movers` section on report detail so early monitored attribution survives beyond Settings. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/synthesize-stage3.test.ts test/unit/first-mover-report.test.ts test/unit/schemas.test.ts test/unit/pulse.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.first-movers.test.tsx`, `npm test` (1895 passing), `npm run test:dashboard` (183 passing), and `npm run build`.
- [x] Cycle 307: Preview discovery surface — exposed one-line `firstMovers` previews on `GET /api/v1/reports` and report-scope search hits, then taught the shared report-preview helper to show `First Mover` ahead of `Unusual Activity` and `Macro Alert` whenever there is no active-chain preview already occupying that slot. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx`, `npm test` (1897 passing), `npm run test:dashboard` (185 passing), and `npm run build`.
- [x] Cycle 308: Delivery surface — added optional `firstMovers` parsing to the Discord webhook embed builder and surfaced a dedicated `First Movers` field in delivery, with the 4-field cap now preferring `Macro Regime` and `First Movers` ahead of `Unusual Activity`, `Macro Alerts`, and `Sentiment` once `Key Events` and `Event Chains` are present. Verified with `npm run format:check`, `node --import tsx/esm --test test/unit/webhook.test.ts`, `npm test` (1898 passing), and `npm run build`.
- [x] Cycle 309: Verification closeout — verified the full first-mover contract with focused routed dashboard coverage for Settings entity timing, report-list preview priority, search preview priority, and report-detail rendering, then executed `buildFields()` directly to confirm Discord delivery still resolves to `Key Events`, `Event Chains`, `Macro Regime`, and `First Movers` under the 4-field cap. Verified with `cd dashboard && npx vitest run src/pages/__tests__/Settings.influencer.test.tsx src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx`, `node --import tsx/esm --input-type=module (buildFields ordering)`, `npm run format:check`, `npm test` (1898 passing), `npm run test:dashboard` (185 passing), and `npm run build`.

Design decisions:
- Use the earliest `author_calls.timestamp` per entity as the first-mover source of truth, because it is precise, auditable, and already tied to a source item when available.
- Keep the first-mover data additive to the existing influencer review flow: extend the entity-author payload and Settings surfaces rather than changing the author-profile or claim-resolution route contracts first.
- Scope report-level first-mover attribution to a recent 7-day lookback instead of “earliest ever” history, so daily/pulse output highlights timely monitored calls rather than stale all-time trivia for permanently popular assets.
- Preserve the existing preview hierarchy by leaving active-chain previews first, then slotting `First Mover` ahead of crowding and macro fallback lines so timing attribution surfaces early without displacing stronger story context.
- Preserve the established Discord delivery hierarchy by keeping `Macro Regime` visible under the 4-field cap, then slotting `First Movers` ahead of unusual-activity, macro-alert, and sentiment fallback fields so timing attribution shows up without displacing the more structural regime signal.

### Influencer Review-History Language Alignment (verified)
Started: Cycle 362
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 362: Review-history language alignment — extended the `GET /api/v1/first-movers` watchlist payload with `totalCalls` and `correctCalls`, then replaced the remaining percent-style “accurate” labels on the main-dashboard first-mover card and Settings influencer profile with factual reviewed-outcome summaries like `4/5 reviewed correct`. I also changed the remaining Settings copy from “credibility” to “review history” so the influencer surfaces no longer imply a false-precision score the roadmap explicitly says we should avoid. Added focused query/API structural coverage plus routed `ReportView` and Settings regressions, then verified with `node --import tsx/esm --test test/unit/first-mover-watchlist.test.ts test/unit/first-mover-watchlist-api.test.ts test/unit/influencer-dashboard.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/Settings.influencer.test.tsx`, `npm run format:check`, `npm test` (2020 passing), `npm run test:dashboard` (227 passing), and `npm run build`.

Design decisions:
- Keep the stored `credibility_score` field internal for now, but stop presenting it as a polished user-facing percentage so the product language stays honest about what is actually being measured.
- Prefer reviewed-outcome counts over percent labels because they are factual, auditable, and consistent with the roadmap’s guidance that free-text influencer scoring should stay descriptive rather than quantitative.

### Stage 3 Validation Hardening (verified)
Started: Cycle 310
Estimated: 1 cycle
Focus: Reliability follow-up

Sub-tasks:
- [x] Cycle 310: Schema-guided report retry — daily, flash, and pulse synthesis now distinguish invalid JSON from schema-invalid JSON, and when the model returns valid JSON with the wrong shape they retry once with explicit Zod error-path feedback instead of repeating a blind second call. Added focused regressions covering daily, flash, and pulse validation-retry behavior. Verified with `node --import tsx/esm --test test/unit/synthesize-stage3.test.ts test/unit/pulse.test.ts`, `npm run format:check`, `npm test` (1901 passing), and `npm run build`.
- [x] Cycle 348: Verification closeout — extended the Stage 3 retry regressions to prove malformed JSON retries keep the original wrapped user payload and do not append schema feedback, while schema-invalid retries keep the same payload and only augment the system prompt with field-level repair guidance. Re-ran the focused synthesize/pulse suites, formatter, full unit suite, and a fresh production build to close the remaining verification gap on the retry contract.

Design decisions:
- Keep the existing single retry budget for Stage 3 outputs, but spend that retry more intelligently by preserving the original user payload and only augmenting the system prompt when schema validation fails.
- Treat malformed JSON and schema-invalid JSON differently: malformed JSON still gets a plain retry, while schema-invalid JSON now includes concrete field-level repair guidance.

### Summarize Split Failure Hardening (verified)
Started: Cycle 311
Estimated: 1 cycle
Focus: Reliability follow-up

Sub-tasks:
- [x] Cycle 311: Split-path success accounting — when Stage 1 chunk processing recursively splits on `ContextLengthExceededError`, `handleChunk()` now marks only the successfully summarized subchunk item IDs as processed instead of incorrectly promoting the whole parent chunk. Added a targeted regression for the half-success/half-failure path and updated the older structural plumbing test to assert the new `itemIds` contract on `ProcessedChunk`. Verified with `node --import tsx/esm --test test/unit/cycle92-summarize-regression.test.ts test/unit/summarize-budget.test.ts test/unit/summarize.test.ts`, `npm run format:check`, `npm test` (1902 passing), and `npm run build`.
- [x] Cycle 347: Verification closeout — extended the focused summarize regression suite with a nested recursive split case that proves multiple successful leaf chunks keep all of their `itemIds` through aggregation while only the failed leaf returns to retry cleanup. Re-ran the focused summarize suites, formatter, full unit suite, and a fresh production build to close the remaining verification gap on recursive split success accounting.

Design decisions:
- Preserve the existing recursive split strategy and batch status flow, but extend `ProcessedChunk` with explicit `itemIds` so success accounting follows the actual processed leaves instead of inferring from the parent chunk size.
- Keep failed split halves on the normal retry/failed path by deriving chunk failures as `parent chunk IDs - succeeded leaf IDs`, rather than adding a second bespoke failure bookkeeping path.

### URL Validation Fail-Closed Hardening (verified)
Started: Cycle 312
Estimated: 1 cycle
Focus: Security follow-up

Sub-tasks:
- [x] Cycle 312: Fail closed on unresolved hosts — `validateUrl()` now rejects hostnames when both DNS lookups fail instead of treating an empty resolution set as safe, and it also validates literal IP hosts directly so raw private IP URLs are blocked instead of bypassing DNS checks. Added focused coverage for raw private/public IP hosts and the new DNS-failure behavior. Verified with `node --import tsx/esm --test test/unit/url-validator.test.ts`, `npm run format:check`, `npm test` (1904 passing), and `npm run build`.
- [x] Cycle 346: Verification closeout — extended the focused `validateUrl()` regression suite to prove the literal-IP fast path never consults DNS, including raw private IPv4 rejection plus public and private IPv6 coverage. Re-ran the focused validator suite, formatter, full unit suite, and a fresh production build to close the remaining verification gap on the fail-closed literal-host behavior.

Design decisions:
- Treat “no IPs resolved” as a security failure, not a soft pass, because SSRF protection should fail closed whenever host resolution cannot establish a public destination.
- Handle literal IP hosts before DNS resolution so private raw IP URLs cannot slip through on `ENOTFOUND`, while still allowing explicit public IP URLs when they are intentionally provided.

### DNS-Pinned Outbound Fetch Hardening (verified)
Started: Cycle 313
Estimated: 1 cycle
Focus: Security follow-up

Sub-tasks:
- [x] Cycle 313: Pin validated outbound fetches end to end — `fetchValidated()` now uses an `undici` dispatcher whose DNS lookup is pinned to the already validated `resolvedIp`, and it closes that dispatcher when callers consume or cancel the response body. The admin webhook test route, alert-webhook sender, and Discord delivery retry path now all reuse that shared pinned fetch helper instead of validating then calling raw `fetch()`, so retries keep the original safe resolution instead of re-resolving DNS. Added focused helper coverage plus updated structural SSRF-route assertions and health/webhook regressions. Verified with `node --import tsx/esm --test test/unit/url-validator.test.ts test/unit/webhook.test.ts test/unit/api-serialization.test.ts test/unit/cycle110-health-regression.test.ts`, `npm run format:check`, `npm test` (1907 passing), and `npm run build`.
- [x] Cycle 345: Verification closeout — added runtime regressions proving the pinned dispatcher actually closes after `response.text()` consumption and `body.cancel()`, then extended the delivery and DB-down alert-webhook retry tests to show retries reuse the original DNS validation instead of re-resolving on subsequent attempts. Re-ran the focused pinned-fetch, delivery, and health suites, formatter, the full unit suite, and a fresh production build to close the remaining verification gap on the end-to-end DNS-pinning behavior.

Design decisions:
- Keep the original hostname in the outbound URL so TLS SNI and certificate verification still target the real host, while pinning only the socket lookup to the validated IP to remove the DNS rebinding window.
- Allow delivery retries and other multi-attempt call sites to pass a previously validated `UrlValidationResult` back into `fetchValidated()`, so repeated attempts do not silently re-resolve a different address after the initial SSRF check.

### Narrative Watchlist Surface (verified)
Started: Cycle 314
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 314: Narrative watchlist API + Pipeline card — exposed the latest daily narrative clusters through a new authenticated `/api/v1/narratives` read model, then surfaced them on the Settings Pipeline tab with lifecycle badges (`new`, `emerging`, `strong`, `stable`, `fading`), summary counts, and average sentiment so users can finally inspect the existing narrative-clustering system outside the Stage 3 prompt. Added focused query coverage, structural API/UI regression coverage, and dashboard tests for the new card plus the shared Pipeline mocks it touched. Verified with `node --import tsx/esm --test test/unit/narrative-watchlist.test.ts test/unit/narrative-watchlist-api.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/Settings.narratives.test.tsx src/pages/__tests__/Settings.macro.test.tsx src/pages/__tests__/Settings.unusual-activity.test.tsx src/pages/__tests__/Settings.calendar.test.tsx`, `npm run format:check`, `npm test` (1917 passing), `npm run test:dashboard` (187 passing), and `npm run build`.

Design decisions:
- Reuse the latest `narratives` cluster date as the watchlist snapshot instead of trying to deduplicate cross-day cluster names, so the first user-facing surface stays faithful to the detector’s existing daily output contract.
- Keep the UI explicitly descriptive rather than predictive: surface trajectory labels and cluster size/sentiment context, but avoid claiming the system can forecast when a narrative will peak or reverse.

### Narrative Watchlist Drilldown (verified)
Started: Cycle 315
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 315: Inline clustered-summary drilldown for narrative watchlist — added a narrative detail query plus authenticated `/api/v1/narratives/:id` route that reuses stored `summary_ids` to load the most recent clustered summaries, then wired the Settings Pipeline card so each narrative can expand inline and show linked evidence summaries with source, urgency, sentiment, and direct `/summaries/:id` navigation. Added focused query, structural API, and dashboard click-path regressions. Verified with `node --import tsx/esm --test test/unit/narrative-watchlist.test.ts test/unit/narrative-watchlist-api.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/Settings.narratives.test.tsx`, `npm run format:check`, `npm test` (1924 passing), `npm run test:dashboard` (188 passing), and `npm run build`.

Design decisions:
- Keep the drilldown read-only and evidence-first by reusing stored summary links instead of inventing new narrative forecasts or mutable narrative metadata.
- Limit the inline drilldown to the most recent clustered summaries so the first narrative-evidence surface stays scannable inside Settings while still giving users a direct path into full summary detail pages.

### Narrative Snapshot On Reports (verified)
Started: Cycle 316
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 316: Report-list narrative snapshot — reused the existing `/api/v1/narratives` watchlist to add a compact `Narrative Snapshot` surface at the top of the main Reports page, showing the latest cluster date plus the top current themes with trajectory, summary count, and average sentiment so users can see active themes without diving into Settings first. Added focused report-list coverage and updated the report-list fetch mocks to handle the background narrative request. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx`, `npm run format:check`, `npm run test:dashboard` (189 passing), and `npm run build`.
- [x] Cycle 317: Verification closeout — verified the full narrative surface flow with focused routed dashboard coverage for the Reports-page `Narrative Snapshot`, the Settings Pipeline `Narrative Watchlist` plus inline clustered-summary drilldown, and `/summaries/:id` navigation from the drilldown evidence links. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Settings.narratives.test.tsx src/pages/__tests__/SummaryView.test.tsx`, `npm run format:check`, `npm test` (1924 passing), `npm run test:dashboard` (189 passing), and `npm run build`.

Design decisions:
- Keep the Reports-page surface read-only and lightweight because Settings still owns the fuller narrative evidence drilldown; the report list should signal what is active without duplicating the whole Pipeline workflow.
- Reuse the existing narrative watchlist API instead of inventing a second report-specific narrative contract, so the new snapshot stays consistent with the daily clustering output and avoids extra backend churn.

### Narrative Snapshot On Main Dashboard (verified)
Started: Cycle 318
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 318: Main dashboard narrative snapshot — reused the existing `/api/v1/narratives` watchlist in `ReportView` so the main dashboard route (`/`) and report-detail view now show a compact `Narrative Snapshot` card alongside the latest report, making the current theme watchlist visible without first detouring into Reports or Settings. Added focused report-view route coverage for the latest-report path, updated existing report-view fetch mocks for the new background request, and verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx`, `npm run format:check`, `npm run test:dashboard` (191 passing), and `npm run build`.
- [x] Cycle 319: Verification closeout — re-verified the latest-report (`/`) and direct report-detail narrative snapshot paths with focused routed dashboard coverage, then reran formatter, the full dashboard suite, and a fresh production build to confirm the compact watchlist remains green as part of the main landing route rather than only on the Reports page. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (191 passing), and `npm run build`.

Design decisions:
- Keep the home/report narrative surface read-only and compact so it improves the landing-page scan without duplicating the fuller inline evidence drilldown that still belongs in Settings.
- Reuse the same `/api/v1/narratives` contract already used by Reports and Settings instead of inventing a separate home-specific read model, keeping narrative state consistent across dashboard surfaces.

### Unusual Activity Snapshot On Main Dashboard (verified)
Started: Cycle 320
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 320: Main dashboard unusual-activity snapshot — reused the existing `/api/v1/unusual-activity` watchlist in `ReportView` so the main dashboard route (`/`) and report-detail view now show a compact `Unusual Activity` card alongside the latest report, surfacing daily spike and copy-cluster watchlist signals without first detouring into Settings. Added focused report-view coverage for the latest-report path, updated existing report-view fetch mocks for the new background request, and verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx`, `npm run format:check`, `npm run test:dashboard` (192 passing), and `npm run build`.
- [x] Cycle 321: Verification closeout — re-verified the latest-report (`/`) and direct report-detail unusual-activity paths with focused routed dashboard coverage, then reran formatter, the full dashboard suite, and a fresh production build to confirm the new background watchlist fetch stays green as part of the main landing route instead of only in the original build slice. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (192 passing), and `npm run build`.

Design decisions:
- Keep the home/report unusual-activity surface read-only and compact so it improves the landing-page scan without duplicating the fuller Pipeline heuristics card and its explanatory caveats.
- Reuse the same `/api/v1/unusual-activity` contract already used by Settings instead of inventing a separate home-specific watchlist model, keeping the heuristic state consistent across dashboard surfaces.

### Macro Backdrop Snapshot On Main Dashboard (verified)
Started: Cycle 322
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 322: Main dashboard macro backdrop snapshot — reused the existing `/api/v1/macro` watchlist in `ReportView` so the main dashboard route (`/`) and report-detail view now show a compact `Macro Backdrop` card alongside the latest report, surfacing the latest overall macro bias plus the top indicator snapshots without first detouring into Settings. Added focused latest-route macro coverage, updated the existing report-view fetch mocks for the new background request, and verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx`, `npm run format:check`, `npm run test:dashboard` (193 passing), and `npm run build`.
- [x] Cycle 323: Verification closeout — re-verified the latest-report (`/`) and direct report-detail macro snapshot paths with focused routed dashboard coverage, reran formatter, the full dashboard suite, and a fresh production build, and attempted a `dev-browser` mocked preview pass. The browser pass was inconclusive because the mocked Vite preview stayed in the auth loading state even after fulfilling `/api/v1/auth/me`, so the closeout relies on the routed dashboard checks rather than a usable browser screenshot. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (193 passing), and `npm run build`.

Design decisions:
- Keep the home/report macro surface read-only and compact so it frames the broader tape without duplicating the fuller Settings Pipeline card or crowding out the report’s own `Macro Regime` section.
- Reuse the existing `/api/v1/macro` contract instead of inventing a separate home-specific macro model, keeping macro state aligned across the landing page and Settings.

### First Mover Watch On Main Dashboard (verified)
Started: Cycle 324
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 324: Main dashboard first-mover watch — added a new authenticated `/api/v1/first-movers` watchlist backed by recent `author_calls`, then reused that read model in `ReportView` so the main dashboard route (`/`) and report-detail view now show a compact `First Mover Watch` card alongside the latest report. The card surfaces recent tracked first calls with author attribution, claim type/text, lead-window timing, and optional resolved credibility without detouring into Settings or duplicating the report body’s own `First Movers` panel. Added focused first-mover watchlist query/API coverage, a latest-route `ReportView` regression, updated the existing report-view fetch mocks for the new background request, and verified with `node --import tsx/esm --test test/unit/first-mover-watchlist.test.ts test/unit/first-mover-watchlist-api.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm test` (1933 passing), `npm run test:dashboard` (194 passing), and `npm run build`.
- [x] Cycle 325: Verification closeout — re-verified the latest-report (`/`) and direct report-detail first-mover snapshot paths with focused watchlist query/API coverage plus routed dashboard checks, then reran formatter, the full dashboard suite, and a fresh production build to confirm the new background watchlist fetch stays green as part of the main landing route instead of only in the original build slice. Verified with `node --import tsx/esm --test test/unit/first-mover-watchlist.test.ts test/unit/first-mover-watchlist-api.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (194 passing), and `npm run build`.

Design decisions:
- Keep the home/report first-mover surface anchored to factual `author_calls` timing data instead of inventing a new score, so it complements the report’s generated `First Movers` section with a distinct monitored-author watchlist.
- Reuse Settings’ influencer-review concepts in a lighter read-only card, showing lead-window and optional resolved credibility only when those facts already exist.

### Catalyst Watch On Main Dashboard (verified)
Started: Cycle 326
Estimated: 1 cycle
Focus: System-level follow-up

Sub-tasks:
- [x] Cycle 326: Main dashboard catalyst watch — reused the existing `/api/v1/calendar-events` read model in `ReportView` so the main dashboard route (`/`) and report-detail view now show a compact `Catalyst Watch` card alongside the latest report, surfacing scheduled macro, unlock, expiry, governance, launch, legal, and custom events without first detouring into Settings. Added focused latest-route report-view coverage, updated the existing report-view fetch mocks for the new background request, and verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.calendar.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx`, `npm run format:check`, `npm run test:dashboard` (195 passing), and `npm run build`.
- [x] Cycle 327: Verification closeout — added the missing direct `/reports/:id` regression for the `Catalyst Watch` card so the feature is locked on both the latest-report route and direct report detail, then reran focused routed dashboard coverage, formatter, the full dashboard suite, and a fresh production build to confirm the extra background calendar fetch stays green as part of the main landing route. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.calendar.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (196 passing), and `npm run build`.

Design decisions:
- Keep the home/report catalyst surface read-only and compact so it exposes scheduled risk without duplicating the fuller calendar management UI that still belongs in Settings.
- Reuse the existing `/api/v1/calendar-events` contract rather than inventing a new home-specific snapshot model, keeping the prompt-driving catalyst state aligned across the landing page and Settings.

### Dashboard Auth Expiry Hardening (verified)
Started: Cycle 328
Estimated: 1 cycle
Focus: Code-level follow-up

Sub-tasks:
- [x] Cycle 328: Event-driven 401 handling for dashboard fetches — replaced the dashboard `apiFetch()` 401 path that previously returned a never-settling promise with an explicit `AUTH_EXPIRED_EVENT`, then taught `AuthProvider` to clear the current user when that event is broadcast so `ProtectedRoute` can redirect without leaving shared loaders or preview environments stuck in `Loading...`. Added focused regressions for `apiFetch` 401 behavior and the provider’s auth-expiry response, and verified with `cd dashboard && npx vitest run src/lib/__tests__/api.test.ts src/components/__tests__/AuthProvider.test.tsx`, `npm run format:check`, `npm run test:dashboard` (199 passing), and `npm run build`.
- [x] Cycle 329: Redirect hardening after real browser probe — a fresh `dev-browser` preview probe still reproduced the first-load `/auth/me` 401 hang on `/`, so `apiFetch()` now performs a deterministic login redirect through a shared `authRedirect` helper while still broadcasting `AUTH_EXPIRED_EVENT`, and `AuthProvider` now registers its auth-expiry listener before the initial check. I also added routed dashboard regressions for initial-load and focus-triggered 401 redirects alongside the shared API assertions. Verified with `cd dashboard && npx vitest run src/lib/__tests__/api.test.ts src/components/__tests__/AuthProvider.test.tsx`, `npm run format:check`, `npm run test:dashboard` (201 passing), and `npm run build`. A follow-up `dev-browser` pass against Vite preview remained inconclusive for final closeout because the mocked `/auth/me` route still left the page on the protected loading shell despite the stronger redirect path and rebuilt assets, so this slice stays build-complete rather than verified.
- [x] Cycle 330: Verification closeout — added a real routed `AppRouter` regression for both initial-load and focus-triggered `/auth/me` 401 flows so the full dashboard shell, protected route, and login handoff are exercised together instead of only the isolated auth provider. Re-ran the shared auth helper/provider tests, formatter, the full dashboard suite, and a fresh production build. Verified with `cd dashboard && npx vitest run src/lib/__tests__/api.test.ts src/components/__tests__/AuthProvider.test.tsx src/components/__tests__/AppRouter.auth.test.tsx`, `npm run format:check`, `npm run test:dashboard` (203 passing), and `npm run build`.

Design decisions:
- Prefer an explicit auth-expired event over mutating `window.location` inside `apiFetch()`, so shared request callers still settle and clear their loading state instead of hanging until browser navigation wins.
- Keep 401 handling quiet on `/login` itself, where auth failures should still surface to the caller without broadcasting a global logout event.
- Treat the browser-proven initial `/auth/me` 401 hang as higher priority than preserving a purely SPA redirect path, so the shared fetch helper now has a deterministic login redirect fallback instead of relying solely on downstream listeners to move the route.

### Price Watch On Main Dashboard (verified)
Started: Cycle 331
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 331: Main dashboard price watch — added a dedicated `getPriceWatchOverview()` read model plus authenticated `GET /api/v1/price-watch`, reusing the latest token price snapshots together with the latest daily sentiment rollups so the landing page can surface current movers and simple contrarian tags without drilling into entity detail first. `ReportView` now fetches that overview on both `/` and direct report detail and renders a compact `Price Watch` card with 24h/7d moves, volume, market cap, and bearish-vs-price-up or bullish-vs-price-down callouts. I added structural API coverage, extended the routed report-view tests for the new background fetch, and updated the stale auth regression expectations that the full `npm test` sweep exposed from the prior auth-expiry hardening slice. Verified with `node --test test/unit/price-watch-api.test.ts test/unit/regression.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.calendar.test.tsx`, `npm run format:check`, `npm test` (1940 passing), `npm run test:dashboard` (204 passing), and `npm run build`.
- [x] Cycle 332: Verification closeout — added the missing direct `/reports/:id` `Price Watch` regression so both the latest-report route and direct report detail now exercise the new snapshot surface, then re-ran focused API coverage, focused routed `ReportView` checks, the full dashboard suite, formatter, and a fresh production build. A simplified mocked preview probe against the built app stayed inconclusive because the card never rendered under that browser harness, so I treated the routed dashboard shell plus build outputs as the reliable verification signal rather than overstating browser evidence.

Design decisions:
- Keep the new home/report price surface read-only and snapshot-sized so it closes the main-dashboard market-context gap without duplicating the fuller per-entity price history that still belongs in Settings.
- Reuse the existing prompt-level contrarian heuristic thresholds for the watchlist badge, so the compact card speaks the same “price vs sentiment divergence” language as daily and pulse report generation instead of inventing a separate frontend-only rule.

### Regional Divergence Snapshot On Main Dashboard (verified)
Started: Cycle 351
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 351: Main dashboard regional-divergence snapshot — reused the existing authenticated `/api/v1/divergence` contract in `ReportView` so the main dashboard route (`/`) and direct report-detail view now show a compact `Regional Divergence` card alongside the latest report instead of hiding cross-language sentiment splits inside Settings entity drilldowns only. The new snapshot surfaces the top trailing-7d EN-vs-ID gaps with per-entity comparison copy, divergence severity badges, signed sentiment values, and mention counts, while keeping the fuller per-entity investigation flow in Settings &gt; Entities. Added focused routed `ReportView` coverage for both the latest-report and direct report-detail paths, updated the existing report-view fetch mocks for the new background request, and verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.divergence.test.tsx src/pages/__tests__/ReportView.test.tsx src/pages/__tests__/ReportView.price.test.tsx src/pages/__tests__/ReportView.macro.test.tsx src/pages/__tests__/ReportView.unusual-activity.test.tsx src/pages/__tests__/ReportView.first-movers.test.tsx src/pages/__tests__/ReportView.narratives.test.tsx src/pages/__tests__/ReportView.calendar.test.tsx`, `npm run format:check`, `npm run test:dashboard` (218 passing), and `npm run build`.
- [x] Cycle 352: Verification closeout — added the missing empty-watchlist regression so `ReportView` now explicitly proves the `Regional Divergence` card stays hidden when `/api/v1/divergence` returns no entries, then re-ran focused routed report-view coverage, formatter, the full dashboard suite, and a fresh production build to close the remaining verification gap around this new background snapshot fetch. Verified with `cd dashboard && npx vitest run src/pages/__tests__/ReportView.divergence.test.tsx src/pages/__tests__/ReportView.test.tsx`, `npm run format:check`, `npm run test:dashboard` (219 passing), and `npm run build`.

Design decisions:
- Reuse the existing `/api/v1/divergence` contract instead of inventing a separate landing-page read model, so the home/report watch card stays aligned with the existing Settings entity-detail divergence data.
- Keep the landing-page snapshot on a fixed trailing 7-day window with the top three entries by divergence, so it answers “what should I be watching?” without importing the full Settings selector and per-entity drilldown complexity onto the report route.

### Regional Divergence In Reports (verified)
Started: Cycle 353
Estimated: 1 cycle
Focus: System-level follow-up

Sub-tasks:
- [x] Cycle 353: Report-level regional divergence contract — extended `MarketReportLLMSchema` plus the daily and pulse prompt instructions so Stage 3 can emit bounded `regionalDivergence` lines from the existing `<regional_divergence>` context instead of leaving cross-language splits buried inside general analysis prose. Exposed parsed `regionalDivergence` from report serialization, rendered a dedicated `Cross-Language Signals` section on report detail, and extended Discord delivery to render the same lines from both camelCase and snake_case stored report bodies within the existing field-priority flow. Added structural contract coverage, schema/runtime persistence tests, webhook ordering regressions, and routed report-view coverage for the new report-body section. Verified with `node --import tsx/esm --test test/unit/regional-divergence-report.test.ts test/unit/schemas.test.ts test/unit/synthesize-stage3.test.ts test/unit/webhook.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.divergence.test.tsx`, `npm run format:check`, `npm test` (1998 passing), `npm run test:dashboard` (219 passing), and `npm run build`.
- [x] Cycle 354: Verification closeout — added the missing latest-route regression so the report-body `Cross-Language Signals` section is now explicitly covered on `/` as well as direct `/reports/:id`, then re-ran focused dashboard and delivery coverage, a direct `buildEmbed()` probe against a snake_case stored report body, formatter, the full unit suite, the full dashboard suite, and a fresh production build. The direct embed probe confirmed the current fallback delivery order as `Key Events` → `Market Catalysts` → `Regional Divergence` → `Unusual Activity` with the expected public report URL attached.

Design decisions:
- Keep report-level regional divergence as a concise string array rather than duplicating the richer `/api/v1/divergence` snapshot shape inside stored report bodies, so Stage 3 can summarize only the splits that matter for traders while Settings and the landing-page watch card remain the full drilldown surfaces.
- Put `Regional Divergence` after `Market Catalysts` but ahead of `Narrative Shifts`, `Unusual Activity`, and `Macro Alerts` in Discord delivery, because cross-language positioning splits are usually more actionable than softer follow-through heuristics once higher-priority chain, regime, timing, and price fields are absent.

### Regional Divergence Preview On Report Discovery (verified)
Started: Cycle 355
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 355: Report discovery regional-divergence previews — extended the shared `getReportSecondaryPreview()` selector plus both `/api/v1/reports` and `/api/v1/search` serializers so stored `regionalDivergence` now surfaces as a first-class `Regional Divergence` preview on report cards and report search hits instead of staying hidden until report detail or Discord delivery. The preview priority now stays `Event Chain` -> `First Mover` -> `Price Alert` -> `Market Catalyst` -> `Regional Divergence` -> `Narrative Shift` -> `Unusual Activity` -> `Macro Alert`, so cross-language positioning splits show up before narrative-change and softer heuristic summaries while still staying behind direct story, timing, price, and scheduled-catalyst context. Added structural API coverage plus focused shared-preview, report-list, and Search regressions for the new ordering. Verified with `node --test test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/lib/__tests__/reportPreview.test.ts src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx`, `npm run format:check`, `npm test` (2000 passing), `npm run test:dashboard` (223 passing), and `npm run build`.
- [x] Cycle 356: Verification closeout — added the missing shared-preview regression so `Market Catalyst` is now explicitly locked ahead of `Regional Divergence` in `getReportSecondaryPreview()`, then re-ran the structural API contract plus the focused shared-preview and routed Reports/Search coverage to confirm the new preview still appears on both discovery surfaces while staying behind catalysts and ahead of `Narrative Shift`, `Unusual Activity`, and `Macro Alert`. Followed that with formatter, the full unit suite, the full dashboard suite, and a fresh production build to close the remaining verification gap around the shared discovery-preview grammar.

Design decisions:
- Reuse the existing shared `getReportSecondaryPreview()` helper instead of adding page-local regional-divergence rules, so report list and Search keep one fallback grammar as more report-side signals accumulate.
- Put `Regional Divergence` behind `Market Catalyst` but ahead of `Narrative Shift`, `Unusual Activity`, and `Macro Alert`, because cross-language positioning splits are usually more actionable than cluster-trajectory changes and softer heuristics once no active-chain, first-mover, price-alert, or catalyst preview is present.

### Price Alert Preview On Report Discovery (verified)
Started: Cycle 333
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 333: Report preview price signals — extended the shared report-preview selector plus both `/api/v1/reports` and `/api/v1/search` serializers so stored `priceAlerts` now surface as a first-class `Price Alert` preview on report cards and report search hits instead of being hidden until report detail or Discord delivery. The preview priority now stays `Event Chain` -> `First Mover` -> `Price Alert` -> `Unusual Activity` -> `Macro Alert`, keeping active story routing first while still surfacing market-moving price divergences ahead of softer heuristics. Added structural API coverage plus focused report-list and Search regressions for the new preview and ordering. Verified with `node --test test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx`, `npm run format:check`, `npm test` (1942 passing), `npm run test:dashboard` (207 passing), and `npm run build`.
- [x] Cycle 334: Verification closeout — re-ran the structural API contract plus focused routed Reports/Search page coverage to confirm the new `Price Alert` preview appears on both discovery surfaces and still outranks `Unusual Activity` and `Macro Alert` whenever no active-chain preview is present. Followed that with formatter, full unit, full dashboard, and production build sweeps to confirm the preview contract remains stable across the broader report UI and backend serialization path.

Design decisions:
- Reuse the existing shared `getReportSecondaryPreview()` helper instead of adding page-local preview rules, so report list and Search keep the same fallback grammar as more report-side signals accumulate.
- Put `Price Alert` behind `First Mover` but ahead of `Unusual Activity` and `Macro Alert`, because direct price-vs-sentiment or breakout context is usually the most actionable pre-open signal once no active story preview is present.

### Market Catalyst Preview On Report Discovery (verified)
Started: Cycle 335
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 335: Report preview catalyst handoff — extended the shared report-preview selector plus both `/api/v1/reports` and `/api/v1/search` serializers so stored `marketCatalysts` now surface as a first-class `Market Catalyst` preview on report cards and report search hits instead of only appearing after opening report detail. The discovery preview priority now stays `Event Chain` -> `First Mover` -> `Price Alert` -> `Market Catalyst` -> `Unusual Activity` -> `Macro Alert`, so scheduled catalysts remain visible before softer heuristic signals while still staying behind direct story and price context. Added structural API coverage plus focused report-list and Search regressions for the new preview and ordering. Verified with `node --test test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx`, `npm run format:check`, `npm test` (1944 passing), `npm run test:dashboard` (209 passing), and `npm run build`.
- [x] Cycle 336: Verification closeout — re-ran the structural API contract plus focused routed Reports/Search coverage to confirm the new `Market Catalyst` preview appears on both discovery surfaces and still outranks `Unusual Activity` and `Macro Alert` whenever no active-chain, first-mover, or price-alert preview is present. Followed that with formatter, full unit, full dashboard, and production build sweeps to verify the shared preview contract across the broader report UI and backend serialization path. A mocked `dev-browser` pass against Vite preview remained inconclusive because the routed Reports page never got past its initial shell before timing out, so I treated the routed test shell plus full suite as the verification signal instead of overstating browser evidence.

Design decisions:
- Reuse the existing shared `getReportSecondaryPreview()` helper instead of adding page-local catalyst rules, so report list and Search continue to share one fallback grammar as more report-side signals accumulate.
- Put `Market Catalyst` behind `Price Alert` but ahead of `Unusual Activity` and `Macro Alert`, because scheduled event watchpoints are usually more actionable than softer heuristic summaries when there is no active-chain or first-mover preview to lead with.

### Report Delivery Watchlist Completion (verified)
Started: Cycle 337
Estimated: 1 cycle
Focus: System-level follow-up

Sub-tasks:
- [x] Cycle 337: Discord delivery watchlist completion — extended `src/deliver/webhook.ts` so report embeds now recognize and render `Price Alerts` plus `Market Catalysts` alongside the existing event-chain, macro-regime, first-mover, unusual-activity, and macro-alert fields instead of dropping those report-body signals on the floor before Discord delivery. The delivery parser now also tolerates camelCase and snake_case stored report bodies for the newer watchlist fields, keeping older or mixed-format reports from silently missing the new embed sections. Added focused webhook regressions for bullet formatting, field-cap ordering, and backward-compatible snake_case parsing, then verified with `node --import tsx/esm --test test/unit/webhook.test.ts`, `npm run format:check`, `npm test` (1948 passing), and `npm run build`.
- [x] Cycle 338: Verification closeout — added the missing fallback-priority regression for `Market Catalysts` versus softer heuristics when no `Price Alerts` are present, then re-ran focused delivery coverage, a direct `buildEmbed()` probe using a snake_case stored report body, formatter, the full unit suite, and a fresh production build. The direct embed probe confirmed the final push-notification order as `Price Alerts` → `Market Catalysts` → `Unusual Activity` → `Macro Alerts` when no higher-priority chain/regime/first-mover fields are present, with the expected public report URL attached. Verified with `node --import tsx/esm --test test/unit/webhook.test.ts`, `node --import tsx/esm --input-type=module`, `npm run format:check`, `npm test` (1949 passing), and `npm run build`.

Design decisions:
- Keep the new delivery fields inside the existing 4-field cap and embed grammar instead of widening Discord payload complexity, so the webhook stays compact and still honors the long-standing field-priority model.
- Put `Price Alerts` ahead of `Market Catalysts`, `Unusual Activity`, and `Macro Alerts` once `Key Events`, `Event Chains`, and any `Macro Regime` / `First Movers` context are accounted for, because direct price-vs-sentiment divergences are usually the most actionable extra trader signal in a push notification.
- Accept both camelCase and snake_case report-body keys for the newly added delivery fields so older stored bodies and mixed historical payloads do not silently lose watchlist context during delivery.

### Market Report Blank-String Hardening (verified)
Started: Cycle 339
Estimated: 1 cycle
Focus: Code-level follow-up

Sub-tasks:
- [x] Cycle 339: Report schema blank-string hardening — introduced a shared trimmed `nonEmptyText` helper in `src/process/schemas.ts` and used it across `MarketReportLLMSchema` plus `MacroRegimeLLMSchema` so whitespace-only `tldr`, report lines, rationale, sentiment reasons, section copy, and new-project descriptions can no longer pass validation and leak into report detail, previews, or Discord delivery. Added focused schema regressions for blank `tldr`, blank key-event / catalyst / price-alert lines, blank macro-regime rationale, blank entity-sentiment reasons, blank section bodies, and blank project descriptions. Updating the schema surfaced a few older brittle structural tests that matched the prior literal `z.array(z.string())` / `.min(1)` source text, so I tightened those tests to accept the shared helper instead of pinning the implementation to one exact spelling. Verified with `node --import tsx/esm --test test/unit/schemas.test.ts test/unit/unusual-activity-report.test.ts test/unit/price-prompt-api.test.ts test/unit/first-mover-report.test.ts test/unit/macro-report-alerts.test.ts test/unit/cycle121-correlate-regression.test.ts`, `npm run format:check`, `npm test` (1957 passing), and `npm run build`.
- [x] Cycle 344: Verification closeout — added focused Stage 3 retry regressions proving the blank-string guard trips on real LLM response paths, not just direct schema parses: daily synthesis now retries with validation feedback when a `marketCatalysts` line is whitespace-only, and pulse synthesis now retries when `macroRegime.rationale` is blank after trimming. Re-ran the focused daily/pulse suites, formatter, the full unit suite, and a fresh production build to close the remaining verification gap around schema-invalid report retries. Verified with `node --import tsx/esm --test test/unit/synthesize-stage3.test.ts test/unit/pulse.test.ts`, `npm run format:check`, `npm test` (1976 passing), and `npm run build`.

Design decisions:
- Keep the hardening scoped to the shared market-report contract rather than broadening it into chunk-summary or prompt refactors, so the diff directly targets the blank-output risk on user-facing report surfaces.
- Use one shared trimmed `nonEmptyText` helper instead of repeating `z.string().trim().min(1)` across every report field, so future schema additions inherit the same empty-string guard without repeating brittle source patterns.
- Fix the impacted structural tests to assert the validation contract rather than the previous literal implementation spelling, because the repo already documents those source-string tests as fragile under harmless refactors.

### Config Bootstrap Secret Logging Hardening (verified)
Started: Cycle 361
Estimated: 1 cycle
Focus: Code-level research

Sub-tasks:
- [x] Cycle 361: Pre-logger config secret logging hardening — tightened `loadConfig()` so bootstrap warnings no longer echo any generated `API_KEY` fragment or the raw invalid `ALERT_WEBHOOK_URL` value before the masked logger exists. The API key warning box now states that the temporary key was generated in memory only, and invalid alert webhook warnings are generic instead of interpolating the secret-looking input. Added a behavioral runtime regression in `test/unit/config-bootstrap-logging.test.ts` that captures `console.error` / `console.warn`, and updated the older structural guard in `test/unit/cycle110-config-regression.test.ts` to assert that generated keys are omitted entirely and invalid webhook warnings stay generic. Verified with `node --import tsx/esm --test test/unit/config-bootstrap-logging.test.ts test/unit/cycle110-config-regression.test.ts`, `npm run format:check`, `npm test` (2020 passing), and `npm run build`.

Design decisions:
- Treat pre-logger stdout as sensitive because Pino masking is not active yet, so bootstrap warnings should describe the state change without echoing any secret or secret fragment.
- Add a runtime console-capture regression alongside the older structural source guard so future refactors still prove the emitted warning text stays secret-safe.

### Narrative Shifts In Reports (verified)
Started: Cycle 340
Estimated: 1 cycle
Focus: Roadmap-gap follow-up

Sub-tasks:
- [x] Cycle 340: Report-level narrative shifts — added bounded `narrativeShifts` to `MarketReportLLMSchema`, taught the daily and pulse prompts to emit it from `<narrative_context>`, and pulled recent narrative-clustering context into pulse synthesis so the new field is available across both daily and 3-hour report outputs instead of staying daily-only background context. I also exposed parsed `narrativeShifts` from `GET /api/v1/reports/:id`, rendered a dedicated `Narrative Shifts` section in report detail, and extended Discord delivery to render the same lines from both camelCase and snake_case stored report bodies within the existing embed-field priority model. Added focused structural/runtime coverage for schema + prompt wiring, pulse narrative-context injection, report-body persistence, report-detail rendering, and delivery ordering, then fixed one brittle `api-serialization` source-slice test that the new report field pushed beyond its old hard-coded window. Verified with `node --import tsx/esm --test test/unit/narrative-report-shifts.test.ts test/unit/schemas.test.ts test/unit/pulse.test.ts test/unit/synthesize-stage3.test.ts test/unit/webhook.test.ts test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportView.narratives.test.tsx`, `npm run format:check`, `npm test` (1972 passing), `npm run test:dashboard` (210 passing), and `npm run build`.
- [x] Cycle 341: Verification closeout — added the missing latest-report (`/`) regression so `Narrative Shifts` is now explicitly covered on both the home route and direct `/reports/:id` detail instead of only on the direct report path, then re-ran focused structural delivery coverage, a direct `buildEmbed()` runtime probe using snake_case `narrative_shifts`, formatter, the full unit suite, the full dashboard suite, and a fresh production build. The direct embed probe confirmed the final push-notification ordering as `Key Events` → `Market Catalysts` → `Narrative Shifts` → `Unusual Activity` when those are the highest-priority available watchlist fields, with the expected public report URL attached.

Design decisions:
- Keep `narrativeShifts` as a concise string array rather than a heavier nested object so it fits the existing report/watchlist grammar across report detail, stored bodies, and Discord delivery.
- Pull narrative context into pulse synthesis too, so the new field works consistently across daily and pulse reports instead of turning into a daily-only contract hole.
- Place `Narrative Shifts` after `Market Catalysts` but ahead of `Unusual Activity` and `Macro Alerts` in delivery ordering, because cluster-trajectory changes are usually more actionable than softer heuristics when higher-priority chain, regime, first-mover, and price fields are absent.

### Narrative Shift Preview On Report Discovery (verified)
Started: Cycle 342
Estimated: 1 cycle
Focus: Product-level follow-up

Sub-tasks:
- [x] Cycle 342: Report discovery narrative-shift previews — extended the shared report-preview selector plus both `/api/v1/reports` and `/api/v1/search` serializers so stored `narrativeShifts` now surface as a first-class `Narrative Shift` preview on report cards and report search hits instead of staying hidden until report detail or Discord delivery. The preview priority now stays `Event Chain` -> `First Mover` -> `Price Alert` -> `Market Catalyst` -> `Narrative Shift` -> `Unusual Activity` -> `Macro Alert`, so changes in cluster trajectory are visible before softer heuristic summaries while still staying behind direct story, timing, price, and scheduled-catalyst context. Added structural API coverage plus focused report-list and Search regressions for the new preview and ordering. Verified with `node --test test/unit/api-serialization.test.ts`, `cd dashboard && npx vitest run src/pages/__tests__/ReportList.test.tsx src/pages/__tests__/Search.test.tsx`, `npm run format:check`, `npm test` (1974 passing), `npm run test:dashboard` (213 passing), and `npm run build`.
- [x] Cycle 343: Verification closeout — added a shared `reportPreview` regression so the preview grammar explicitly keeps `Market Catalyst` ahead of `Narrative Shift` and suppresses secondary previews entirely when active chains are present, then re-ran focused API serialization plus routed Reports/Search coverage, formatter, the full dashboard suite, and a fresh production build. This closes the remaining verification gap by proving the shared helper order directly instead of only through page-local fixture examples.

Design decisions:
- Reuse the existing shared `getReportSecondaryPreview()` helper instead of adding page-local narrative rules, so report list and Search keep one fallback grammar as more report-side signals accumulate.
- Put `Narrative Shift` behind `Market Catalyst` but ahead of `Unusual Activity` and `Macro Alert`, because cluster-trajectory changes are usually more actionable than softer heuristic summaries once no active-chain, first-mover, price-alert, or catalyst preview is present.

## Queue

**Open: 0 critical, 1 high, 1 medium, 0 feature-gap**

### Critical
(none)

### High
- **H-002** (Cycle 364, codex-audit): Discord REST poll failures masked as success. `discordFetch()` returns `null` on 401/403/429/network errors; `pollDiscordChannel()` treats that as empty; `last_fetched_at` still updates. Health monitor sees a recent timestamp while ingestion is stalled. Fix: distinguish fetch error from empty response, don't update `last_fetched_at` on error.

### Medium
- **M-004** (Cycle 364, codex-audit): Discord REST poller has no pagination — single page of 50 messages per tick. If >50 messages arrive between polls, newest 50 are fetched but older ones are skipped (Discord returns newest-first with `?after=`). Fix: paginate until fewer than 50 messages returned, or accept 50-per-tick as sufficient for poll_interval=60s.

### Feature Gaps
(none — R.2 first-mover timing verification closed in Cycle 309)

<details>
<summary>391 findings resolved across cycles 27-137 — click to expand</summary>

See git history for full details. Key milestones:
- Cycles 27-80: Core pipeline wiring and bug fixes
- Cycles 80-128: Code-level audits, entity lifecycle, adapters, health monitor
- Cycles 129-137: Product-level audit (27 dashboard findings), normalize audit, UX polish
</details>

## Lessons

### Critical Lessons

- **Product completeness > code quality.** 128 cycles auditing code while the dashboard couldn't add a source. Always check: can the user complete core workflows?
- **Auto-compact this file.** Every 10 cycles or if >150 lines, prune. Resolved findings and per-cycle logs are in git history.
- **Product-level audits are highest-yield.** Cycle 129 found 27 issues (2 critical) in one scan.

### Recurring Bug Patterns

- **Epoch ms vs seconds** — `to_timestamp(created_at)` treats ms as seconds. Watch new code touching `created_at`.
- **BIGINT as strings** — node-postgres OID 20 needs `pg.types.setTypeParser(20, parseInt)`.
- **json_agg without ORDER BY** — non-deterministic. Any `result[0]` from json_agg is vulnerable.
- **Zod empty strings** — `z.string()` allows empty. LLM output name/title fields need `.min(1)`.
- **console.error bypasses pino** — always use logger for secrets.
- **Dashboard-API gaps** — backend endpoints built but never surfaced in UI. Cross-check routes vs fetch calls.
- **CI health check pitfalls** — auth-required endpoints fail unauthenticated curl; health endpoints return 503 for data-level degradation, not server failure.

### Build Patterns

- Feature builds need 5-6 agents: schema, impl, integration, prompt, dashboard, test
- Always verify end-to-end after a feature build before marking done
- Structural tests (read source as string, regex match) are fragile to Prettier reformatting — use flexible patterns

### Product Decisions

- No plugin system, no event bus, no multi-agent (CLAUDE.md — killed v1)
- Discord tokens are user tokens (TOS risk, disposable accounts)
- News adapter exists but is URL-extraction based, not poll-based — skip in poll loop
- Narrative clustering shipped in Phase 1 (ahead of 2.2 schedule)

### Research Strategy

- **Rotate 4 types**: product-level → system-level → code-level → roadmap-gap
- **Agent false positives need verification.** Always cross-reference against prior changes and CLAUDE.md decisions.
- **Batch medium fixes by file** to avoid conflicts.
- **New features are highest-yield audit targets.** New code always has more bugs.

## Coverage Map

| Area | Last audit | Notes |
|------|-----------|-------|
| Chat handler | Cycle 172 | `keyword_search` surfaces recent linked event chains for entity lookups, `semantic_search(type='report')` returns compact report previews with TLDR plus chain context, the system prompt prefers report-mode search for ongoing-story and timeline questions, omitted-type timeline/follow-up queries auto-route to report search, chat responses return report-vs-summary tool usage labels plus semantic-search citations, and `read_raw` calls now surface clickable raw item citations |
| LLM/prompts | Cycle 340 | Market-report schemas now reject whitespace-only `tldr`, report lines, macro-regime rationale, sentiment reasons, section copy, and new-project descriptions via a shared trimmed non-empty-text helper, and the daily plus pulse prompts now both consume `<narrative_context>` and can emit report-level `narrativeShifts` alongside the existing event-chain, catalyst, price, unusual-activity, first-mover, and macro surfaces |
| Scheduler/ops | Cycle 152 | Health check now refreshes recurring calendar lifecycle; startup catch-up added |
| Webhook/delivery | Cycle 340 | Discord embeds now surface event chains, macro regime, first movers, price alerts, market catalysts, narrative shifts, unusual activity, and macro alerts when present while still enforcing the existing 4-field and total embed size limits, delivery parsing tolerates camelCase plus snake_case report-body keys for the newer watchlist fields, and focused verification covers the narrative-shift fallback priority plus the existing snake_case delivery contract |
| Normalize | Cycle 134 | batch_id, claim-by-design, news skip, regex lookahead |
| Embed/vector | Cycle 128 | Dimension guard added |
| Config/deploy | Cycle 137 | camelCase PATCH, CI health check fixed |
| Dashboard frontend | Cycle 254 | Report view renders Event Chains panels, report detail pages now expose persisted chain drilldown cards that link into the latest focused summary in each active chain, and can now also honor a focused `chain` query param so multi-story reports move the requested story to the top, highlight it, and expose clear focus/switch/reset controls instead of staying a flat unfocused grid. The report list shows chain previews on cards plus direct focused-summary entry links for multiple active chains when a report is tracking more than one story, Search can query reports alongside summaries with chain snippets, preserve normal deep-links into the correct report or summary detail page, and now also expose multiple active-chain jumps from report hits instead of collapsing everything to one arbitrary story, plus an exact-count overflow handoff like `3 more active chains in report` when extra active stories are hidden beyond the visible two preview links, with those report-list and Search cards now able to reveal the hidden active-chain links inline through a shared preview section with in-place loading and retry instead of forcing a full report hop, keep those expanded inline chain links sticky across remounts and rerenders within the same session so the same story set does not collapse back to preview state after refreshes, now also let users collapse those sticky inline expansions back to the default preview in place instead of forcing a one-way expanded state, now reconcile cached inline expansions against the current preview roots plus current hidden-chain count so stale cached story sets disappear when report previews change or shrink while partial cached expansions still keep `Show more` available, and now let fully expanded inline chain previews refresh the latest active-chain set in place instead of forcing a collapse/reset to pick up newer hidden-story changes while also keeping the shared report/search Event Chain preview summary sentence in sync when those inline previews refresh or collapse, with the surrounding report/search preview body copy now following the same refresh/collapse lifecycle instead of getting stuck on the older list/search payload, and the top-row report/search preview chrome now exposes a live `Lead chain` label that tracks the current active story during refresh and collapse instead of staying frozen on the original preview order, plus a live `Active stories` count that tracks the currently visible story set during expansion and collapse instead of staying buried inside the inline preview section, while the lead-chain header chip itself now acts as a direct focused-summary drilldown link that follows the current lead story route during refresh and collapse instead of staying passive text, the active-story count chip now drives the same inline expand/collapse preview state from the card header instead of just mirroring it, fully expanded cards now surface a top-row `Refresh stories` control that refetches the current active-story set in place instead of forcing users to return to the lower inline preview controls, both header actions now swap to explicit loading copy like `Loading stories...` and `Refreshing stories...` while preview updates are in flight so the card chrome itself explains the current async state, and failed expand/refresh fetches now also surface header-level retry copy like `Retry loading stories` and `Retry refresh stories` so recovery stays visible in the top-row chrome instead of only in the lower inline preview error row. Those same preview surfaces now also expose an `Open focused report` link that follows the current lead story across expansion, refresh, and collapse so users can enter `/reports/:id?chain=...` already anchored to the right Event Chain instead of landing only on the generic report route. Focused /reports/:id?chain=... routes now also reorder and highlight the matching textual Event Chain summary so the top summary panel stays aligned with the selected story instead of leaving only the drilldown cards focused. The main report-card body links on report-list and Search now also follow the current focused report-chain route from shared preview state instead of staying on the generic report page while the lead story changes. Chat report citations now also carry focused report-chain routes when report-mode semantic search can identify a lead chain, so those source pills open directly into the right anchored report detail instead of always landing on the generic report route. Chat summary citations now also carry focused summary-chain routes when summary-mode semantic search can identify persisted chain context, so those source pills open directly into the right anchored summary drilldown instead of always landing on the generic summary route, and anchored report/summary chat pills now visibly mark focused chains with richer tooltip context so the story-specific drilldown path is clearer before opening, while repeated same-type citations from the same focused chain now collapse into one grouped chat pill with a visible citation count badge and merged tooltip context instead of rendering as separate duplicate story pills, and those grouped chat pills can now reveal the hidden same-chain citations inline through lightweight show/hide controls while keeping the primary anchored pill intact, with those revealed same-chain links now also rendering compact inline snippet previews so the chat source row shows enough context to choose the right follow-up citation without getting noisy, plus lightweight date badges sourced from semantic-search metadata so the revealed citations are easier to scan chronologically, and now also relative `Earlier`, `Later`, and `Same day` badges so the expanded citation list is easier to compare against the grouped lead at a glance, while grouped lead pills now also surface a visible `Lead cite` badge plus the lead citation date so those relative chronology cues have a clear inline anchor, and those revealed same-chain citations can now promote themselves into the grouped lead slot in place so the main pill’s route, date, and comparison cues follow the citation the user wants to anchor on without leaving the chat source row. The chat tool indicator distinguishes report semantic search from summary semantic search, chat messages render clickable report, summary, and raw item source pills, `/summaries/:id` provides a summary drilldown page that now surfaces persisted event timestamps plus linked-chain position and timeline metadata when the backend has real event-chain rows for that summary, keeps previous/next summary drilldowns anchored to a focused `chain` query param, and highlights matching chain events on arrival so cross-summary chain browsing stays anchored to the same story, and the raw-item/feed citation surfaces now share a common raw-message card component, a common raw-message helper module for attachment parsing plus time/footer formatting, a common raw-message action helper module for feed/item drilldown navigation, a common raw-feed navigation helper module for focused/live route building plus target-aware raw citation `context` query-param handoffs, a common raw-source-item model/normalization module for `/items/:id` responses plus focused feed context, a common focused raw-feed hook for pin-state selection, context loading, the live-gap handoff label, and retryable wider-context fetches that preserve the current pinned citation surface instead of blanking on expansion errors, a common raw-feed stream hook for source resolution, feed paging, deduped append/prepend updates, and live polling, a common focused-citation panel component for the pinned raw-feed surface, a common top-level citation-nav component for focused-feed and raw-item header link chrome, a common timeline context-section component for the focused-feed and raw-item “Earlier/Later” bands, a common panel-header component for focused-feed and raw-item explanatory framing, a common timeline-divider component for focused-feed live-gap and raw-item context handoff cues, a common bordered citation-surface component for the focused-feed panel and raw-item primary drilldown shell, a shared context-window control component for the focused-feed and raw-item expand/loading/error/retry chrome, and a shared metadata-grid surface across the focused-feed and raw-item destinations for source/capture/flag details. A common live feed list component handles the stream’s loading, empty, focused-card, and load-more states, while a common raw-feed header component handles the source picker, live toggle, and retryable feed error banner, and a common no-sources feed shell handles the route fallback state. A common item-view source-context panel handles earlier/later same-source drilldown cards plus inline retry/error feedback for wider context loads, shared item-view header and metadata-grid components handle the remaining detail-page chrome, and a shared raw-item view hook handles the remaining `/items/:id?context=` fetch, normalization, and request-key route-state orchestration while keeping the current item stable during larger same-item context fetches. `/items/:id` provides a raw-message drilldown page whose main citation surface, explanatory route-level framing, shared top-of-surface feed navigation, nearby same-source context, timeline actions, closing metadata, in-place `Show more context` path, retryable wider-context error feedback, and sticky feed/item route handoffs stay visually aligned with the pinned citation experience in `/feed`, with the raw-item destination able to expand nearby same-source context from 3 to 6 messages per side without dropping out of the focused item view while the wider context loads, keep that wider window active while you continue walking adjacent raw items in the same drilldown route, and carry that wider context back into `/feed` through route-level `context` params instead of falling back to the feed default; `/feed` can deep-link into raw item detail pages while honoring query-string source context, pin older cited messages with nearby context above the live stream, refocus that pinned citation window in place through earlier/later context cards without leaving the feed page, and surface both live-feed return and direct raw-item drilldown navigation in the focused citation header while reusing the same upper-shell source/timestamp plus previous/next sequence header as the raw-item destination, plus the same direct source-link escape hatch on both the pinned citation card and the nearby earlier/later context cards when original URLs exist, with count labels and status badges visible on the focused-feed context bands, an in-place `Show more context` path that expands pinned citation context from 2 to 5 same-source messages per side without duplicate same-item refetches, retryable wider-context failures that leave the current pinned citation visible instead of collapsing the surface, and route-level `context` handoffs into `/items/:id` that preserve as much widened same-source context as the raw-item surface supports. The current detail-page loading flows now satisfy the active React hook lint rules |
| Auth/sessions | Cycle 136 | OAuth error handling, admin promotion, blocked/rejected redirects |
| Entity lifecycle | Cycle 117 | Resurrection + alias resolution solid |
| Twitter adapter | Cycle 117 | Circuit breaker + rate limit persistence |
| Pre-summarize | Cycle 134 | Atomic claim, regex, batch_id fixes |
| Correlate | Cycle 120 | Trust weight fixed |
| Synthesize/pulse | Cycle 160 | Daily and pulse prompts now receive recent linked event-chain context and can emit visible `eventChains` summaries alongside catalyst panels and pre/post event analysis |
| Decay | Cycle 117 | Clean |
| Server/API routes | Cycle 176 | Calendar routes still resolve linked entities on save, `/entities/search` powers alias-aware suggestions, `/reports/:id` exposes optional `marketCatalysts` plus `eventChains`, `GET /reports` derives list-level chain previews, `GET /search` supports report/all scope with event-chain previews for matching reports, and `GET /items/:id` now exposes parsed raw source items plus optional neighboring same-source context for chat citation drilldowns |
| RSS ingest | Cycle 124 | Most robust subsystem |
| Sentiment momentum | Cycle 120 | DST-safe, idempotent |
| DB queries | Cycle 159 | Event history now supports inserts, recent same-entity lookups for chain inheritance, and grouped recent-chain reads so synthesis can see multi-step stories for active entities |
| Health monitor | Cycle 124 | NULL source fix, dedup race fix |
| Discord adapter | Cycle 143 | reconnect() for hot token reload |

## Stats

- Total cycles: 269 (evolve cycles 27-280)
- Findings: 398 discovered, 396 resolved, 2 open
- Tests: 1768 passing + 163 dashboard
- Skills: 5 project-local (audit, migration, source, fix, workloop)
