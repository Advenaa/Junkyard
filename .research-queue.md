# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 0 medium** (Cycle 99 — QUEUE EMPTY)

Last scan: Cycle 98 — code-level audit of pre-summarize, decay, llm, embed
Tests: 1056 passing, 0 failing.

## Critical

(none)

## High

- ~~**PS-020**~~ — `src/pre-summarize/index.ts:119` — Atomic claim with UPDATE...RETURNING + FOR UPDATE SKIP LOCKED — **pending**
- ~~**PS-021**~~ — `src/pre-summarize/index.ts:147` — retry_count < 3 guard + increment on parse failure — **pending**
- ~~**EM-023**~~ — `src/embed.ts:240` — Batch length check + null padding for missing embeddings — **pending**

## Medium

- ~~**PS-022**~~ — `src/pre-summarize/index.ts:119` — ORDER BY created_at ASC added — **pending**
- ~~**PS-023**~~ — `src/pre-summarize/index.ts:143` — Token budget 300→500 per item — **pending**
- ~~**LM-020**~~ — `src/llm.ts:88` — Structured status check first + negative lookahead regex — **pending**
- ~~**LM-025**~~ — `src/llm.ts:392` — & escaped to &amp; before <> escaping — **pending**
- ~~**EM-024**~~ — `src/embed-pipeline.ts:31` — ALLOWED_TABLES allowlist validation — **pending**
