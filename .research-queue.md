# Fix Queue — Podders v2

**Open: 2 critical, 11 high, 14 medium** (Cycle 129)

Last scan: Cycle 129 — product-level audit, 27 new findings
Tests: 1380 passing, 0 failing.

## Critical

- **PD-001** — `dashboard/src/pages/Settings.tsx` — No "Add Source" form or dialog. POST /api/v1/sources exists with validation but the SourcesTab has zero UI to invoke it. EmptyState says "Add your first source" but provides no button. Sources can only be created via curl.
- **PD-043** — `src/auth/middleware.ts:48-52` — Middleware unconditionally demotes DB-stored `admin` role to `viewer` if user not in ADMIN_USER_IDS env var. Admin promotion via Settings > Users UI is non-functional — promoted users remain viewer at API layer.

## High

- **PD-008** — `src/server.ts:198-199` — POST /sources doesn't create source_state row. State created lazily on first poll. Toggle PATCH returns 404 for new sources until first poll completes.
- **PD-009** — `dashboard/src/pages/Settings.tsx:72-77` — Toggle logic checks `stateStatus === 'active'` but new sources have null stateStatus. Toggle always sends {enabled: true} (re-enable) for new sources, hitting the 404 from PD-008.
- **PD-006** — `dashboard/src/components/StatusBadge.tsx:1-5` — StatusBadge COLORS map only covers delivered/pending/failed (report statuses). Source states active/disabled/halted all fall through to grey default — visually identical.
- **PD-010** — `dashboard/src/pages/ReportView.tsx:117` — Home page (/) fetches `/reports?limit=1` (list endpoint) which doesn't parse body JSON into keyEvents/entitySentiment/sections. Only TL;DR and date render.
- **PD-011** — `dashboard/src/components/Header.tsx:16-17` — Feed and Chat pages missing from both desktop and mobile nav. Routes exist in router but unreachable without typing URL directly.
- **PD-022** — `dashboard/src/pages/Chat.tsx:22-26` — Conversation history lost on page navigation. Messages stored in useState only — no localStorage/sessionStorage. Server retains context but user sees empty chat on return.
- **PD-030** — `dashboard/src/pages/Settings.tsx:162-271` — digest_time and timezone fetched from GET /config but no input fields rendered. save() only sends webhook_url. Backend supports editing both with validation.
- **PD-052** — `src/server.ts:103` — GET /api/v1/health has zero dashboard callers. System health (DB status, halted sources, cost spikes) invisible to dashboard users.
- **PD-053** — `src/server.ts:297` — GET /api/v1/search has zero dashboard callers. Keyword search inaccessible outside of chat. No search bar in dashboard.
- **PD-002** — `dashboard/src/pages/Settings.tsx` + `src/server.ts` — No edit capability for source label/poll_interval/trust_weight. No PATCH fields for these properties either (only {enabled} supported).
- **PD-003** — `src/server.ts` — No DELETE /sources endpoint. Sources can only be disabled, never removed.

## Medium

- **PD-004** — `dashboard/src/pages/Settings.tsx:86-88` + `dashboard/src/lib/api.ts:14` — Halted source toggle 409 response body discarded; UI shows generic "Failed to toggle" with no explanation of why re-enable is blocked.
- **PD-005** — `dashboard/src/pages/Settings.tsx:138-149` — No visual distinction for halted sources in toggle UI. Halted renders identically to disabled (both "off").
- **PD-007** — `src/server.ts:173-212` — POST /sources additionalProperties:false rejects poll_interval. All sources get default 2h interval with no way to customize.
- **PD-012** — `dashboard/src/pages/Feed.tsx:13,42` — FeedItemRaw.timestamp typed as string but API returns number (epoch-ms BIGINT). Works accidentally but tests use wrong type.
- **PD-013** — `src/server.ts:127` — Report list endpoint SELECT * includes full body column. List only needs id/date/type/tldr/sentiment/status/created_at.
- **PD-015** — `dashboard/src/components/` — 8 dead components: ReportCard, FeedItem, LoadMoreButton, FilterPills, SectionAccordion, KeyEventCard, TldrBlock, SourceCitation.
- **PD-023** — `dashboard/src/pages/Chat.tsx:49-53` — No streaming. Entire LLM response (including tool calls) completes server-side before return. 15-30s with only bouncing dots.
- **PD-024** — `dashboard/src/components/ChatMessage.tsx:11-38` — Markdown rendering only handles **bold** and \`inline code\`. Bullets, headers, links, code blocks render as raw text.
- **PD-025** — `dashboard/src/components/ChatMessage.tsx:40-68` — Tool results opaque to user. ToolUsageIndicator shows tool names but underlying data (search hits, entity data, scores) not inspectable.
- **PD-026** — `dashboard/src/pages/Chat.tsx:59-64` — Generic chat error message with no retry button. Rate limit (429) indistinguishable from server crash.
- **PD-027** — `dashboard/src/pages/Chat.tsx:117-126` — Chat textarea rows=1 with resize-none, no auto-grow. Multi-line queries invisible.
- **PD-031** — `dashboard/src/pages/Settings.tsx` — No API key display or management. Users must check server logs or .env to find API key.
- **PD-040** — `src/auth/discord-oauth.ts:188` — Invite-only rejection returns raw JSON to browser instead of redirecting to /login?error=unauthorized.
- **PD-041** — `src/auth/discord-oauth.ts:193-246` + `dashboard/src/lib/api.ts:10-11` — Blocked users complete full OAuth flow (session created, cookie set) before being rejected on next API call. Login page shows no blocked explanation.

---

## Resolved (Cycles 27-128)

<details>
<summary>350+ findings resolved — click to expand</summary>

### Critical (resolved)

- ~~**CF-010** — `src/server.ts:223-261` + `src/scheduler.ts:72` — PATCH /config doesn't call refreshDailyCron()~~ ✅
- ~~**DA-001** — `src/server.ts:282` — Search endpoint BIGINT vs ISO string~~ ✅

### High (resolved)

- ~~**EM-032** — `src/embed.ts:186,199` — No embedding dimension validation~~ ✅
- ~~**LM-041** — `src/llm.ts:307-308` — 429 Retry-After jitter violates spec~~ ✅
- ~~**CF-011** — `src/server.ts:236-254` — digest_time/timezone not validated on PATCH~~ ✅
- ~~**HM-020** — `src/health.ts:70-73` — checkSourceSilence skips NULL last_fetched_at~~ ✅
- ~~**CH-019** — `src/chat/handler.ts:321+231` — Double-sanitization~~ ✅
- ~~**IP-012** — `src/pre-summarize/index.ts:28-43` — Urgent articles skip pre-summarize~~ ✅
- ~~**SD-016** — `src/index.ts:296-321` — Health catch-up races with onDaily~~ ✅
- ~~**SD-013** — `src/process/synthesize.ts` — Divergence window too narrow~~ ✅
- ~~**DC-013** — `src/ingest/discord.ts` — Queue drain on disconnect~~ ✅
- ~~**PS-001** — `src/pre-summarize/index.ts:198` — Pre-summarize success doesn't reset retry_count~~ ✅
- ~~**AC-002** — `src/auth/discord-oauth.ts:255` — /auth/me missing avatar~~ ✅

### Medium (resolved)

- ~~**HM-021** — Health event dedup non-atomic~~ ✅
- ~~**CH-020** — Mixed nonce on translation failure~~ ✅
- ~~**D-020** — No retry for failed deliveries~~ ✅
- ~~**SM-020** — getSentimentMomentum timezone~~ ✅
- ~~**DA-002** — StatusBadge field mismatch~~ ✅
- ~~**SD-012** — Pulse missing alias-aware lookup~~ ✅
- ~~**CH-017** — Semantic search truncation~~ ✅
- ~~**CH-010** — created_at raw epoch-ms~~ ✅
- ~~**DA-010** — PATCH /config lacks body schema~~ ✅
- ~~**SD-015** — Correlation cutoff off-by-one~~ ✅
- ~~**VE-010** — cosineSimilarity no dimension guard~~ ✅
- ~~**IP-015** — Entity resolution failure deletes summary~~ ✅
- ~~**DL-025** — Response body socket leak~~ ✅
- ~~**DL-028** — Rate-limit retry spin~~ ✅
- ~~**AU-035** — Blocked user sessions not purged~~ ✅
- ~~**NP-040** — HTTP intermediary hops dead code~~ ✅
- ~~**DC-011** — Circuit breaker counts non-error close codes~~ ✅
- ~~**DC-012** — closeAndResume bypasses circuit breaker~~ ✅
- ~~**DC-015** — Reconnect backoff resets too easily~~ ✅
- ~~**LM-032** — Chat translation bypasses nonce~~ ✅
- ~~**CF-002** — alertWebhookUrl not validated~~ ✅
- ~~**EL-002** — Resurrection resets relevance~~ ✅
- ~~**EP-001** — Unembedded summaries pushed out of LIMIT~~ ✅
- ~~**SM-001** — Oversized item marked failed~~ ✅
- ~~**AC-003** — lastFetchedAt type mismatch~~ ✅
- ~~**TW-003** — Rate limit backoff lost on restart~~ ✅
- ~~**EL-005** — No mention tracking on resurrection~~ ✅
- ~~**EL-007** — CoinGecko seeding timeout~~ ✅
- ~~**SC-013** — buildDailyCron out-of-range hours~~ ✅
- ~~**CO-011** — Trust weight first summary only~~ ✅
- ~~**CO-013** — json_agg lacks ORDER BY~~ ✅
- ~~**SY-009** — Entity sentiment schema empty names~~ ✅
- ~~**PL-004** — 300 maxTokens too low~~ ✅
- ~~**SR-009** — Webhook test leaks response body~~ ✅
- ~~**OP-001** — Advisory unlock errors not caught~~ ✅
- ~~**OP-002** — Orphaned embeddings no iteration cap~~ ✅
- ~~**SR-002** — PATCH /sources missing enum validation~~ ✅

</details>
