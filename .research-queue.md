# Fix Queue — Podders v2

**Open: 5 critical, 6 high, 7 medium** (Cycle 79 research)

Last scan: Cycle 79 — system-level audit of 6 integration paths after cycles 75-78 refactoring
Tests: 812 passing, 0 failing.

## Critical

- **CD-001** — `src/server.ts:123` — GET /reports/:id returns flat object, frontend expects `{ report: ... }` wrapper
- **CD-002** — `src/server.ts` (missing) — PATCH /sources/:source/:sourceId endpoint not implemented; source toggle always 404s
- **CD-003** — `src/server.ts` (missing) — PATCH /users/:discordId endpoint not implemented; role changes always fail
- **CD-004** — `src/server.ts` (missing) — GET /api/v1/status endpoint not implemented; Pipeline tab broken
- **CD-005** — `src/server.ts` (missing) — POST /api/v1/config/test-webhook not implemented; test button broken

## High

- **SY-001** — `src/knowledge/sentiment.ts:47-50` — `dateToEpochMsBounds` uses UTC midnight, but dateString is timezone-local; sentiment aggregates wrong 24h window
- **AE-002** — `src/db/migrations.ts:159` — `Date.now()` (~1.7 trillion ms) stored in INTEGER column (max ~2.1 billion); sessions overflow on INSERT
- **CD-006** — `dashboard/src/pages/Settings.tsx:185` — Config PATCH sends camelCase `webhookUrl`, server expects snake_case `webhook_url`
- **CD-007** — `dashboard/src/pages/Settings.tsx:168` — Config GET returns snake_case, frontend expects camelCase
- **CD-008** — `src/server.ts:102` — Reports list ignores `type` query filter; filter pills have no effect
- **HE-001** — `src/health.ts:346` — DB-down prevents alert webhook from firing; insertEvent throws before sendAlertWebhook

## Medium

- **CD-009** — Systemic snake_case/camelCase mismatch between server responses and frontend interfaces
- **CD-010** — `src/server.ts:219` — Feed endpoint ignores `offset` and `after` query params; pagination broken
- **HE-002** — `src/health.ts:193` — `todayStart` parsed in server timezone, not configured timezone (same pattern as SY-001)
- **HE-003** — `src/health.ts:48` — Halted sources (Twitter 401) invisible to health monitoring
- **PP-003** — `src/index.ts:102` — Concurrent source polling shares stale `now` timestamp across async batches
- **ST-006** — `src/index.ts:336` — Server accepts requests before crash recovery runs inside scheduler.start()
- **AE-001** — `src/auth/sessions.ts:51` — `lastCleanupAt` module-scoped, shared across instances
