# Fix Queue — Podders v2

**Open: 0 critical, 3 high, 9 medium** (Cycle 109 — code-level audit of DB, health, scheduler, RSS, config)

Last scan: Cycle 109 — code-level audit of queries, migrations, health, scheduler, RSS, config
Tests: 1214 passing, 0 failing.

## Critical

(none)

## High

- **QR-001** — `src/db/queries.ts` + `src/db/connection.ts` — BIGINT columns typed as `number` but pg returns strings (no type parser)
- **CG-001** — `src/config.ts:88` — Auto-generated API key printed in plaintext to stderr (bypasses pino masking)
- **MG-001** — `src/db/queries.ts:140` + `src/db/migrations.ts:244` — ON CONFLICT (content_hash) may not match partial unique index (verify)

## Medium

- **QR-010** — `src/server.ts:198` — insertSource caller passes epoch-seconds (Date.now()/1000) instead of epoch-ms
- **QR-008** — `src/db/queries.ts:603` — computeDailySentiment AVG() can return null but typed as number
- **QR-009** — `src/db/queries.ts:293` — insertSummary has no ON CONFLICT — crash recovery can duplicate
- **HE-010** — `src/health.ts:311` — Fetch response body never consumed on non-ok webhook — socket leak
- **HE-011** — `src/health.ts:246` — isDuplicate exact message match fails for dynamic cost/silence messages
- **SC-012** — `src/scheduler.ts:68` — refreshDailyCron error after stop() silently loses daily cron
- **RS-010** — `src/ingest/rss.ts:114` — NaN timestamp from unparseable isoDate bypasses Date.now() fallback
- **CG-002** — `src/config.ts:107` — PUBLIC_URL not validated as URL, trailing slash breaks concatenation
- **MG-013** — `src/db/migrations.ts:450` — schema_version table has no PK, multiple rows possible

## Resolved

- ~~**SY-006** — `src/process/synthesize.ts:375` — No report on quiet days/day 1~~ ✅ `pending`
- ~~**EL-004** — `src/knowledge/entities.ts:116` — Tier 2 excludes archived entities~~ ✅ `pending`
- ~~**EL-008** — `src/chat/tools.ts:128` — keyword_search $BTC lookup fails~~ ✅ `pending`
- ~~**EL-012** — `src/knowledge/seed.ts:157` — CoinGecko market_cap_rank dead code~~ ✅ `pending`
- ~~**SY-001** — `src/process/synthesize.ts` — Narratives never consumed by synthesis~~ ✅ `pending`
- ~~**SY-003** — `src/process/synthesize.ts:413` — Stale momentum data used silently~~ ✅ `pending`
- ~~**SY-008** — `src/process/pulse.ts` — Pulse lacks momentum + divergence context~~ ✅ `pending`
- ~~**TW-001** — `src/ingest/twitter.ts` — No circuit breaker for Twitter~~ ✅ `pending`
- ~~**TW-006** — `src/ingest/twitter.ts:11` — Zod schema drops ~15 documented fields~~ ✅ `pending`
- ~~**SR-008** — `src/server.ts:251` — Search missing days and mode params~~ ✅ `pending`
- ~~**SM-008** — `src/process/synthesize.ts:408` — Entity name-to-ID matching with alias fallback~~ ✅ `pending`
