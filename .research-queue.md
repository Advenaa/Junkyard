# Fix Queue — Podders v2

**Open: 0 critical, 3 high, 10 medium** (Cycle 103 code-level research)

Last scan: Cycle 103 — code-level audit of correlate, webhook, normalize, auth, dashboard
Tests: 1056 passing, 0 failing.

## Critical

(none)

## High

- **CO-001** — `src/process/correlate.ts:83` — Trust weight lookup returns arbitrary value when multiple source_ids exist per source type; Map keeps last row, Postgres order undefined
- **NP-032** — `src/normalize/index.ts:177` — No injection re-scan on LLM translation output; adversarial translation could inject instructions that persist through pipeline
- **AU-031** — `src/auth/middleware.ts:54` — Blocked user sessions not deleted; persist in DB, reactivate immediately if user unblocked

## Medium

- **CO-003** — `src/process/correlate.ts:51` — json_agg result has no runtime validation (no zod schema); could silently produce wrong data if pg driver config changes
- **CO-006** — `src/process/correlate.ts:68` — Flash trigger from per-source poll uses default 24h window, not poll's actual window; can cause duplicate flash reports
- **DL-015** — `src/deliver/webhook.ts:136` — embedCharCount omits url field; near-limit embeds could exceed Discord 6000-char total
- **DL-016** — `src/deliver/webhook.ts:199` — Rate-limit + server-error retry combo can block for ~6 minutes total wall-clock time
- **DL-020** — `src/deliver/webhook.ts:277` — DB failure after successful POST causes duplicate Discord delivery on next cycle
- **NP-029** — `src/normalize/index.ts:139` — Short Indonesian content (5-99 chars) silently skips translation; franc returns 'und' for short text
- **NP-033** — `src/normalize/instruct-detector.ts:43` — Human:/Assistant: pattern false-positives on AI discussion content
- **NP-037** — `src/normalize/url-expand.ts:67` — HTTP-to-HTTPS redirect chains not followed; some short URLs fail to expand
- **UI-011** — `dashboard/src/components/Modal.tsx:3` — Modal lacks Escape key dismiss, focus trap, and ARIA attributes
- **UI-014** — `dashboard/src/pages/Settings.tsx:170` — DeliveryTab fetch has no error handling; permanent loading spinner on network error
