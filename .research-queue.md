# Fix Queue — Podders v2

**Open: 0 critical, 4 high, 7 medium** (Cycle 106 system-level research)

Last scan: Cycle 106 — system-level audit of synthesize, twitter, server, sentiment, entities
Tests: 1133 passing, 0 failing.

## Critical

(none)

## High

- **SY-006** — `src/process/synthesize.ts:375` — No report on quiet days/day 1; synthesizer returns null on zero summaries, user sees silence
- **EL-004** — `src/knowledge/entities.ts:116` — Tier 2 co-occurrence filters `status='active'`, excluding archived entities from resurrection; causes duplicate entity creation
- **EL-008** — `src/chat/tools.ts:128` — keyword_search uses `.toLowerCase()` instead of `normalizeAlias()`, so `$BTC` lookups fail (no results for valid entities)
- **EL-012** — `src/knowledge/seed.ts:157` — CoinGecko `/coins/list` never returns `market_cap_rank`; top-100 symbol shortcut is dead code, all symbols get contextualized keys

## Medium

- **SY-001** — `src/index.ts:272` — Narratives computed+stored but never consumed by synthesis; narrative context missing from daily reports
- **SY-003** — `src/process/synthesize.ts:413` — Stale momentum data used silently on rollup failure; no indicator to LLM that data is old
- **SY-008** — `src/process/pulse.ts` — Pulse lacks momentum + divergence context despite prompt expecting it
- **TW-001** — `src/ingest/twitter.ts` — No circuit breaker for Twitter adapter; 5xx errors invisible in source_state (TW-010)
- **TW-006** — `src/ingest/twitter.ts:11` — Zod schema drops ~15 documented fields (viewCount, replyCount, followers, verification, hashtags)
- **SR-008** — `src/server.ts:251` — Search endpoint missing documented `days` and `mode` params; only implements basic ILIKE
- **SM-008** — `src/process/synthesize.ts:408` — Entity name-to-ID matching for momentum uses `entities.name`; may miss if correlator returns non-canonical names
