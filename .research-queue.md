# Fix Queue — Podders v2

**Open: 0 critical, 3 high, 5 medium** (Cycle 98 code-level research)

Last scan: Cycle 98 — code-level audit of pre-summarize, decay, llm, embed
Tests: 1035 passing, 0 failing.

## Critical

(none)

## High

- **PS-020** — `src/pre-summarize/index.ts:119` — Race condition: no claim-before-process. SELECT grabs ready items without setting to processing; summarize pipeline can pick up same items concurrently
- **PS-021** — `src/pre-summarize/index.ts:147` — Infinite retry livelock. Items where parseLabeledOutput returns null remain ready forever; no retry counter or fail-after-N
- **EM-023** — `src/embed.ts:240` — embedBatch result length mismatch silently produces undefined; embed-pipeline reads undefined as not-null, crashes on .model access

## Medium

- **PS-022** — `src/pre-summarize/index.ts:119` — No ORDER BY on LIMIT 100 query; non-deterministic item selection, newer items may starve older
- **PS-023** — `src/pre-summarize/index.ts:143` — Token budget 300 per item may truncate; trailing items parse as null, feeding into PS-021 livelock
- **LM-020** — `src/llm.ts:88` — extractHttpStatus regex matches non-status numbers (e.g., "432ms" → 432 treated as 4xx)
- **LM-025** — `src/llm.ts:392` — sanitizeForPrompt escapes <> but not & first; pre-existing &lt; survives and LLM may interpret as real tags
- **EM-024** — `src/embed-pipeline.ts:31` — SQL interpolation of tableName/textColumn; latent injection if future callers pass user-derived values
