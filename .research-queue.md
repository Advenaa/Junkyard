# Fix Queue — Podders v2

**Open: 0 critical, 3 high, 7 medium** (Cycle 84 research)

Last scan: Cycle 84 — code-level audit of 5 areas (new server endpoints, Discord, chat, embed, LLM)
Tests: 840 passing, 0 failing.

## Critical

(none)

## High

- **SV-004** — `src/server.ts:467` — SSRF TOCTOU in test-webhook: URL validated then fetched separately (DNS rebinding). Should use fetchValidated or pin to resolved IP.
- **DC-002** — `src/ingest/discord.ts:320` — disconnect() leaves concurrency queue promises hanging forever, blocking graceful shutdown (30s timeout kills process)
- **DC-005** — `src/ingest/discord.ts:529` — `new Date(d.timestamp).getTime()` produces NaN on malformed Discord messages, corrupts DB timestamps and breaks downstream pipeline

## Medium

- **SV-001** — `src/server.ts:31` — toCamelCase has no __proto__/constructor/prototype guard (prototype pollution risk on untrusted keys)
- **SV-005** — `src/server.ts:401` — PATCH /users/:discordId accepts arbitrary format discordId, allows phantom user creation
- **SV-010** — `src/server.ts:414` — GET /status uses server timezone for startOfToday, not configured timezone (verify: may also have ms/s mismatch)
- **DC-003** — `src/ingest/discord.ts:521` — Attachment URLs not validated against Discord CDN allowlist; stored SSRF/XSS vector
- **DC-006** — `src/ingest/discord.ts:686` — No circuit breaker on reconnect attempts; non-fatal error loops reconnect forever
- **EP-001** — `src/embed.ts:176` — Query embeddings use RETRIEVAL_DOCUMENT instead of RETRIEVAL_QUERY (reduces search quality)
- **LM-012** — `src/pre-summarize/index.ts:73` — Double sanitization: sanitizeForPrompt called before wrapWithNonce which also sanitizes; angle brackets double-escaped
