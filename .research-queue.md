# Fix Queue — Podders v2

**Open: 0 critical, 3 high, 8 medium** (Cycle 87 research)

Last scan: Cycle 87 — system-level audit of entity lifecycle, RSS ingest, webhook delivery, daily timing, CRUD endpoints
Tests: 916 passing, 0 failing.

## Critical

(none)

## High

- **EL-011** — `src/knowledge/seed.ts:150` — CoinGecko seeder creates conflicting symbol aliases; many tokens share symbols (e.g., "sol"), first-seeded wins arbitrarily
- **EL-015** — `src/knowledge/entities.ts:91-115` — Tier 1 resolves archived entities without reactivating them; archived-entity reactivation code at lines 97-115 is unreachable dead code
- **EL-016** — `src/knowledge/sentiment.ts:47-59` — `dateToEpochMsBounds` timezone offset computed at `now`, not target date; wrong across DST boundaries for non-Jakarta timezones

## Medium

- **EL-010** — `src/knowledge/entities.ts:130` — Tier 2 co-occurrence ignores entity type; `coOccurMap` overwrites on duplicate aliases across types
- **EL-014** — `src/ops/retention.ts` — `entity_sentiment_daily` rows never pruned; unbounded table growth (~3.6M rows/year at 10K entities)
- **RS-010** — `src/ingest/rss.ts:73` — No response body size limit on feed XML download; OOM risk on oversized feeds
- **RS-011** — `src/ingest/rss.ts:76` — No item count cap; feed with 10K+ entries spawns unbounded article extraction
- **RS-012** — `src/server.ts:186` — RSS feed URLs not validated at source creation time; SSRF caught at poll time but source persists
- **TM-002** — `src/scheduler.ts:71-73` — Pulse/health crons use system timezone instead of configured timezone; delivery timing misaligned for non-UTC servers
- **CR-001** — `src/server.ts:372` — Source toggle silently overwrites 'halted' status; re-enabled halted sources immediately re-halt
- **CR-002** — `dashboard/src/pages/Settings.tsx:70` — Frontend toggle uses `sources.enabled` but PATCH updates `source_state.status`; state desync after toggle
