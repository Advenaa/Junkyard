# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 10 medium** (Cycle 91 research)

Last scan: Cycle 91 — code-level audit of normalize, summarize/correlate, webhook, dashboard
Tests: 965 passing, 0 failing.

## Critical

(none)

## High

(none)

## Medium

- **NP-020** — `src/normalize/index.ts:189` — `translated` flag set to true even when translation returned garbage (<20 chars); Indonesian content marked as translated
- **NP-014** — `src/normalize/url-expand.ts:38` — HEAD requests in URL expansion lack DNS pinning (TOCTOU on redirect hops)
- **NP-011** — `src/normalize/index.ts:83` — Content hash stale after translation; hash reflects original Indonesian, not stored English content
- **SM-010** — `src/process/summarize.ts:499` — `itemCount` inflated when chunks are recursively split (reports full chunk size, not sub-chunk)
- **CO-010** — `src/process/correlate.ts:151` — `elevated` urgency + high-trust sources can false-positive flash reports
- **DL-010** — `src/deliver/webhook.ts:55` — `truncate()` can split UTF-16 surrogate pairs, producing invalid strings
- **DL-011** — `src/deliver/webhook.ts:219` — Retry-After has no upper bound clamp and no NaN guard
- **DL-014** — `src/deliver/webhook.ts:315` — Idempotency check is SELECT-then-UPDATE, not atomic; race allows duplicate delivery
- **UI-003** — `dashboard/src/pages/Chat.tsx:44` — Stale response appended to new conversation on rapid "New Chat"
- **UI-007** — `dashboard/src/App.tsx` (router) — No 404/catch-all route; blank page on unknown paths
