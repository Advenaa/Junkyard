# Fix Queue — Podders v2

**Open: 2** (0 critical, 0 high, 2 medium)
Last scan: Cycle 48 — code-level correctness audit of most-changed files (5 agents)
Tests: 567 passing, 0 failing.

## Medium

### [R2-001] sanitizeContent strips diacritics from stored content (unintended)
- **Location**: `src/normalize/index.ts:48` calls `sanitizeContent()` which includes `stripDiacritics()`
- **Description**: NFKC + zero-width stripping is documented in CLAUDE.md layer 1, but diacritic stripping is not. Stored content loses accents (e.g. "café" → "cafe"). stripDiacritics should only run inside detectInjection for scanning, not on stored content.

### [R2-002] Chat token tracking significantly undercounts — misses system prompt, history, tools
- **Location**: `src/chat/handler.ts:234`
- **Description**: Token estimate only counts processedQuery + response. Misses system prompt (~2K), conversation history, tool definitions, and tool results. Daily budget of 100K tokens is reached much later than intended. Translation calls also not tracked.

## Notes (not bugs, documented)
- statement_timeout (30s) could theoretically interfere with CoinGecko bulk seeding — seeding is already batched so individual statements should complete in <30s
- pulse.ts uses `stage: string` not `stage: Stage` type — works but slightly less type-safe
