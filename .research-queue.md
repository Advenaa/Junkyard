# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 28 medium** (Cycle 76 fix-critical+high)

Last scan: Cycle 76 — fixed 1 critical + 10 high + 2 bonus medium (MG-001, AU-023)
Tests: 728 passing, 0 failing.

## Critical

- ~~**CF-001**~~ — RESOLVED (pending) — Missing `pool.on('error')` handler; idle client error crashes process instead of self-healing

## High

- ~~**SM-001**~~ — RESOLVED (pending) — Incorrect blending math: today gets 2x weight in recent average
- ~~**SM-002**~~ — RESOLVED (pending) — System prompt references data never included in pulse user message
- ~~**CF-004**~~ — RESOLVED (pending) — `computeDailySentiment` epoch ms/s mismatch; now uses range-based filtering
- ~~**TW-006**~~ — RESOLVED (pending) — `halted` flag now persisted to source_state DB table
- ~~**AU-020**~~ — RESOLVED (pending) — OAuth consumed-state capped at 10K + single-process documented
- ~~**AU-021**~~ — RESOLVED (pending) — `POST /api/v1/sources` now requires requireAdmin
- ~~**BK-001**~~ — RESOLVED (pending) — Backup now rejects files under 1KB
- ~~**HM-001**~~ — RESOLVED (pending) — Cost spike day boundary now timezone-aware
- ~~**VC-001**~~ — RESOLVED (pending) — `bytesToVector` validates buffer alignment, skips corrupt vectors
- ~~**FE-012**~~ — RESOLVED (pending) — 401 handler returns never-resolving promise instead of throwing

## Medium

### Sentiment Momentum (new — never audited before)
- **SM-003** — `src/knowledge/sentiment.ts:104` — N+2 queries per entity in runDaily transaction (chatty loop, should batch)
- **SM-004** — `src/knowledge/sentiment.ts:205` — getMomentumContext yesterday query can cross-match entity rows
- **SM-005** — `src/knowledge/sentiment.ts:85` — Function-on-column (`to_char(to_timestamp(...))`) prevents index usage; full table scan
- **SM-006** — `src/knowledge/sentiment.ts:76` — No concurrent-run protection on runDaily (scheduler mutex may cover)
- **SM-008** — `src/process/synthesize.ts:135` + `pulse.ts:83` — `parseSummaryBody` uses unsafe `as` cast, no zod validation
- **DV-001** — `src/knowledge/divergence.ts:37` — `minMentions` param invisible to synthesizer interface

### Twitter
- **TW-001** — `src/ingest/twitter.ts:187` — Pagination error mid-stream advances lastId, permanently skipping tweets
- **TW-003** — `src/ingest/twitter.ts:130` — 4xx (400/403) silently falls through to JSON parsing
- **TW-004** — `src/ingest/twitter.ts:187` — No bounds on accumulated tweet count or text length in memory
- **TW-005** — `src/ingest/twitter.ts:11` — Zod `.default(0)` on engagement fields masks API contract changes

### Config/Deploy
- **CF-002** — `src/index.ts:401` — `migrate` CLI command leaks DATABASE_URL password in unmasked error output
- **CF-003** — `src/config.ts:107` — Session cookie values not covered by secret masking architecture
- ~~**MG-001**~~ — RESOLVED (pending) — `SET statement_timeout` promise now has `.catch()` handler
- **MG-003** — `src/db/migrations.ts:411` — Advisory lock held on pooled connection until idle-timeout on error path
- **BK-002** — `src/ops/backup.ts:63` — Timeout kill doesn't clean up .tmp file immediately
- **RT-001** — `src/ops/retention.ts:25` — Delete ordering creates implicit dependency on matching cutoff periods
- **RT-002** — `src/ops/retention.ts:39` — Unbatched orphaned embeddings delete could hold locks on large tables

### Auth/Sessions
- **AU-022** — `src/auth/sessions.ts` — `cleanupExpired()` defined but never called; sessions accumulate
- ~~**AU-023**~~ — RESOLVED (pending) — consumedStates Set capped at 10K (fixed with AU-020)
- **AU-024** — `src/auth/sessions.ts:157` — Double-normalization of User-Agent may invalidate cookie sessions
- **AU-025** — `src/auth/sessions.ts:57` — ULID session IDs have reduced entropy vs cryptographic random
- **AU-026** — `src/auth/sessions.ts:186` — Sliding refresh updates last_refreshed_at but never extends expires_at

### Frontend
- **FE-002** — `dashboard/src/pages/ReportList.tsx:54` — loadMore missing error handling, button stuck on failure
- **FE-003** — `dashboard/src/pages/Feed.tsx:136` — loadMore missing error handling, button stuck on failure
- **FE-004** — `dashboard/src/pages/Feed.tsx:54` — Sources fetch no .catch(), silent failure
- **FE-006** — `dashboard/src/pages/ReportView.tsx:110` — Race condition on rapid id changes
- **FE-009** — `dashboard/src/components/AuthProvider.tsx:28` — Network error on tab focus logs user out
- **FE-010** — `dashboard/src/components/ReportCard.tsx:8` — Sentiment color missing neutral range (inconsistent with ReportView)
- **FE-011** — `dashboard/src/lib/api.ts:6` — options spread overwrites merged headers

### DB/Health/Vector
- **DB-020** — `src/db/queries.ts:250` — Missing index on summaries.created_at
- **DB-021** — `src/health.ts:104` — Missing index on llm_usage.created_at
- **DB-022** — `src/health.ts:136` — Missing index on reports(type, created_at)
- **DB-023** — `src/db/queries.ts:170` — claimBatch index missing source_id column
- **DB-024** — `src/db/queries.ts:412` — getSentimentMomentum unbounded result set
- **DB-025** — `src/db/queries.ts:440` — getRegionalDivergence unbounded result set
- **DB-027** — `src/db/queries.ts:193` — resetCrashed two UPDATEs lack atomicity
- **HM-002** — `src/health.ts:196` — Cost spike 7-day avg excludes zero-activity days
- **HM-003** — `src/health.ts:173` — Pool exhaustion check hardcodes max=10
- **HM-004** — `src/health.ts:304` — DB-down cascades into 6 spurious critical alerts
- **VC-002** — `src/vector-cache.ts:65` — No periodic cache resync mechanism

## Resolved (Cycle 74)

- ~~**EL-002**~~ — RESOLVED (b6fd07f) — Decay checks recent mentions before archiving
- ~~**EL-003**~~ — RESOLVED (b6fd07f) — Summary deleted on entity resolution failure
- ~~**EL-004**~~ — RESOLVED (b6fd07f) — Alias lookup JOINs entities, filters by type
- ~~**EL-005**~~ — RESOLVED (b6fd07f) — Deterministic ORDER BY on multi-row alias matches
- ~~**SD-002**~~ — RESOLVED (b6fd07f) — Correlator injected into synthesizer
- ~~**SD-006**~~ — RESOLVED (b6fd07f) — sourceId populated from summary's source_id column
- ~~**NP-002**~~ — RESOLVED (b6fd07f) — Race condition documented with comment
- ~~**NP-003**~~ — RESOLVED (b6fd07f) — Length re-validation in translation catch block
- ~~**DP-004**~~ — RESOLVED (b6fd07f) — DropAccumulator batches overflow records to health_events
- ~~**CS-004**~~ — RESOLVED (b6fd07f) — Vector cache load limited to last 30 days
- ~~**CS-005**~~ — RESOLVED (b6fd07f) — Dead entity map removed from vector cache
- ~~**CS-006**~~ — RESOLVED (b6fd07f) — Indonesian translation threshold raised to 5x
