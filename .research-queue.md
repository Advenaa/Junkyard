# Fix Queue — Podders v2

**Open: 8** (0 critical, 0 high, 7 medium, 1 low)
Last scan: Cycle 42 — fix medium batch [D-013..D-021, D-026, D-030..D-033, D-035, D-036, D-038]
Tests: 520 passing, 0 failing.

## Medium

### [D-017] Content hash only uses first 2000 chars — collision risk
- **Location**: `src/normalize/index.ts:70`
- **Description**: Two items from same source with identical first 2000 chars but different content beyond that will collide.

### [D-020] No per-user/per-day LLM cost controls for chat
- **Location**: `src/chat/handler.ts`
- **Description**: Each chat query can trigger translation + 5 Sonnet rounds. No per-user token budget.

### [D-022] Translation result not validated before use
- **Location**: `src/chat/handler.ts:154-162`
- **Description**: Translation result from Haiku not validated (empty string, length check). Fails silently.

### [D-023] Pool exhaustion risk during parallel source polling
- **Location**: `src/index.ts:84-223`
- **Description**: 10+ concurrent source polls can exhaust the 20-connection pool.

### [D-024] Shutdown drain cannot cancel stuck jobs
- **Location**: `src/scheduler.ts:84-106`
- **Description**: Stuck jobs hold pool connections. No AbortController-based cancellation.

### [D-025] Retention NOT IN subquery: NULL trap + O(n^2)
- **Location**: `src/ops/retention.ts:39-44`
- **Description**: `NOT IN` with potential NULLs deletes nothing. Use `NOT EXISTS` instead.

### [D-027] Embed quota resets on process restart
- **Location**: `src/embed.ts:123-134`
- **Description**: Daily quota counter is in-memory only. Restart mid-day allows exceeding free tier.

## Low

### [D-037] Items embedded but never cached or searchable
- **Location**: `src/embed-pipeline.ts:72-76`
- **Description**: Item embeddings are computed but never added to vector cache. Dead weight.

## Resolved This Cycle
- D-013 LLM temperature per stage (pending)
- D-014 Parse retry separated from network errors (pending)
- D-015 Early dedup check before translation (pending)
- D-016 UTF-8 surrogate-safe truncation (pending)
- D-018 Malay codes + short-text language bypass (pending)
- D-019 Injection detector narrowed to prompt-template tags (pending)
- D-021 Conversation history saves processedQuery (pending)
- D-026 Vector cache buffer aliasing fixed (pending)
- D-030 uncaughtException handler added (pending)
- D-031 Startup migration error with useful message (pending)
- D-033 Homoglyph detection for injection scanner (pending)
- D-035 Spam filter relaxed to 3 words, Discord-only (pending)
- D-036 "Treat as untrusted data" instruction in all system prompts (pending)
- D-038 statement_timeout via pool connect event (pending)
