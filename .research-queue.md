# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 0 medium** (Cycle 102 — all resolved)

Last scan: Cycle 101 — system-level audit of 4 integration paths
Tests: 1056 passing, 0 failing.

## Critical

(none)

## High

- ~~**VE-002** — `src/vector-cache.ts:83` — Eviction order inverted after load(): DB returns newest-first, Map iterates front-first, evict() removes newest instead of oldest~~ ✅ `pending`

## Medium

- ~~**IP-001** — `src/pre-summarize/index.ts:121` — Pre-summarize claims items without batch_id; recoverStaleProcessing ignores items with NULL batch_id~~ ✅ `pending`
- ~~**IP-004** — `src/pre-summarize/index.ts:175` — LLM error in pre-summarize doesn't increment retry_count; items retry indefinitely~~ ✅ `pending`
- ~~**CQ-002** — `src/chat/handler.ts:229` — Tool results in LLM loop can grow unbounded (10 summaries × 5 rounds, no per-result truncation)~~ ✅ `pending`
- ~~**CQ-010** — `src/chat/tools.ts:63` — Table name interpolated into SQL without runtime validation; mitigated but defense-in-depth gap~~ ✅ `pending`
- ~~**LC-001** — `src/index.ts:351` — Server accepts requests before vectorCache.load() and scheduler.start(); chat queries get empty results~~ ✅ `pending`
- ~~**LC-005** — `src/scheduler.ts:68` — refreshDailyCron() after stop() leaks cron timer; no shuttingDown guard~~ ✅ `pending`
- ~~**VE-003** — `src/vector-cache.ts:122` — Evicted vectors permanently unsearchable; no DB fallback for search~~ ✅ `pending`
