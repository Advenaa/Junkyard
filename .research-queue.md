# Fix Queue — Podders v2

**Open: 0 critical, 7 high, 9 medium** (Cycle 94 research)

Last scan: Cycle 94 — system-level audit of 5 end-to-end paths
Tests: 1003 passing, 0 failing.

## Critical

(none)

## High

- **RS-001** — `src/ingest/rss.ts:142` — HTML content not stripped when `contentSnippet` missing; HTML tags flow into normalize+summarize
- **RS-004** — `src/ingest/rss.ts:167` — RSS engagement always 0, misleads LLM in summarize prompt ("engagement: 0")
- **SP-003** — `src/db/queries.ts:162` — Orphaned 'processing' items not recovered mid-run; only reset on startup via resetCrashed
- **CH-002** — `src/vector-cache.ts:82` — Vector cache loads 30-day window only; older summaries/reports unsearchable
- **CH-003** — `src/vector-cache.ts:121` — MIN_SIMILARITY=0.5 too conservative; valid results filtered out
- **SL-012** — `src/index.ts` (shutdown) — 30s force-exit vs 25s scheduler wait can orphan items
- **EN-001** — `src/knowledge/sentiment.ts:89` — Missing sentiment momentum when runDaily fails; no retry or cache

## Medium

- **EN-005** — `src/knowledge/entities.ts:112` — Tier 2 co-occurrence can resurrect archived entities (missing status filter)
- **HM-001** — `src/health.ts:265` — Alert webhook URL validated only on critical event; invalid URL discovered at worst time
- **SR-001** — `src/server.ts:548` — Port binding EADDRINUSE not caught; cryptic error message
- **ST-002** — `src/index.ts:340` — Health monitor not run before server accepts requests; first /health may timeout
- **CF-002** — `src/config.ts:102` — Model IDs never validated at config load; invalid models fail late during LLM calls
- **RS-007** — `src/ingest/rss.ts:143` — RSS timestamp fallback to Date.now() unlogged; items with missing pubDate get current time
- **CH-004** — `src/chat/handler.ts:206` — Indonesian chat translation errors swallowed; untranslated queries fail semantic search
- **CH-006** — `src/chat/handler.ts:299` — Tool execution failures don't trigger circuit breaker; LLM hammers broken tools 5x
- **SL-010** — `src/scheduler.ts` — digest_time config changes ignored until restart; cron not rebuilt on config update
