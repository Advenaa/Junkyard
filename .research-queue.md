# Fix Queue — Podders v2

**Open: 2** (0 critical, 0 high, 1 medium, 1 low)
Last scan: Cycle 55 — fix high + medium [P-001..P-009]
Tests: 657 passing, 0 failing.

## Medium

### [P-008] No daily report catch-up — failed synthesis lost until next day
- **Location**: `src/index.ts` onDaily
- **Description**: If daily synthesis fails at 09:00, there's no retry. Report lost until next day's cron.

## Low

### [P-010] normalizeAlias("$") returns "" — empty entity name/alias in DB
- **Location**: `src/knowledge/entities.ts:38-40`
- **Description**: Already partially mitigated by rows[0] guards + minimum content gate, but empty string can theoretically be inserted as entity name.

## Resolved This Cycle
- P-001 Minimum content gate (5 chars) drops empty/whitespace (pending)
- P-002 Heartbeat jitter setTimeout tracked + cleared on reconnect (pending)
- P-003 Embed pipeline running guard prevents double-execution (pending)
- P-004 Pulse summaries capped at 50 (newest kept) (pending)
- P-005 Timezone validated with Intl.DateTimeFormat probe in narratives + pulse (pending)
- P-006 Oversized items marked 'failed' instead of resetting to 'ready' (pending)
- P-007 Backup writes to .tmp then renames; stale .tmp cleaned on start (pending)
- P-009 userTokens pruned of stale days in cleanup interval (pending)
