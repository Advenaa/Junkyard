# Fix Queue — Podders v2

**Open: 1 critical, 4 high, 8 medium** (Cycle 112 — system-level audit of chat, ingest, synthesis, embed, dashboard API)

Last scan: Cycle 112 — system-level audit of chat pipeline, ingest→summarize, synthesis+delivery, embed+vector, dashboard API
Tests: 1253 passing, 0 failing.

## Critical

- **DA-001** — `src/server.ts:282` — Search endpoint compares BIGINT created_at against ISO date string (toISOString → type error)

## High

- **CH-019** — `src/chat/handler.ts:321` + `src/chat/handler.ts:231` — Double-sanitization of tool results corrupts nonce tags; sanitizeForPrompt on user query garbles `<>`
- **IP-012** — `src/pre-summarize/index.ts:28-43` — Urgent long articles skip pre-summarize → single-item context overflow → permanent 'failed' status
- **SD-016** — `src/index.ts:296-321` — Health check catch-up can race with onDaily under different mutex keys, wasting LLM call
- **SD-013** — `src/index.ts:269` + `src/knowledge/divergence.ts` — Divergence window only covers midnight→digest_time, missing prior afternoon/evening

## Medium

- **DA-002** — `dashboard/src/pages/Settings.tsx:129` — Frontend StatusBadge expects `status` but API returns `stateStatus`
- **SD-012** — `src/process/pulse.ts:391` — Pulse entity lookup misses aliases (no LEFT JOIN entity_aliases like synthesize has)
- **CH-017** — `src/chat/tools.ts:93-96` — Semantic search fetches 10 results but handler truncates to 2000 chars (~1-2 results)
- **CH-010** — `src/chat/tools.ts:71-76` — Semantic search displays created_at as raw epoch-ms number
- **DA-010** — `src/server.ts:223-248` — PATCH /config lacks Fastify body schema (silently ignores camelCase keys)
- **SD-015** — `src/process/correlate.ts:61` — Correlation cutoff uses `>` instead of `>=`, off-by-one with summaries query
- **VE-010** — `src/vector-cache.ts:23` — cosineSimilarity has no dimension guard, mismatched vectors produce NaN
- **IP-015** — `src/process/summarize.ts:508-529` — Entity resolution failure deletes summary and retries whole chunk (wastes LLM)
