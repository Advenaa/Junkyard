# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 7 medium** (Cycle 80 fix-critical+high)

Last scan: Cycle 80 — fixed 5 critical + 6 high
Tests: 812 passing, 0 failing.

## Critical

- ~~**CD-001**~~ — RESOLVED (pending) — GET /reports/:id now wraps in `{ report: ... }`
- ~~**CD-002**~~ — RESOLVED (pending) — PATCH /sources/:source/:sourceId implemented
- ~~**CD-003**~~ — RESOLVED (pending) — PATCH /users/:discordId implemented
- ~~**CD-004**~~ — RESOLVED (pending) — GET /api/v1/status implemented
- ~~**CD-005**~~ — RESOLVED (pending) — POST /api/v1/config/test-webhook implemented

## High

- ~~**SY-001**~~ — RESOLVED (pending) — `dateToEpochMsBounds` now timezone-aware via Intl offset
- ~~**AE-002**~~ — RESOLVED (pending) — Migration 13: all 20 epoch-ms INTEGER columns widened to BIGINT
- ~~**CD-006**~~ — RESOLVED (pending) — Config PATCH sends snake_case keys
- ~~**CD-007**~~ — RESOLVED (pending) — Config GET mapped to camelCase
- ~~**CD-008**~~ — RESOLVED (pending) — Reports list type filter implemented
- ~~**HE-001**~~ — RESOLVED (pending) — DB-down sends alert webhook directly, bypasses insertEvent

## Medium

- **CD-009** — Systemic snake_case/camelCase mismatch between server responses and frontend interfaces
- **CD-010** — `src/server.ts:219` — Feed endpoint ignores `offset` and `after` query params; pagination broken
- **HE-002** — `src/health.ts:193` — `todayStart` parsed in server timezone, not configured timezone (same pattern as SY-001)
- **HE-003** — `src/health.ts:48` — Halted sources (Twitter 401) invisible to health monitoring
- **PP-003** — `src/index.ts:102` — Concurrent source polling shares stale `now` timestamp across async batches
- **ST-006** — `src/index.ts:336` — Server accepts requests before crash recovery runs inside scheduler.start()
- **AE-001** — `src/auth/sessions.ts:51` — `lastCleanupAt` module-scoped, shared across instances
