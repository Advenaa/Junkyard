# Fix Queue — Podders v2

**Open: 3** (0 critical, 1 high, 2 medium)
Last scan: Cycle 50 — system-level composition audit (4 agents)
Tests: 567 passing, 0 failing.

## High

### [F-003] Blocked users pass requireAuth — can access all API endpoints
- **Location**: `src/auth/middleware.ts:38-58`
- **Description**: No check for `role === 'blocked'`. Frontend redirects blocked users but API is wide open. One-line fix.

## Medium

### [F-001] Webhook delivery + alert webhook vulnerable to DNS rebinding
- **Location**: `src/deliver/webhook.ts:295`, `src/health.ts:274`
- **Description**: Uses `validateUrl` + raw `fetch` instead of `fetchValidated`. Between validation and fetch, DNS could rebind to private IP.

### Doc drift: 8 mismatches between docs and code
- **Locations**: CLAUDE.md (injection layers, env vars, session secret), ARCHITECTURE.md (rate limit 100→50, chat body field, planned endpoints)
- **Description**: Docs accumulated drift over 23 fix cycles. Need updating.

## Test gaps identified (6 modules with zero coverage)
- ops/backup.ts, ops/retention.ts, scheduler.ts, chat/tools.ts, ingest/twitter.ts, ingest/rss.ts
