# Fix Queue — Podders v2

**Open: 3** (0 critical, 0 high, 3 medium)
Last scan: Cycle 32 — test writing (4 dynamic agents, 32 new tests)
Tests: 518 passing, 0 failing.

## Medium

### [CL-005] Flash report TOCTOU race — concurrent triggers can duplicate
- **Location**: `src/process/synthesize.ts:433-439`
- **Category**: data-integrity
- **Description**: Two concurrent flash triggers can both pass the duplicate guard before either inserts. No unique constraint on `(type, date)` in reports table.
- **Suggested fix**: Add unique partial index: `CREATE UNIQUE INDEX ON reports(date) WHERE type = 'flash'`.

### [CL-012] Session IDs logged in plaintext
- **Location**: `src/auth/sessions.ts:83, 125-129, 165`
- **Category**: security
- **Description**: Session IDs appear as `sessionId` field in structured logs. If logs are forwarded to external service, valid session tokens are exposed.
- **Suggested fix**: Truncate to first 8 chars + '...' in log output.

### [SL-015] CoinGecko seeding performance — 15K sequential queries
- **Location**: `src/knowledge/seed.ts:96-136`
- **Category**: performance
- **Description**: CoinGecko seeder processes each of ~15K coins with individual queries.
- **Suggested fix**: Batch inserts.

## Resolved This Cycle
- T-010 normalizeUA tests (10 cases) + OS detection bug fix (pending)
- T-011 Admin revocation test (pending)
- T-012 midnightEpoch tests (7 cases) + decrementDate tests (4 cases) (pending)
- T-013 Health module tests (9 cases) (pending)
