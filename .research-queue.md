# Fix Queue — Podders v2

**7 findings (2 critical, 4 high, 1 medium).** System-level research found 6 new integration issues.

Last scan: Cycle 20 — system-level research (5 trace agents)
Tests: 486 passing, 0 failing.

## Critical (2)

- **I-001** Entity resolution never called — resolveEntities() never invoked from summarizer, entities never persisted [src/process/summarize.ts + src/index.ts]
- **I-002** Seeder never instantiated — CoinGecko + Indonesian seeds never run, entity alias table empty [src/knowledge/seed.ts + src/index.ts]

## High (4)

- **I-003** Embedding order wrong in onDaily — narratives run BEFORE embedPipeline, no embeddings for clustering [src/index.ts:226-229]
- **I-004** Health monitor duplicated — separate instances in index.ts and server.ts [src/index.ts:62 + src/server.ts:20]
- **I-005** Chat handler not wired — POST /api/v1/chat is still a stub, handler never instantiated [src/server.ts + src/chat/handler.ts]
- **W-007** All API routes return {todo:true} — no real implementations [src/server.ts:44-82]

## Medium (1)

- **I-006** GET /api/v1/auth/me lacks explicit auth preHandler [src/auth/discord-oauth.ts:229]
