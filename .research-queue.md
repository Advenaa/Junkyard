# Fix Queue — Podders v2

**Open: 0 critical, 1 high, 2 medium** (Cycle 82 fix-medium + audit)

Last scan: Cycle 82 — CD-009 resolved, post-fix audit found 3 remaining gaps
Tests: 827 passing, 0 failing.

## Critical

(none)

## High

- **CD-012** — `src/db/queries.ts:299` — getSources query missing JOIN to source_state; admin dashboard can't see lastFetchedAt, errorCount, status. Also filters `WHERE enabled = true` hiding disabled sources from admin view.

## Medium

- **CD-013** — `src/server.ts` (feed) — `attachments` column is raw JSON string from DB; should be parsed server-side before returning (frontend has a workaround normalizer)
- **CD-014** — `src/server.ts` (reports/:id) — Report `body` JSON not parsed server-side; `keyEvents`, `entitySentiment`, `sections` fields never extracted for frontend
