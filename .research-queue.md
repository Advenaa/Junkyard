# Fix Queue — Podders v2

**Open: 0 critical, 0 high, 0 medium** (Cycle 97 — QUEUE EMPTY)

Last scan: Cycle 94 — system-level audit of 5 end-to-end paths
Tests: 1035 passing, 0 failing (1 pre-existing flaky: rss.test.ts 'uses synthetic guid').

## Critical

(none)

## High

- ~~**RS-001**~~ — `src/ingest/rss.ts:142` — HTML content stripped via linkedom textContent — **950324a**
- ~~**RS-004**~~ — `src/ingest/rss.ts:167` — RSS engagement sentinel -1 — **950324a**
- ~~**SP-003**~~ — `src/db/queries.ts:224` — recoverStaleProcessing via ULID timestamp — **950324a**
- ~~**CH-002**~~ — `src/vector-cache.ts:82` — Removed 30-day vector cache window — **950324a**
- ~~**CH-003**~~ — `src/vector-cache.ts:121` — MIN_SIMILARITY 0.5→0.3 — **950324a**
- ~~**SL-012**~~ — `src/index.ts:360` — Force-exit 30s→60s — **950324a**
- ~~**EN-001**~~ — `src/knowledge/sentiment.ts:89` — runDaily retry-once — **950324a**

## Medium

- ~~**EN-005**~~ — `src/knowledge/entities.ts:112` — Tier 2 co-occurrence status filter — **pending**
- ~~**HM-001**~~ — `src/health.ts:265` — Alert webhook validated at startup — **pending**
- ~~**SR-001**~~ — `src/server.ts:548` — EADDRINUSE caught with clear message — **pending**
- ~~**ST-002**~~ — `src/index.ts:340` — Health monitor run before server accepts — **pending**
- ~~**CF-002**~~ — `src/config.ts:102` — Model ID format validation at load — **pending**
- ~~**RS-007**~~ — `src/ingest/rss.ts:143` — RSS timestamp fallback logged — **pending**
- ~~**CH-004**~~ — `src/chat/handler.ts:206` — Translation errors caught, fallback to original — **pending**
- ~~**CH-006**~~ — `src/chat/handler.ts:299` — Tool failure counter, 2-strike circuit breaker — **pending**
- ~~**SL-010**~~ — `src/scheduler.ts` — refreshDailyCron rebuilds cron on config change — **pending**
