# Fix Queue — Podders v2

**Open: 1** (0 critical, 0 high, 1 medium)
Last scan: Cycle 58 — system-level composition + deployment sim + test quality + dep audit
Tests: 683 passing, 0 failing.

## Medium

### [S3-001] Daily catch-up can waste one LLM call racing with onDaily
- **Location**: `src/index.ts` onHealthCheck catch-up vs onDaily
- **Description**: Catch-up runs under health-monitor mutex, onDaily under daily-synthesis mutex. If both fire in the same minute, both call synthesizer.runDaily(). Unique index prevents duplicate report, but one Sonnet call is wasted (~$0.02). Fix: add 2-min delay past digest time before catch-up attempts.

## Notes (informational, no fix needed)
- Dependencies clean: 0 vulnerabilities, all critical packages current
- `rss-parser` unmaintained (3y) — works fine, migrate if issues arise
- Test quality rated GOOD across all 4 recently-added test files
- Deployment simulation: system degrades gracefully, only 3 env vars truly required
- All P-series fixes verified to compose correctly
