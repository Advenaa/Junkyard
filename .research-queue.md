# Fix Queue — Podders v2

**Open: 2** (0 critical, 0 high, 1 medium, 1 low)
Last scan: Cycle 34 — system-level research (4 dynamic agents)
Tests: 518 passing, 0 failing.

## Medium

### [SL2-001] runDaily missing 23505 catch despite unique partial index
- **Location**: `src/process/synthesize.ts:378` (insertReport in runDaily)
- **Category**: reliability
- **Description**: runFlash correctly catches 23505 (unique constraint violation on concurrent race), but runDaily does not despite having the same `idx_reports_daily_per_day` unique partial index. A race would throw 23505 caught by outer onDaily try/catch (no crash), but it's inconsistent and logs an error instead of gracefully returning null.
- **Suggested fix**: Add same 23505 catch pattern around runDaily's insertReport call.

## Low

### [SL2-002] Health runChecks uses Promise.all — DB-down loses granular results
- **Location**: `src/health.ts:278-288`
- **Category**: reliability
- **Description**: Only checkDbConnectivity has try/catch. Other DB-dependent checks throw when DB is down, causing Promise.all to reject and lose individual check results. System still returns 503 (no crash), but loses diagnostics.
- **Suggested fix**: Use Promise.allSettled or add try/catch to each check function.
