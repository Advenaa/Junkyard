# Research Lessons

Cycle: 117
Last research type: system-level
Last action: research

---

## Research Strategy

- **Never idle when there are unaudited areas.** Cycle 39's deep dive found 50+ issues. Previous idle cycles were wrong.
- **Target the least-audited modules for maximum yield.** Chat, ops, webhook, normalize had the most critical bugs.
- **Batch medium fixes efficiently.** Cycles 42-43 resolved 21 mediums in 2 commits with 10 parallel agents.
- **System-level audits find integration bugs code-level misses.** Cycle 63 found CH-001 (wrong column) and CF-002 (CHECK constraint).
- **LLM wrapper has subtle retry/backoff issues.** Cycle 66 found timeout non-retry (LM-008), aggressive backoff (LM-002), and cost validation gap (LM-004). These have been stable for 24 cycles — easy to miss.
- **RSS ingest never had a dedicated audit.** Cycle 66 found XXE risk (RS-001) and SSRF on feed URL (RS-002). Important for security.
- **Embedder has broken retry-after parsing.** EM-007 means Gemini 429 always uses 60s default regardless of server preference.
- **Auth system is mostly sound.** Cycle 66 found OAuth state reuse (AU-007) and session limit race (AU-010) but no critical auth bypasses.
- **Semantic search has a fundamental vector mismatch.** Cycle 71 found CS-001: embed-pipeline prepares text with type prefix but chat search embeds raw query. This silently degrades all search results. Highest-impact find since CH-001.
- **Cycle 74: All 12 mediums resolved in one batch.** 6 agents, all succeeded. SD-002 (correlator injection) caused most test updates (31 createSynthesizer calls). CS-005 (entity map removal) was the right call — nothing used entity vectors. DP-004 DropAccumulator pattern is clean for batched audit logging.
- **Cycle 73: Regression tests for cycle 72 fixes.** 12 new tests across 5 files (chat-tools, health, webhook, entities, scheduler). Structural test pattern works well for pipeline ordering (SD-005) — reads source and asserts call positions.
- **Cycle 72: Largest single-cycle fix batch.** 1 critical + 8 high in one commit with 7 parallel agents. All succeeded. Migration 9 adds retry_count column. Test updates needed for interface changes (prepareText on Embedder, atomic content_anchor, retry CASE query).
- **Agent false positives need verification.** Cycle 71: SD-003 (decrementDate timezone) was a false positive — function operates on date strings already in correct timezone. DP-002 (maxTokens) was overstated — 300 per compressed summary is reasonable. Always verify critical findings before queuing.
- **Pipeline ordering matters.** SD-005: narratives are detected after daily synthesis, so daily reports never include narrative context. This is a design-level issue, not a code bug — easy to miss in code-level audits.
- **New features are highest-yield audit targets.** Cycle 75: v2.1 sentiment momentum had never been audited and yielded 2 high + 6 medium findings. New code always has more bugs than mature code.
- **Epoch ms/seconds mismatch is a recurring pattern.** CF-004/SM-007/DB-026 all found the same bug in `computeDailySentiment` — `to_timestamp(created_at)` treats ms as seconds. Three independent agents caught it. Also SM-005 found the same pattern in the sentiment rollup. This is a systemic pattern to watch for in any new code touching `created_at`.
- **Auth double-normalization (AU-024) may be silently breaking all cookie sessions.** The stored normalized UA gets re-normalized on validation, producing a different string. If this is deployed, either nobody uses cookie auth or sessions are silently broken. Must verify before fixing.
- **AU-021 (missing requireAdmin on POST /sources) is a real authz gap.** Viewers can create ingestion sources — this is a write operation that should be admin-only.
- **Cycle 76: 1 critical + 10 high + 2 bonus medium in one commit.** 8 parallel agents, all succeeded. Twitter halt persistence required updating test mocks (makeHaltAwarePool). Synthesize tests needed mockDivergenceTracker added to all 32 createSynthesizer calls — this was a pre-existing gap from the v2.1 commit, not caused by our agents. CF-004 fix also addresses SM-005/DB-026 performance by switching to range-based filtering.

## Coverage Map (updated)

| Area | Last deep audit | Status |
|------|----------------|--------|
| Chat handler | Cycle 39/41/43/63/64/71/94/**112** | CH-019 double-sanitization (high), CH-017 truncation waste, CH-010 epoch display |
| LLM/prompts | Cycle 39/42/66/98/**115** | LM-032 chat translation nonce bypass; LM-033 false positive (CH-019 design) |
| Scheduler/ops | Cycle 39/41/43/60/71/94/**109** | SC-012 refreshDailyCron error loses cron, prior fixes verified OK |
| Webhook/delivery | Cycle 39/40/63/65/71/103/**115** | DL-025 socket leak, DL-028 rate-limit spin |
| Normalize pipeline | Cycle 39/42/43/71/103/**115** | NP-040 HTTP intermediary hops dead code in expandUrl |
| Embed/vector | Cycle 39/42/43/66/71/75/98/**112** | VE-010 dimension guard, prior fixes (VE-002/003, EM-023) all verified OK |
| Config/deploy | Cycle 39/42/63/64/65/75/94/**117** | CF-002 alert webhook not validated at startup, HM-001 fire-and-forget validation |
| Dashboard frontend | Cycle 44-46/63/64/65/75/103/112/**117** | AC-002 avatar missing from /auth/me, AC-003 lastFetchedAt type mismatch |
| Auth/sessions | Cycle 30/32/63/66/75/103/**115** | AU-035 blocked user sessions not purged on role change |
| Entity lifecycle | Cycle 34/60/61/71/94/106/**117** | EL-002 relevance reset on resurrection, EL-005 no mention on resurrection, EL-007 seeding timeout |
| Twitter adapter | Cycle 60/62/75/106/**117** | TW-003 rate limit not persisted across restarts |
| Pre-summarize | Cycle 60/61/62/71/98/**117** | PS-001 retry_count not reset on success, EP-001 embedding gap |
| Correlate | Cycle 60/61/62/71/**103** | CO-001 trust weight nondeterministic (high), CO-003/006 medium |
| Synthesize/pulse | Cycle 60/61/71/75/**106** | SY-006 no quiet-day report, SY-001 narratives unwired, SY-008 pulse lacks momentum |
| Decay | Cycle 60/61/71/98/**117** | Clean — decay wired correctly, archival thresholds reasonable |
| Server/API routes | Cycle 63/64/65/75/**106** | SR-008 search missing days/mode params |
| RSS ingest | Cycle 66/94/**109** | RS-010 NaN timestamp from bad isoDate, prior fixes verified OK |
| Data pipeline flow | Cycle 71/94/**112** | IP-012 urgent article failure (high), IP-015 entity resolution deletes summary |
| Sentiment momentum | Cycle 75/**106** | SM-008 name-to-ID matching gap, SM-009 boundary mismatch |
| DB queries/migrations | Cycle 75/**109** | QR-001 BIGINT as string (high), MG-001 ON CONFLICT partial idx (high), QR-010 epoch-sec, 7 medium |
| Health monitor | Cycle 75/**109** | HE-010 socket leak, HE-011 dedup exact match, prior fixes verified OK |
| Config | Cycle 94/**109** | CG-001 API key in stderr (high), CG-002 PUBLIC_URL not validated |

## Summary: All Evolve Cycles (27-75)

Over 49 cycles:
- **320+ findings** discovered, 210+ resolved
- **728 tests** passing (was 486)
- Cycles 60-66: Found and fixed 30+ issues across LLM, embed, correlate, synthesize, twitter, pre-summarize, chat, delivery, config, dashboard, server routes
- Cycle 71: System-level audit found 21 new findings (1 critical, 8 high, 12 medium) across 6 integration paths
- Cycle 72: Fixed 1 critical + 8 high in one commit
- Cycle 73: 12 regression tests for cycle 72 fixes
- Cycle 74: All 12 remaining mediums resolved in one batch
- Cycle 75: Code-level audit of 6 areas found 1 critical + 10 high + 30 medium (41 total)
- Cycle 76: Fixed 1 critical + 10 high + 2 medium in one commit with 8 parallel agents
- Cycle 77: 84 new regression tests for cycle 76 fixes (728→812 tests). 5 agents, all succeeded.
- Cycle 78: ALL 28 mediums resolved in one massive commit. 8 parallel agents + 1 test-fix agent. Sentiment module refactored (CTE batching, epoch-ms ranges, idempotency guard). Twitter pagination safety, 4xx handling, memory bounds. Auth sessions: crypto random IDs, fixed double-normalization, sliding window expiry, opportunistic cleanup. Health: SUM/7 avg, dynamic pool max, DB-down cascade guard. DB: query limits, atomic resetCrashed, 4 new indexes (migration 12). Frontend: error handling, race guards, sentiment colors, header merge fix. Ops: migrate error masking, advisory lock release, backup tmp cleanup, retention reorder + batched deletes.
- Queue: 0 critical + 0 high + 0 medium (EMPTY)
- Cycle 79: System-level audit after massive refactoring (cycles 75-78). Found 5 critical + 6 high + 7 medium. **Dashboard frontend built ahead of backend** — 4 CRUD endpoints missing from server.ts. Systemic snake_case/camelCase mismatch across all API boundaries. SY-001 (UTC timezone in sentiment bounds) and HE-002 (same pattern in health) are the same root cause — `dateToEpochMsBounds` ignores configured timezone. AE-002 (INTEGER overflow for epoch-ms) is a schema-level bug that would crash session INSERT in production.
- Queue: 5 critical + 6 high + 7 medium
- Cycle 80: Fixed 5 critical + 6 high in one commit. 5 agents, all succeeded. Server.ts gained 4 new CRUD endpoints (source toggle, user roles, pipeline status, webhook test) + report type filter + response wrapper fix. Migration 13 widens 20 epoch-ms INTEGER columns to BIGINT across 11 tables. Sentiment dateToEpochMsBounds now timezone-aware via Intl offset. Health cost spike uses SQL AT TIME ZONE, DB-down sends alerts directly. Settings.tsx fixed snake_case/camelCase for config API.
- Queue: 0 critical + 0 high + 7 medium
- Cycle 81: Fixed 6 mediums (CD-010, HE-002, HE-003, PP-003, ST-006, AE-001). Feed pagination with offset/after params. Halted source monitoring as dedicated critical health check. Fresh Date.now() per source in polling. Crash recovery before server start. Session cleanup state moved inside factory. CD-009 (systemic snake/camelCase) deferred — needs coordinated multi-file changes.
- Queue: 0 critical + 0 high + 1 medium (CD-009)
- Cycle 82: Resolved CD-009 (systemic snake_case/camelCase) with toCamelCase server utility applied to 8 endpoints + frontend interface alignment. Post-fix audit (running in parallel with fix agents) caught 3 remaining API contract gaps: sources missing source_state JOIN (CD-012), feed attachments as raw JSON (CD-013), report body not parsed (CD-014). Parallel audit pattern is powerful — catches what fix agents miss.
- Queue: 0 critical + 1 high + 2 medium
- Cycle 83: Fixed CD-012 (sources JOIN source_state), CD-013 (feed attachments parsed), CD-014 (report body parsed). 13 new structural tests. Queue EMPTY again.
- Cycle 84: Code-level audit of 5 areas — 10 findings (3H + 7M). Discord adapter: NaN timestamps, disconnect queue drain. Server: SSRF TOCTOU in test-webhook. Embed: wrong TaskType for queries. LLM: double-sanitization in pre-summarize.
- Queue: 0 critical + 3 high + 7 medium
- Cycle 85: Fixed all 10 — 3 high + 7 medium in one commit. Queue EMPTY.
- Cycle 86: 76 new regression tests (840→916).
- Cycle 87: System-level audit of 5 untouched paths. Entity lifecycle found 3 high: CoinGecko symbol conflicts (EL-011), archived entity reactivation dead code (EL-015), DST-unsafe timezone offset (EL-016). RSS found 3 medium: no body size limit, no item cap, no URL validation at creation. Timing found pulse/health crons misaligned to system TZ. CRUD found source toggle enabled/status desync. Webhook delivery confirmed clean.
- Queue: 0 critical + 3 high + 8 medium
- Cycle 88: Fixed all 11 (3H + 8M). Queue EMPTY.
- Cycle 89: 49 new regression tests (916→965).
- Cycle 90: Docs update — 5 reference docs updated.
- Cycle 91: Code-level audit of normalize, summarize/correlate, webhook, dashboard. Normalize found translated flag set on garbage (NP-020), TOCTOU in URL expansion (NP-014), stale hash after translation (NP-011). Summarize found inflated itemCount on split chunks (SM-010). Correlate found false-positive flash risk (CO-010). Webhook found surrogate pair truncation (DL-010), uncapped Retry-After (DL-011), non-atomic idempotency (DL-014). Dashboard found stale conversation response (UI-003), no 404 route (UI-007).
- Queue: 0 critical + 0 high + 10 medium
- Cycle 92: Fixed all 10 mediums in one commit. 6 agents, all succeeded. NP-020/NP-011 fix: translated flag + hash recompute only on successful translation. NP-014: DNS pinning on redirect hops via resolvedIp. SM-010: processChunk returns ProcessedChunk with accurate itemCount. CO-010: flash requires breaking-only + weightedSum >= 2.0. DL-010: surrogate pair preservation in truncate(). DL-011: 60s cap + NaN guard on Retry-After. DL-014: atomic UPDATE...RETURNING replaces SELECT-then-UPDATE. UI-003: conversationId ref discards stale responses. UI-007: 404 catch-all route.
- Cycle 93: 38 new regression tests for cycle 92 fixes (965→1003 tests). 5 agents, all succeeded.
- Cycle 94: System-level audit of 5 end-to-end paths (RSS→normalize→summarize, chat query pipeline, scheduler lifecycle, entity→sentiment→synthesis, config→migration→deploy). 16 findings queued (7H + 9M). RSS contentSnippet fallback sends HTML to LLM (RS-001). Vector cache 30-day window means older data unsearchable (CH-002). Orphaned processing items only recovered on restart (SP-003). Several agents reported false positives on by-design items (in-memory conversations, duplicate validation) — filtered out.
- **System-level audits produce more false positives than code-level.** 5 agents returned ~60 findings total, but only 16 survived triage. Many were design choices documented in CLAUDE.md or already fixed in prior cycles.
- Queue: 0 critical + 7 high + 9 medium
- Cycle 95: Fixed all 7 highs in one commit. 5 agents, all succeeded. RS-001 uses linkedom textContent for HTML stripping (already imported). RS-004 changes engagement to -1 sentinel. SP-003 adds recoverStaleProcessing using ULID timestamp decoding — wired to health check (every 5min). CH-002 removes 30-day window from vector cache. CH-003 lowers MIN_SIMILARITY 0.5→0.3. SL-012 extends force-exit 30s→60s. EN-001 adds retry-once with 2s delay in runDaily.
- **ULID timestamp decoding for stale detection** is a reusable pattern. Any ULID-based ID can be time-checked without adding columns.
- Queue: 0 critical + 0 high + 9 medium
- Cycle 96: 32 new regression tests for cycle 95 fixes (1003→1035 tests). 5 agents, all succeeded. Discovered pre-existing flaky test in rss.test.ts ('uses synthetic guid') — hardcoded date `2026-04-03T10:00:00Z` fails after 12:00 UTC due to 2-hour cutoff filter.
- **Skill evolution (Cycle 95)**: Created 4 project-local skills (podders-audit, podders-migration, podders-source, podders-fix). Updated evolve SKILL.md with Step 7 (skill & hook evolution) and `evolve-skills` action type. Skills registry at `.claude/skills/podders-skills-registry.md`.
- Cycle 97: ALL 9 mediums resolved in one commit. 7 agents (8 files), all succeeded. SL-010 (refreshDailyCron) required test fix — log format mismatch (`job` field missing). Chat handler now has translation try/catch (CH-004) and per-tool 2-strike circuit breaker (CH-006). Queue EMPTY.
- **Pre-existing flaky test**: `rss.test.ts` 'uses synthetic guid' — hardcoded `2026-04-03T10:00:00Z` fails after that time passes the 2-hour cutoff. **Fixed Cycle 98** with dynamic dates.
- Cycle 98: Code-level audit of pre-summarize, decay, llm, embed + flaky test fix. 5 agents. Pre-summarize had the most issues (PS-020 race, PS-021 livelock). Decay module is clean — only 2 low-severity nits. LLM has subtle sanitization gap (LM-025 & ordering). Embed batch mismatch (EM-023) can crash pipeline. All 1035 tests now pass (flaky test fixed with dynamic dates).
- **Pre-summarize is the weakest module.** Never had a proper claim-before-process pattern despite being documented in CLAUDE.md. The livelock (PS-021) means some articles may never get summarized.
- Cycle 99: Fixed 3 high + 5 medium in one commit. 4 agents, all succeeded. Pre-summarize now uses atomic claim (FOR UPDATE SKIP LOCKED), retry_count < 3 livelock guard, ORDER BY created_at ASC, and 500 token budget. Embed batch mismatch pads nulls. LLM sanitize escapes & first; extractHttpStatus checks structured properties before regex. Embed-pipeline has ALLOWED_TABLES allowlist. Queue EMPTY again.
- Cycle 100: 21 new regression tests for cycle 99 fixes (1035→1056). 4 agents, all succeeded. **Milestone: Cycle 100, 1056 tests, 350+ findings discovered.**
- Cycle 101: System-level audit of 4 integration paths + skill evolution. Found VE-002 (HIGH): vector cache eviction order is inverted — DB returns newest-first but evict() removes from front of Map, so newest vectors get evicted first. This silently degrades search quality. Also found 7 medium integration issues. Updated podders-audit skill with 8 new bug patterns from cycles 95-100.
- **Vector cache eviction is a subtle Map ordering bug.** JS Maps maintain insertion order, but the query ORDER BY and evict() direction must agree. ORDER BY created_at DESC + front-eviction = wrong. Need ASC.
- Queue: 0 critical + 1 high + 7 medium
- Cycle 102: Fixed 1 high + 7 medium in one commit. 6 agents, all succeeded. VE-002: ORDER BY ASC fixes eviction order. VE-003: async DB fallback for evicted vectors. IP-001: pre-summarize now sets batch_id (ULID) so recoverStaleProcessing finds stuck items. IP-004: LLM error catch block now increments retry_count. CQ-002: per-tool-result truncation (2K chars) + total budget (30K chars). CQ-010: ALLOWED_TABLES allowlist for SQL table names. LC-001: vectorCache.load() moved before startServer(). LC-005: shuttingDown guard in refreshDailyCron(). Queue EMPTY.
- **Making search() async for DB fallback requires updating all callers and tests.** VE-003 fix changed the VectorCache interface — caught 2 test failures from sync assertions.
- Cycle 103: Code-level audit of 5 areas (correlate, webhook, normalize, auth, dashboard). 5 agents, all returned quality findings. 3 high + 10 medium queued (plus many low/info deferred). Correlate has trust-weight nondeterminism (CO-001) when multiple source_ids per source type. Normalize has no injection re-scan on translation output (NP-032). Auth blocked-user sessions persist (AU-031). Webhook has duplicate delivery risk on DB failure (DL-020). Dashboard has many accessibility gaps but no XSS vectors.
- **Translation output is an untrusted LLM boundary.** NP-032: the normalize pipeline scans for injection BEFORE translation, but the Haiku translation call could itself produce injected content. This is a defense-in-depth gap — the nonce wrapping in downstream stages provides some protection, but a detectInjection re-scan after translation would be more robust.
- **Correlate trust-weight query is fundamentally wrong for multi-source setups.** CO-001: `SELECT trust_weight FROM sources WHERE source = ANY($1)` returns multiple rows per source type. The Map deduplication is nondeterministic. This has been present since the module was written.
- **Dashboard accessibility is systematically weak.** No ARIA attributes on modals, selects, or toggle switches. No keyboard dismiss on overlays. This is a pattern, not individual bugs — consider a focused accessibility pass.
- Queue: 0 critical + 3 high + 10 medium
- Cycle 104: Fixed 3 high + 10 medium in one commit. 7 agents, all succeeded. CO-001: trust weights now queried per (source, source_id) pair via summaries metadata — query order changed to correlation→summaries→trust. CO-003: json_agg runtime validation with string fallback. CO-006: per-source poll passes min_ts as cutoff. NP-032: detectInjection re-scan on translation output. NP-029: translate msa/zlm Malay variants. NP-033: claude-role-marker requires injection-indicative follow-up. NP-037: HTTP intermediary hops followed in URL expansion. AU-031: blocked users get session deleted + cookie cleared. DL-015/016/020: embed char count includes URL, 2min total retry cap, duplicate delivery prevented. UI-011: Modal Escape + ARIA. UI-014: DeliveryTab error handling. Queue EMPTY.
- **Correlate query reorder breaks test mocks.** CO-001 fix changed query order from (correlation, trust, summaries) to (correlation, summaries, trust). Test helper `buildBatchPool` and batch query assertions needed updating. Always check mock response order matches actual query order.
- **Injection pattern tests must avoid triggering earlier patterns.** NP-033 test inputs for claude-role-marker must not contain "ignore previous" (caught by ignore-previous) or XML closing tags (caught by xml-closing-tag). Patterns are checked sequentially — the first match wins.
- Cycle 105: 76 new regression tests for cycle 102+104 fixes (1057→1133 tests). 6 agents, all succeeded. Covers correlate (CO-001/003/006), normalize (NP-032/029/033/037), auth (AU-031), webhook (DL-015/016/020), vector cache (VE-003), chat (CQ-002/010), scheduler (LC-001/005), pre-summarize (IP-001/004).
- Cycle 106: System-level audit of 5 integration paths (synthesize+pulse, twitter, server routes, sentiment momentum, entity lifecycle). 11 findings queued (4H + 7M). SY-006 (HIGH): no report on quiet days — silence looks like system failure. EL-004/008/012 (HIGH): Tier 2 excludes archived entities, $BTC chat lookup fails, CoinGecko market_cap_rank dead code. SY-001: narratives computed but never fed to synthesis. TW-001: no circuit breaker for Twitter. SR-008: search endpoint missing documented features.
- **Narratives are fully dead weight.** SY-001: narrativeDetector runs daily, makes LLM calls to name clusters, stores results in `narratives` table, but synthesize.ts never queries that table. The CLAUDE.md promise of "cluster-based thematic analysis fed to Sonnet" is unwired. This has been true since the feature was added.
- **CoinGecko seeder uses wrong API endpoint.** EL-012: `/coins/list` returns name/symbol/id but NOT `market_cap_rank`. The code branches on rank <= 100 for empty context_key, but rank is always undefined → all symbols get `coingecko:` prefix → Tier 1 alias lookup degrades for common tokens like BTC, ETH.
- **Entity keyword_search alias normalization mismatch.** EL-008: entities.ts uses `normalizeAlias()` which strips leading `$`, but chat tools.ts uses `.toLowerCase()`. User searching `$BTC` gets `$btc`, but aliases table has `btc`. Simple fix: import and use normalizeAlias.
- Queue: 0 critical + 4 high + 7 medium
- Cycle 107: Fixed 4 high + 7 medium in one commit. 7+3 agents (7 fix + 3 test-fix), all succeeded. SY-006: quiet-day reports now generated instead of silence. SY-001: narratives table queried and injected as XML context in daily synthesis. SY-003: stale momentum data annotated with date warning. SM-008: entity alias fallback in momentum lookup. EL-004: Tier 2 includes archived entities + resurrection. EL-008: normalizeAlias in chat keyword_search. EL-012: TOP_SYMBOLS Set replaces dead market_cap_rank check. SY-008: pulse now receives sentimentTracker+divergenceTracker. TW-001: consecutive-failure circuit breaker (10 failures). TW-006: zod schema expanded with 8+ fields. SR-008: search endpoint gains days + mode params. Queue EMPTY.
- **Search endpoint is broken after BIGINT migration.** DA-001: `created_at > $2` passes ISO date string but column is BIGINT epoch-ms. PostgreSQL throws type error. Introduced when SR-008 added days/mode params (cycle 107) — the cutoff uses `toISOString()` but should use `.getTime()`. This was masked because no integration test hits the actual DB search path.
- **Chat double-sanitization silently degrades quality.** CH-019: handler.ts applies `sanitizeForPrompt` to ALL tool results, but tools already sanitize internally. This destroys nonce-wrapped XML tags and double-escapes HTML entities. Also CH-011: user queries get sanitized, converting `<` to `&lt;`. Both are the same root cause — sanitizeForPrompt applied too broadly.
- **Health catch-up and daily synthesis use different mutex keys.** SD-016: onDaily runs under `'daily-synthesis'` mutex, onHealthCheck under `'health-monitor'`. Health catch-up calls `synthesizer.runDaily()` which can race with the real onDaily. Both can pass `dailyReportExists()` before either inserts.
- **node-postgres returns BIGINT as strings.** QR-001: OID 20 (INT8) has no type parser set, so all epoch-ms BIGINT columns come back as strings at runtime. TypeScript types say `number` but actual values are `"1712345678000"`. Arithmetic like `row.created_at + 86400000` silently produces string concatenation. Fix: `pg.types.setTypeParser(20, parseInt)` in connection.ts. This is a systemic issue affecting every query with timestamp columns.
- **console.error bypasses pino secret masking.** CG-001: auto-generated API key printed via console.error, not through the pino logger. If stderr goes to a log file (pm2/systemd), the secret is in plaintext on disk.
- **DB queries module has 5 dead exports.** QR-003-007: markProcessed, insertEmbedding, insertHealthEvent, upsertSentimentDaily, getSentimentMomentum are exported but never imported in src/. The sentiment pipeline functions suggest the daily rollup is incomplete.
- **ON CONFLICT with partial unique indexes needs WHERE predicate.** MG-001: PostgreSQL requires the index predicate in the ON CONFLICT clause when targeting a partial unique index. Without it, Postgres can't match the conflict target. Needs runtime verification.
- **Epoch-seconds vs epoch-ms is still recurring.** QR-010 found insertSource caller in server.ts divides Date.now() by 1000. This is the same class of bug as CF-004/SM-007 — now a total of 4 independent occurrences across the codebase.
- **Interface changes require test mock updates.** createPulse gained 2 params, runDaily gained 2 new DB queries + changed empty-summary behavior. Test mocks must match query order exactly. Spawning test-fix agents immediately after main agents is the right pattern.
- **Quiet-day reports prevent user confusion.** SY-006: silence on quiet days looked like system failure. Now a brief "quiet market" report is generated. The LLM receives yesterday's TLDR + any available momentum/divergence for continuity.

- Cycle 115: Code-level audit of LLM, webhook, auth, normalize, Discord. 5 agents, all returned findings. 1 high + 8 medium queued. LM-033 (tool results without sanitization) is a false positive — conflicts with CH-019 design decision where tools handle own sanitization. Discord adapter has 3 circuit breaker blind spots (DC-011/012/015) that compound — non-error close codes inflate failure count while closeAndResume bypasses it entirely. DC-013 (HIGH) is the queue drain race from cycle 84 but with a new angle — waiters unblocked during drain can execute onMessage callbacks after shutdown flag is set.
- **LM-033 false positive pattern.** Agent flagged "tool results pass unsanitized to LLM" but CH-019 (cycle 113) intentionally removed handler-level sanitization because each tool handles its own. This is a design decision, not a gap. When triaging, always cross-reference new findings against prior intentional changes.
- **Discord circuit breaker has 3 interacting bugs.** DC-011 (non-error codes counted), DC-012 (closeAndResume bypass), DC-015 (premature backoff reset) compound together: normal reconnects inflate the failure counter while intentional reconnects bypass it, and any brief success resets the backoff. Net effect: circuit breaker is unreliable in both directions.
- Cycle 116: Fixed 1 high + 8 medium in one commit. 5 agents, all succeeded. Discord adapter: shutdown guard in handleMessageCreate (DC-013), extracted checkCircuitBreaker helper, only non-resumable codes count as errors (DC-011), closeAndResume now increments errorCount + checks breaker (DC-012), backoff resets only after 30s stable connection via connectedAt timestamp (DC-015). Webhook: response body cancel on non-2xx (DL-025), 1s floor on Retry-After (DL-028). Auth: deleteAllForUser purges sessions on block (AU-035). Normalize: validateIntermediateHop allows HTTP hops with SSRF checks minus HTTPS requirement (NP-040). Chat: translated queries get wrapWithNonce (LM-032). 4 structural test assertions updated for checkCircuitBreaker extraction.
- **Extracting helpers from large methods breaks structural tests.** DC-011/012 fix extracted checkCircuitBreaker from handleClose. Tests that asserted patterns inside handleClose had to be repointed to the new method. Always check structural tests when refactoring method boundaries.
- Cycle 117: System-level audit of 5 integration paths (config/deploy, entity/decay, pre-summarize pipeline, twitter→correlate, dashboard→API). 5 agents, all returned findings. Heavy false positive rate — 10+ findings rejected as already fixed (EL-004, CO-001, IP-015), by design (shutdown order, momentum exclusion), or non-issues (ULID waste, hash race). 2 high + 8 medium queued after triage.
- **System-level audits generate many duplicates of prior fixes.** 5 of 50+ raw findings were exact duplicates of fixes from cycles 104-113. Agents lack context of what's already been fixed. Triage step is critical — always cross-reference against resolved findings.
- **Pre-summarize retry_count is a systemic gap.** PS-001: success path doesn't reset retry_count. This means items that failed pre-summarize once then succeed still carry elevated retry_count into the summarizer, exhausting the 3-retry budget prematurely. The fix is trivial (add `retry_count = 0` to the UPDATE) but the impact is significant — items silently marked 'failed' after fewer real failures.
- **Dashboard API contract audits find type mismatches.** AC-002 (avatar missing from /auth/me) and AC-003 (lastFetchedAt number vs string) are both cases where server returns one type and dashboard TypeScript expects another. JavaScript's loose coercion hides these — they work at runtime but the types lie.

## Recent Commits
- 04ec5dd feat: v2.1 — sentiment momentum
- b6fd07f fix: 12 medium [EL-002..CS-006]
- e7e2fd8 ci: add GitHub Actions CI/CD pipeline
- dec61bd test: 12 new regression tests for cycle 72 fixes
- 9f370ba fix: 1 critical + 8 high [CS-001..DP-001]
