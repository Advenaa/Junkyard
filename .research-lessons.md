# Research Lessons

Cycle: 30
Last research type: code-level
Last action: research

---

## Research Strategy

- **System-level audits find the highest-impact issues.** Cycle 27 found Discord stub + report race.
- **Code-level audits catch bugs introduced by fixes.** Cycle 30 found that the narrative TZ fix (SL-013) introduced a critical bug: midnightEpoch is wrong for west-of-UTC timezones. Always audit recent fixes.
- **Timezone math is consistently the hardest thing to get right.** Three cycles have now found timezone bugs. The midnightEpoch function needs a complete rewrite using a robust offset calculation approach.
- **Security changes need security review.** trustProxy:true was introduced to fix SL-005 (IP mismatch) but introduced CL-002 (anyone can spoof IP). The fix created a new vulnerability.

## High-Value Patterns to Scan

- **Timezone/date math bugs** — third cycle finding these. midnightEpoch wrong for UTC- and fractional offsets.
- **Missing rows[0] guards after INSERT ON CONFLICT + SELECT** — found in entities.ts Tier 3 at 3 locations.
- **normalizeAlias consistency** — still incomplete. Tier 3 match (line 207) and seed.ts still use toLowerCase.
- **Empty string edge cases** — normalizeAlias("$") = "" can create empty aliases.
- **trustProxy security** — `true` is too permissive, need specific proxy IPs.
- **try/catch around DB calls in non-critical paths** — handleTokenDeath, narrative app_config query.

## Agent Tuning

- **Code-level research after fixes is essential.** Cycle 30 found a critical bug (midnightEpoch) introduced by cycle 29's fix.
- **6 agents for code-level audit scales well.** discord+index, report chain, auth, health+entity, narrative+LLM, test coverage. Good domain separation.
- **Test coverage auditor is valuable.** Identified 4 high/medium test gaps that correspond to actual bugs.

## Coverage Assessment

- **486 tests passing.**
- **Test gaps identified**: normalizeUA (no test), admin revocation (no test), midnightEpoch (no test), health module (zero tests).
- **14 open findings** (1 critical, 3 high, 9 medium, 1 low).
- **Next priority**: Fix CL-001 (midnightEpoch), CL-002 (trustProxy), CL-003 (handleTokenDeath), CL-004 (rows[0] guard).

## Recent Commits
- (pending) research: code-level audit [CL-001..CL-013]
- 427025f fix: medium batch [SL-007..SL-010, SL-013, SL-014, SL-016]
- 00ad708 fix: critical + high [SL-001..SL-006, SL-011, SL-012]
- 3c336c5 research: system-level audit [SL-001..SL-015]
- 0c6fc20 fix: error handling + security hardening [E-001..E-010]
