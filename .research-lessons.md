# Research Lessons

Cycle: 28
Last research type: system-level
Last action: fix-critical

---

## Research Strategy

- **System-level audits find the highest-impact issues.** Cycle 15's integration audit found the #1 problem (nothing wired) that 5 prior code-level cycles missed entirely.
- **Cycle 27 system-level audit found 2 criticals code-level missed.** Discord channels hardcoded empty (entire ingest path broken) and getLatestReport race (reports created but never delivered). Both invisible to code-level audits.
- **Alternate system-level and code-level research.** Code-level finds bugs; system-level finds "it doesn't work at all." Both are necessary.
- **Comparing code vs docs is high-yield.** Cycle 2 found 5 critical/high issues by diffing code against PIPELINE.md, QUERIES.md.
- **Tracing data flow end-to-end catches integration gaps.** The INSERT-then-SELECT-latest pattern in report delivery was only visible when tracing the full flash/daily/pulse paths.
- **Reading error handling paths (catch blocks) finds more than scanning for missing try/catch.**

## High-Value Patterns to Scan

- **Timezone/date math bugs** — narrative window misalignment (SL-013) confirms this pattern.
- **INSERT-then-SELECT-latest race** — the getLatestReport pattern was fundamentally racy. Now fixed with RETURNING *. Scan for similar patterns.
- **Stub functions that were never completed** — Discord getDiscordChannels() was a stub for 10+ cycles.
- **N+1 query loops** — CoinGecko seeder is 15K sequential queries (SL-015).
- **`ON CONFLICT DO NOTHING` without logging** — found in entity aliases, CoinGecko seed.

## Agent Tuning

- **Dynamic agents > fixed roles.** Every cycle uses custom agents designed for the specific work.
- **One agent per file avoids merge conflicts.** Cycle 28 used 5 agents across 8 files with 0 conflicts.
- **Test updates must accompany API changes.** Cycle 28 changed return types (MarketReport → ReportRow) in 3 files, requiring 3 test file updates. Plan for this in agent design or handle in verification step.
- **Mock pool insertReport must return RETURNING * row.** After changing insertReport to use RETURNING *, all mock pools that simulate INSERT must return `{ rows: [<ReportRow>] }` not `{}`.

## Coverage Assessment

- **System wiring**: COMPLETE — index.ts connects all modules including Discord channels from DB.
- **Report delivery**: FIXED — no more INSERT-then-SELECT-latest race. All 3 paths (flash/daily/pulse) use returned ReportRow directly.
- **Source error tracking**: FIXED — error_count resets on success, UPSERT for first-poll errors.
- **486 tests passing**, all updated for new return types.
- **Remaining 8 medium findings** — auth UX (SL-007,008,009,016), health (SL-010), narratives (SL-013), entities (SL-014), performance (SL-015).

## Recent Commits
- (pending) fix: critical + high fixes [SL-001..SL-006, SL-011, SL-012]
- 3c336c5 research: system-level audit [SL-001..SL-015]
- 0c6fc20 fix: error handling + security hardening [E-001..E-010]
- ab36739 feat: implement API routes + code-level audit [W-007]
- 01f30e6 fix: integration wiring [I-001..I-006]
